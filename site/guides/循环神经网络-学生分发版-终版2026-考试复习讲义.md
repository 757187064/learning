# 循环神经网络-学生分发版-终版2026：期末考试复习讲义

> 使用方式：先通读“学习路线”，再按章节背“必记句子”，最后用每章自测题检查。这里不是课件原文搬运，而是按考试复习顺序重写。

## 一、这份课件先解决什么问题

这一类课件处理有先后顺序的数据。复习时先理解序列、时间步和隐藏状态，再看 RNN、GRU、LSTM 的差异。

考试常考 shape、hidden state、output 与 h_n 的区别，以及门控结构为什么能缓解长期依赖问题。

## 二、学习路线

1. **第五章 循环神经网络**：先抓 GRU、LSTM、Padding、Packing、1D卷积。
2. **第五章 循环神经网络 / 第一节 序列数据与循环神经网络**：先抓 隐藏状态、双向RNN、RNN、激活函数、梯度消失。
3. **第五章 循环神经网络 / 第二节 门控循环单元（GRU）**：先抓 Sigmoid、激活函数、GRU、隐藏状态。
4. **第五章 循环神经网络 / 第三节 长短时记忆网络（LSTM）**：先抓 Sigmoid、激活函数、隐藏状态、梯度消失、RNN。
5. **第五章 循环神经网络 / 第四节 变长序列处理：Padding与Packing**：先抓 Padding、Packing、隐藏状态、Batch、PackedSequence。
6. **第五章 循环神经网络 / 第五节 1D卷积在序列数据上的应用**：先抓 卷积、感受野、1D卷积、RNN、GRU。
7. **第五章 循环神经网络 / 第六节 PyTorch实现综合案例**：先抓 Dropout、学习率、梯度爆炸、梯度裁剪、Adam。

## 三、章节详解

## 第 1 部分：第五章 循环神经网络

### 1. 本节先看什么

这一节先把 **GRU、LSTM、Padding、Packing** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| GRU | 用更新门和重置门控制历史信息保留与遗忘，是简化版门控循环网络。 | 常和 LSTM 对比门结构和参数量。 | GRU 没有单独的细胞状态 c_t。 | GRU：更新门 + 重置门。 |
| LSTM | 通过输入门、遗忘门、输出门和细胞状态保存长期信息。 | 常考门控作用，以及为什么能缓解长序列梯度问题。 | 不要漏掉细胞状态是 LSTM 的关键通道。 | LSTM：输入门、遗忘门、输出门、细胞状态。 |
| Padding | 把不同长度的序列补到同一长度，方便组成 batch 输入模型。 | 常考为什么需要 padding，以及它和 packing 的关系。 | Padding 补出来的位置不是真实信息，训练时要避免把它当有效时间步。 | Padding：补齐长度；Packing：跳过无效补位。 |
| Packing | 把变长序列的有效部分打包，让 RNN 少处理 padding 位置。 | 常考 pack_padded_sequence 的目的和使用前提。 | Packing 不是改变序列含义，而是提高变长序列处理效率。 | Packing 让 RNN 聚焦真实时间步。 |
| 1D卷积 | 在序列的一维时间/位置方向上滑动卷积核，提取局部 n-gram 或短期模式。 | 常和 RNN 对比：1D 卷积并行提取局部模式，RNN 逐步传递隐藏状态。 | 1D 卷积适合局部依赖，不等于天然拥有长期记忆。 | 1D 卷积：沿序列维度提取局部模式。 |

### 3. 像考试答案一样组织语言

- **GRU**：用更新门和重置门控制历史信息保留与遗忘，是简化版门控循环网络。 考试写作时要补一句：常和 LSTM 对比门结构和参数量。 易错点是：GRU 没有单独的细胞状态 c_t。
- **LSTM**：通过输入门、遗忘门、输出门和细胞状态保存长期信息。 考试写作时要补一句：常考门控作用，以及为什么能缓解长序列梯度问题。 易错点是：不要漏掉细胞状态是 LSTM 的关键通道。
- **Padding**：把不同长度的序列补到同一长度，方便组成 batch 输入模型。 考试写作时要补一句：常考为什么需要 padding，以及它和 packing 的关系。 易错点是：Padding 补出来的位置不是真实信息，训练时要避免把它当有效时间步。
- **Packing**：把变长序列的有效部分打包，让 RNN 少处理 padding 位置。 考试写作时要补一句：常考 pack_padded_sequence 的目的和使用前提。 易错点是：Packing 不是改变序列含义，而是提高变长序列处理效率。
- **1D卷积**：在序列的一维时间/位置方向上滑动卷积核，提取局部 n-gram 或短期模式。 考试写作时要补一句：常和 RNN 对比：1D 卷积并行提取局部模式，RNN 逐步传递隐藏状态。 易错点是：1D 卷积适合局部依赖，不等于天然拥有长期记忆。

### 4. 本节自测

- 判断：GRU只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：LSTM只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Padding只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Packing只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：1D卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- GRU：更新门 + 重置门。
- LSTM：输入门、遗忘门、输出门、细胞状态。
- Padding：补齐长度；Packing：跳过无效补位。
- Packing 让 RNN 聚焦真实时间步。
- 1D 卷积：沿序列维度提取局部模式。

## 第 2 部分：第五章 循环神经网络 / 第一节 序列数据与循环神经网络

### 1. 本节先看什么

这一节先把 **隐藏状态、双向RNN、RNN、激活函数** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 隐藏状态 | RNN 在每个时间步保存的当前记忆，用来把历史信息传给后续时间步。 | 常考 h_t 与 h_{t-1} 的关系，以及 output 和 h_n 的区别。 | 隐藏状态不是模型参数，而是随输入序列变化的中间表示。 | h_t = 当前输入 + 过去记忆的综合表示。 |
| 双向RNN | 同时从前往后和从后往前读序列，让当前位置利用左右两侧上下文。 | 常考它为什么比单向 RNN 能看到更多上下文。 | 双向 RNN 不适合严格只能用过去信息的实时预测场景。 | 双向 RNN = 正向隐藏状态 + 反向隐藏状态。 |
| RNN | 按时间步处理序列，用隐藏状态把前面信息传到后面。 | 常考隐藏状态、序列建模、梯度消失/爆炸。 | RNN 不是一次性把所有时间步完全独立处理。 | 当前输出依赖当前输入和上一时刻隐藏状态。 |
| 激活函数 | 给线性变换加入非线性，使网络能拟合非线性关系。 | 常考没有激活函数时多层线性网络仍等价于线性模型。 | 不要把激活函数说成只改变维度，它主要提供非线性。 | 仿射变换 + 激活函数 = 神经网络层的基本模式。 |
| 梯度消失 | 反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。 | 常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。 | 梯度消失不是过拟合；它是参数难以有效更新的问题。 | 多个小于 1 的因子连乘，会让梯度趋近 0。 |
| 堆叠RNN | 把多层 RNN 叠起来，上一层的输出序列作为下一层输入。 | 常考堆叠带来更强表达能力，也增加训练难度。 | 堆叠不是时间步变多，而是层数变深。 | 堆叠 RNN：时间方向 + 层方向都要传播。 |
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |
| Padding | 把不同长度的序列补到同一长度，方便组成 batch 输入模型。 | 常考为什么需要 padding，以及它和 packing 的关系。 | Padding 补出来的位置不是真实信息，训练时要避免把它当有效时间步。 | Padding：补齐长度；Packing：跳过无效补位。 |
| Dropout | 训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 | 常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 | Dropout 不是提高模型容量，而是正则化。 | Dropout：训练随机失活，推理正常使用。 |
| Seq2Seq | 编码器把输入序列压成表示，解码器再生成输出序列，常用于翻译、问答等序列到序列任务。 | 常考 Encoder-Decoder 结构、Teacher Forcing 和注意力机制为何被引入。 | 普通 Seq2Seq 容易受固定长度上下文瓶颈影响。 | Seq2Seq = Encoder + Decoder。 |
| Batch | 一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 | 常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 | Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。 | Batch 是一批样本；Epoch 是全训练集完整训练一遍。 |
| PackedSequence | PyTorch 中表示变长序列打包后的对象，内部只保留有效时间步，交给 RNN 处理时可以跳过 padding。 | 常考 pack_padded_sequence 与 pad_packed_sequence 的前后关系。 | PackedSequence 不是普通张量；使用前后要注意 lengths、batch_first 和是否排序。 | 先 padding 组成 batch，再 packing 跳过补位，必要时再 pad 回来。 |
| 梯度爆炸 | 梯度在反向传播中变得过大，使参数更新剧烈，训练损失可能震荡或变成 NaN。 | 常考梯度裁剪为什么能缓解 RNN 训练不稳定。 | Dropout 主要缓解过拟合，不是解决梯度爆炸的标准答案。 | 梯度裁剪限制梯度范数，防止更新步子过大。 |

### 3. 像考试答案一样组织语言

- **隐藏状态**：RNN 在每个时间步保存的当前记忆，用来把历史信息传给后续时间步。 考试写作时要补一句：常考 h_t 与 h_{t-1} 的关系，以及 output 和 h_n 的区别。 易错点是：隐藏状态不是模型参数，而是随输入序列变化的中间表示。
- **双向RNN**：同时从前往后和从后往前读序列，让当前位置利用左右两侧上下文。 考试写作时要补一句：常考它为什么比单向 RNN 能看到更多上下文。 易错点是：双向 RNN 不适合严格只能用过去信息的实时预测场景。
- **RNN**：按时间步处理序列，用隐藏状态把前面信息传到后面。 考试写作时要补一句：常考隐藏状态、序列建模、梯度消失/爆炸。 易错点是：RNN 不是一次性把所有时间步完全独立处理。
- **激活函数**：给线性变换加入非线性，使网络能拟合非线性关系。 考试写作时要补一句：常考没有激活函数时多层线性网络仍等价于线性模型。 易错点是：不要把激活函数说成只改变维度，它主要提供非线性。
- **梯度消失**：反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。 考试写作时要补一句：常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。 易错点是：梯度消失不是过拟合；它是参数难以有效更新的问题。
- **堆叠RNN**：把多层 RNN 叠起来，上一层的输出序列作为下一层输入。 考试写作时要补一句：常考堆叠带来更强表达能力，也增加训练难度。 易错点是：堆叠不是时间步变多，而是层数变深。
- **损失函数**：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 考试写作时要补一句：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 易错点是：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- **Padding**：把不同长度的序列补到同一长度，方便组成 batch 输入模型。 考试写作时要补一句：常考为什么需要 padding，以及它和 packing 的关系。 易错点是：Padding 补出来的位置不是真实信息，训练时要避免把它当有效时间步。

### 4. 本节自测

- 判断：隐藏状态只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：双向RNN只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：RNN只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：激活函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：梯度消失只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：堆叠RNN只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：损失函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Padding只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- h_t = 当前输入 + 过去记忆的综合表示。
- 双向 RNN = 正向隐藏状态 + 反向隐藏状态。
- 当前输出依赖当前输入和上一时刻隐藏状态。
- 仿射变换 + 激活函数 = 神经网络层的基本模式。
- 多个小于 1 的因子连乘，会让梯度趋近 0。
- 堆叠 RNN：时间方向 + 层方向都要传播。
- 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
- Padding：补齐长度；Packing：跳过无效补位。
- Dropout：训练随机失活，推理正常使用。
- Seq2Seq = Encoder + Decoder。

## 第 3 部分：第五章 循环神经网络 / 第二节 门控循环单元（GRU）

### 1. 本节先看什么

这一节先把 **Sigmoid、激活函数、GRU、隐藏状态** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| Sigmoid | 把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 | 常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 | Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。 | Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。 |
| 激活函数 | 给线性变换加入非线性，使网络能拟合非线性关系。 | 常考没有激活函数时多层线性网络仍等价于线性模型。 | 不要把激活函数说成只改变维度，它主要提供非线性。 | 仿射变换 + 激活函数 = 神经网络层的基本模式。 |
| GRU | 用更新门和重置门控制历史信息保留与遗忘，是简化版门控循环网络。 | 常和 LSTM 对比门结构和参数量。 | GRU 没有单独的细胞状态 c_t。 | GRU：更新门 + 重置门。 |
| 隐藏状态 | RNN 在每个时间步保存的当前记忆，用来把历史信息传给后续时间步。 | 常考 h_t 与 h_{t-1} 的关系，以及 output 和 h_n 的区别。 | 隐藏状态不是模型参数，而是随输入序列变化的中间表示。 | h_t = 当前输入 + 过去记忆的综合表示。 |

### 3. 像考试答案一样组织语言

- **Sigmoid**：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 考试写作时要补一句：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 易错点是：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- **激活函数**：给线性变换加入非线性，使网络能拟合非线性关系。 考试写作时要补一句：常考没有激活函数时多层线性网络仍等价于线性模型。 易错点是：不要把激活函数说成只改变维度，它主要提供非线性。
- **GRU**：用更新门和重置门控制历史信息保留与遗忘，是简化版门控循环网络。 考试写作时要补一句：常和 LSTM 对比门结构和参数量。 易错点是：GRU 没有单独的细胞状态 c_t。
- **隐藏状态**：RNN 在每个时间步保存的当前记忆，用来把历史信息传给后续时间步。 考试写作时要补一句：常考 h_t 与 h_{t-1} 的关系，以及 output 和 h_n 的区别。 易错点是：隐藏状态不是模型参数，而是随输入序列变化的中间表示。

### 4. 本节自测

- 判断：Sigmoid只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：激活函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：GRU只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：隐藏状态只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
- 仿射变换 + 激活函数 = 神经网络层的基本模式。
- GRU：更新门 + 重置门。
- h_t = 当前输入 + 过去记忆的综合表示。

## 第 4 部分：第五章 循环神经网络 / 第三节 长短时记忆网络（LSTM）

### 1. 本节先看什么

这一节先把 **Sigmoid、激活函数、隐藏状态、梯度消失** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| Sigmoid | 把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 | 常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 | Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。 | Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。 |
| 激活函数 | 给线性变换加入非线性，使网络能拟合非线性关系。 | 常考没有激活函数时多层线性网络仍等价于线性模型。 | 不要把激活函数说成只改变维度，它主要提供非线性。 | 仿射变换 + 激活函数 = 神经网络层的基本模式。 |
| 隐藏状态 | RNN 在每个时间步保存的当前记忆，用来把历史信息传给后续时间步。 | 常考 h_t 与 h_{t-1} 的关系，以及 output 和 h_n 的区别。 | 隐藏状态不是模型参数，而是随输入序列变化的中间表示。 | h_t = 当前输入 + 过去记忆的综合表示。 |
| 梯度消失 | 反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。 | 常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。 | 梯度消失不是过拟合；它是参数难以有效更新的问题。 | 多个小于 1 的因子连乘，会让梯度趋近 0。 |
| RNN | 按时间步处理序列，用隐藏状态把前面信息传到后面。 | 常考隐藏状态、序列建模、梯度消失/爆炸。 | RNN 不是一次性把所有时间步完全独立处理。 | 当前输出依赖当前输入和上一时刻隐藏状态。 |
| GRU | 用更新门和重置门控制历史信息保留与遗忘，是简化版门控循环网络。 | 常和 LSTM 对比门结构和参数量。 | GRU 没有单独的细胞状态 c_t。 | GRU：更新门 + 重置门。 |
| LSTM | 通过输入门、遗忘门、输出门和细胞状态保存长期信息。 | 常考门控作用，以及为什么能缓解长序列梯度问题。 | 不要漏掉细胞状态是 LSTM 的关键通道。 | LSTM：输入门、遗忘门、输出门、细胞状态。 |

### 3. 像考试答案一样组织语言

- **Sigmoid**：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 考试写作时要补一句：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 易错点是：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- **激活函数**：给线性变换加入非线性，使网络能拟合非线性关系。 考试写作时要补一句：常考没有激活函数时多层线性网络仍等价于线性模型。 易错点是：不要把激活函数说成只改变维度，它主要提供非线性。
- **隐藏状态**：RNN 在每个时间步保存的当前记忆，用来把历史信息传给后续时间步。 考试写作时要补一句：常考 h_t 与 h_{t-1} 的关系，以及 output 和 h_n 的区别。 易错点是：隐藏状态不是模型参数，而是随输入序列变化的中间表示。
- **梯度消失**：反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。 考试写作时要补一句：常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。 易错点是：梯度消失不是过拟合；它是参数难以有效更新的问题。
- **RNN**：按时间步处理序列，用隐藏状态把前面信息传到后面。 考试写作时要补一句：常考隐藏状态、序列建模、梯度消失/爆炸。 易错点是：RNN 不是一次性把所有时间步完全独立处理。
- **GRU**：用更新门和重置门控制历史信息保留与遗忘，是简化版门控循环网络。 考试写作时要补一句：常和 LSTM 对比门结构和参数量。 易错点是：GRU 没有单独的细胞状态 c_t。
- **LSTM**：通过输入门、遗忘门、输出门和细胞状态保存长期信息。 考试写作时要补一句：常考门控作用，以及为什么能缓解长序列梯度问题。 易错点是：不要漏掉细胞状态是 LSTM 的关键通道。

### 4. 本节自测

- 判断：Sigmoid只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：激活函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：隐藏状态只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：梯度消失只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：RNN只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：GRU只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：LSTM只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
- 仿射变换 + 激活函数 = 神经网络层的基本模式。
- h_t = 当前输入 + 过去记忆的综合表示。
- 多个小于 1 的因子连乘，会让梯度趋近 0。
- 当前输出依赖当前输入和上一时刻隐藏状态。
- GRU：更新门 + 重置门。
- LSTM：输入门、遗忘门、输出门、细胞状态。

## 第 5 部分：第五章 循环神经网络 / 第四节 变长序列处理：Padding与Packing

### 1. 本节先看什么

这一节先把 **Padding、Packing、隐藏状态、Batch** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| Padding | 把不同长度的序列补到同一长度，方便组成 batch 输入模型。 | 常考为什么需要 padding，以及它和 packing 的关系。 | Padding 补出来的位置不是真实信息，训练时要避免把它当有效时间步。 | Padding：补齐长度；Packing：跳过无效补位。 |
| Packing | 把变长序列的有效部分打包，让 RNN 少处理 padding 位置。 | 常考 pack_padded_sequence 的目的和使用前提。 | Packing 不是改变序列含义，而是提高变长序列处理效率。 | Packing 让 RNN 聚焦真实时间步。 |
| 隐藏状态 | RNN 在每个时间步保存的当前记忆，用来把历史信息传给后续时间步。 | 常考 h_t 与 h_{t-1} 的关系，以及 output 和 h_n 的区别。 | 隐藏状态不是模型参数，而是随输入序列变化的中间表示。 | h_t = 当前输入 + 过去记忆的综合表示。 |
| Batch | 一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 | 常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 | Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。 | Batch 是一批样本；Epoch 是全训练集完整训练一遍。 |
| PackedSequence | PyTorch 中表示变长序列打包后的对象，内部只保留有效时间步，交给 RNN 处理时可以跳过 padding。 | 常考 pack_padded_sequence 与 pad_packed_sequence 的前后关系。 | PackedSequence 不是普通张量；使用前后要注意 lengths、batch_first 和是否排序。 | 先 padding 组成 batch，再 packing 跳过补位，必要时再 pad 回来。 |

### 3. 像考试答案一样组织语言

- **Padding**：把不同长度的序列补到同一长度，方便组成 batch 输入模型。 考试写作时要补一句：常考为什么需要 padding，以及它和 packing 的关系。 易错点是：Padding 补出来的位置不是真实信息，训练时要避免把它当有效时间步。
- **Packing**：把变长序列的有效部分打包，让 RNN 少处理 padding 位置。 考试写作时要补一句：常考 pack_padded_sequence 的目的和使用前提。 易错点是：Packing 不是改变序列含义，而是提高变长序列处理效率。
- **隐藏状态**：RNN 在每个时间步保存的当前记忆，用来把历史信息传给后续时间步。 考试写作时要补一句：常考 h_t 与 h_{t-1} 的关系，以及 output 和 h_n 的区别。 易错点是：隐藏状态不是模型参数，而是随输入序列变化的中间表示。
- **Batch**：一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 考试写作时要补一句：常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 易错点是：Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。
- **PackedSequence**：PyTorch 中表示变长序列打包后的对象，内部只保留有效时间步，交给 RNN 处理时可以跳过 padding。 考试写作时要补一句：常考 pack_padded_sequence 与 pad_packed_sequence 的前后关系。 易错点是：PackedSequence 不是普通张量；使用前后要注意 lengths、batch_first 和是否排序。

### 4. 本节自测

- 判断：Padding只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Packing只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：隐藏状态只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Batch只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：PackedSequence只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- Padding：补齐长度；Packing：跳过无效补位。
- Packing 让 RNN 聚焦真实时间步。
- h_t = 当前输入 + 过去记忆的综合表示。
- Batch 是一批样本；Epoch 是全训练集完整训练一遍。
- 先 padding 组成 batch，再 packing 跳过补位，必要时再 pad 回来。

## 第 6 部分：第五章 循环神经网络 / 第五节 1D卷积在序列数据上的应用

### 1. 本节先看什么

这一节先把 **卷积、感受野、1D卷积、RNN** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 卷积 | 用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 | 常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 | 不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。 | 输出尺寸：(输入尺寸 + 2P - K) / S + 1。 |
| 感受野 | 某一层一个神经元能看到原始输入图像的区域大小。 | 常考层数、卷积核大小、步长如何影响感受野。 | 步长变大通常会让前一层对应感受野扩大，不是缩小。 | 层越深，感受野通常越大；多层小卷积能逐步扩大感受野。 |
| 1D卷积 | 在序列的一维时间/位置方向上滑动卷积核，提取局部 n-gram 或短期模式。 | 常和 RNN 对比：1D 卷积并行提取局部模式，RNN 逐步传递隐藏状态。 | 1D 卷积适合局部依赖，不等于天然拥有长期记忆。 | 1D 卷积：沿序列维度提取局部模式。 |
| RNN | 按时间步处理序列，用隐藏状态把前面信息传到后面。 | 常考隐藏状态、序列建模、梯度消失/爆炸。 | RNN 不是一次性把所有时间步完全独立处理。 | 当前输出依赖当前输入和上一时刻隐藏状态。 |
| GRU | 用更新门和重置门控制历史信息保留与遗忘，是简化版门控循环网络。 | 常和 LSTM 对比门结构和参数量。 | GRU 没有单独的细胞状态 c_t。 | GRU：更新门 + 重置门。 |
| Padding | 把不同长度的序列补到同一长度，方便组成 batch 输入模型。 | 常考为什么需要 padding，以及它和 packing 的关系。 | Padding 补出来的位置不是真实信息，训练时要避免把它当有效时间步。 | Padding：补齐长度；Packing：跳过无效补位。 |
| TCN | 三、TCN：时序卷积网络；为什么需要TCN？；传统1D卷积感受野有限，难以捕捉长程依赖。TCN（Temporal Convolutional Network）通过三种技术解决这个问题：；技术 / 解决的问题 / 原理；因果卷积 / 信息泄漏（偷看未来） / 只依赖历史信息，左边padding 四、总... | 会结合序列输入、隐藏状态、长距离依赖或门控结构考。 | 不要把序列模型当成普通独立样本分类；时间步之间有信息传递。 | 三、TCN：时序卷积网络；为什么需要TCN？；传统1D卷积感受野有限，难以捕捉长程依赖。TCN（Temporal Convolutional Network）通过三种技术解决这个问题：；技术 / 解决的问题 / 原理；因果卷积 / 信息泄漏... |
| 卷积核 | 卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。 | 常考卷积核大小、通道数、参数共享和输出尺寸计算。 | 卷积核参数会训练更新，不是手工固定模板。 | 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。 |
| LSTM | 通过输入门、遗忘门、输出门和细胞状态保存长期信息。 | 常考门控作用，以及为什么能缓解长序列梯度问题。 | 不要漏掉细胞状态是 LSTM 的关键通道。 | LSTM：输入门、遗忘门、输出门、细胞状态。 |
| Packing | 把变长序列的有效部分打包，让 RNN 少处理 padding 位置。 | 常考 pack_padded_sequence 的目的和使用前提。 | Packing 不是改变序列含义，而是提高变长序列处理效率。 | Packing 让 RNN 聚焦真实时间步。 |
| 梯度消失 | 反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。 | 常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。 | 梯度消失不是过拟合；它是参数难以有效更新的问题。 | 多个小于 1 的因子连乘，会让梯度趋近 0。 |
| Seq2Seq | 编码器把输入序列压成表示，解码器再生成输出序列，常用于翻译、问答等序列到序列任务。 | 常考 Encoder-Decoder 结构、Teacher Forcing 和注意力机制为何被引入。 | 普通 Seq2Seq 容易受固定长度上下文瓶颈影响。 | Seq2Seq = Encoder + Decoder。 |

### 3. 像考试答案一样组织语言

- **卷积**：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 考试写作时要补一句：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 易错点是：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- **感受野**：某一层一个神经元能看到原始输入图像的区域大小。 考试写作时要补一句：常考层数、卷积核大小、步长如何影响感受野。 易错点是：步长变大通常会让前一层对应感受野扩大，不是缩小。
- **1D卷积**：在序列的一维时间/位置方向上滑动卷积核，提取局部 n-gram 或短期模式。 考试写作时要补一句：常和 RNN 对比：1D 卷积并行提取局部模式，RNN 逐步传递隐藏状态。 易错点是：1D 卷积适合局部依赖，不等于天然拥有长期记忆。
- **RNN**：按时间步处理序列，用隐藏状态把前面信息传到后面。 考试写作时要补一句：常考隐藏状态、序列建模、梯度消失/爆炸。 易错点是：RNN 不是一次性把所有时间步完全独立处理。
- **GRU**：用更新门和重置门控制历史信息保留与遗忘，是简化版门控循环网络。 考试写作时要补一句：常和 LSTM 对比门结构和参数量。 易错点是：GRU 没有单独的细胞状态 c_t。
- **Padding**：把不同长度的序列补到同一长度，方便组成 batch 输入模型。 考试写作时要补一句：常考为什么需要 padding，以及它和 packing 的关系。 易错点是：Padding 补出来的位置不是真实信息，训练时要避免把它当有效时间步。
- **TCN**：三、TCN：时序卷积网络；为什么需要TCN？；传统1D卷积感受野有限，难以捕捉长程依赖。TCN（Temporal Convolutional Network）通过三种技术解决这个问题：；技术 | 解决的问题 | 原理；因果卷积 | 信息泄漏（偷看未来） | 只依赖历史信息，左边padding 四、总... 考试写作时要补一句：会结合序列输入、隐藏状态、长距离依赖或门控结构考。 易错点是：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- **卷积核**：卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。 考试写作时要补一句：常考卷积核大小、通道数、参数共享和输出尺寸计算。 易错点是：卷积核参数会训练更新，不是手工固定模板。

### 4. 本节自测

- 判断：卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：感受野只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：1D卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：RNN只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：GRU只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Padding只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：TCN只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：卷积核只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 输出尺寸：(输入尺寸 + 2P - K) / S + 1。
- 层越深，感受野通常越大；多层小卷积能逐步扩大感受野。
- 1D 卷积：沿序列维度提取局部模式。
- 当前输出依赖当前输入和上一时刻隐藏状态。
- GRU：更新门 + 重置门。
- Padding：补齐长度；Packing：跳过无效补位。
- 三、TCN：时序卷积网络；为什么需要TCN？；传统1D卷积感受野有限，难以捕捉长程依赖。TCN（Temporal Convolutional Network）通过三种技术解决这个问题：；技术 | 解决的问题 | 原理；因果卷积 | 信息泄漏...
- 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。
- LSTM：输入门、遗忘门、输出门、细胞状态。
- Packing 让 RNN 聚焦真实时间步。

## 第 7 部分：第五章 循环神经网络 / 第六节 PyTorch实现综合案例

### 1. 本节先看什么

这一节先把 **Dropout、学习率、梯度爆炸、梯度裁剪** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| Dropout | 训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 | 常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 | Dropout 不是提高模型容量，而是正则化。 | Dropout：训练随机失活，推理正常使用。 |
| 学习率 | 控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 | 常考学习率过大/过小的训练现象，以及学习率调度的目的。 | 学习率不是越大越好；也不是模型学到的参数。 | 学习率决定更新步长。 |
| 梯度爆炸 | 梯度在反向传播中变得过大，使参数更新剧烈，训练损失可能震荡或变成 NaN。 | 常考梯度裁剪为什么能缓解 RNN 训练不稳定。 | Dropout 主要缓解过拟合，不是解决梯度爆炸的标准答案。 | 梯度裁剪限制梯度范数，防止更新步子过大。 |
| 梯度裁剪 | 当梯度范数超过阈值时按比例缩小，常用于稳定 RNN 等容易梯度爆炸的训练。 | 常考它处理的是梯度爆炸，而不是梯度消失。 | 裁剪不是把梯度直接清零，而是限制其大小。 | Gradient Clipping：限制梯度大小，稳定训练。 |
| Adam | 结合动量和自适应学习率思想，为不同参数调整更新幅度。 | 常和 SGD、Momentum、RMSProp 对比优化器特点。 | Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。 | Adam = 一阶动量 + 二阶矩估计。 |
| Batch | 一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 | 常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 | Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。 | Batch 是一批样本；Epoch 是全训练集完整训练一遍。 |
| Sigmoid | 把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 | 常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 | Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。 | Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。 |
| 隐藏状态 | RNN 在每个时间步保存的当前记忆，用来把历史信息传给后续时间步。 | 常考 h_t 与 h_{t-1} 的关系，以及 output 和 h_n 的区别。 | 隐藏状态不是模型参数，而是随输入序列变化的中间表示。 | h_t = 当前输入 + 过去记忆的综合表示。 |
| LSTM | 通过输入门、遗忘门、输出门和细胞状态保存长期信息。 | 常考门控作用，以及为什么能缓解长序列梯度问题。 | 不要漏掉细胞状态是 LSTM 的关键通道。 | LSTM：输入门、遗忘门、输出门、细胞状态。 |
| Padding | 把不同长度的序列补到同一长度，方便组成 batch 输入模型。 | 常考为什么需要 padding，以及它和 packing 的关系。 | Padding 补出来的位置不是真实信息，训练时要避免把它当有效时间步。 | Padding：补齐长度；Packing：跳过无效补位。 |
| PackedSequence | PyTorch 中表示变长序列打包后的对象，内部只保留有效时间步，交给 RNN 处理时可以跳过 padding。 | 常考 pack_padded_sequence 与 pad_packed_sequence 的前后关系。 | PackedSequence 不是普通张量；使用前后要注意 lengths、batch_first 和是否排序。 | 先 padding 组成 batch，再 packing 跳过补位，必要时再 pad 回来。 |
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |

### 3. 像考试答案一样组织语言

- **Dropout**：训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 考试写作时要补一句：常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 易错点是：Dropout 不是提高模型容量，而是正则化。
- **学习率**：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 考试写作时要补一句：常考学习率过大/过小的训练现象，以及学习率调度的目的。 易错点是：学习率不是越大越好；也不是模型学到的参数。
- **梯度爆炸**：梯度在反向传播中变得过大，使参数更新剧烈，训练损失可能震荡或变成 NaN。 考试写作时要补一句：常考梯度裁剪为什么能缓解 RNN 训练不稳定。 易错点是：Dropout 主要缓解过拟合，不是解决梯度爆炸的标准答案。
- **梯度裁剪**：当梯度范数超过阈值时按比例缩小，常用于稳定 RNN 等容易梯度爆炸的训练。 考试写作时要补一句：常考它处理的是梯度爆炸，而不是梯度消失。 易错点是：裁剪不是把梯度直接清零，而是限制其大小。
- **Adam**：结合动量和自适应学习率思想，为不同参数调整更新幅度。 考试写作时要补一句：常和 SGD、Momentum、RMSProp 对比优化器特点。 易错点是：Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。
- **Batch**：一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 考试写作时要补一句：常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 易错点是：Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。
- **Sigmoid**：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 考试写作时要补一句：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 易错点是：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- **隐藏状态**：RNN 在每个时间步保存的当前记忆，用来把历史信息传给后续时间步。 考试写作时要补一句：常考 h_t 与 h_{t-1} 的关系，以及 output 和 h_n 的区别。 易错点是：隐藏状态不是模型参数，而是随输入序列变化的中间表示。

### 4. 本节自测

- 判断：Dropout只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：学习率只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：梯度爆炸只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：梯度裁剪只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Adam只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Batch只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Sigmoid只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：隐藏状态只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- Dropout：训练随机失活，推理正常使用。
- 学习率决定更新步长。
- 梯度裁剪限制梯度范数，防止更新步子过大。
- Gradient Clipping：限制梯度大小，稳定训练。
- Adam = 一阶动量 + 二阶矩估计。
- Batch 是一批样本；Epoch 是全训练集完整训练一遍。
- Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
- h_t = 当前输入 + 过去记忆的综合表示。
- LSTM：输入门、遗忘门、输出门、细胞状态。
- Padding：补齐长度；Packing：跳过无效补位。

## 四、考前总复盘

考前不要平均用力。优先检查下面这些问题：

1. 每个核心概念能不能用一句话说明“它是什么”。
2. 能不能说出它在模型结构、训练流程或数据处理中的位置。
3. 能不能说出一个最常见的错误说法。
4. 遇到选择题时，能不能判断选项是在混淆概念、夸大作用，还是写反了训练/推理阶段。

## 五、打印建议

<style>@media print { @page { margin: 8mm; } body { font-size: 11pt; line-height: 1.35; } h1, h2, h3 { page-break-after: avoid; } table { font-size: 9pt; border-collapse: collapse; } th, td { padding: 3px 5px; border: 1px solid #ddd; vertical-align: top; } }</style>
