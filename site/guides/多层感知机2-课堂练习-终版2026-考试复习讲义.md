# 多层感知机2-课堂练习-终版2026：按课件顺序重写的考试复习讲义

这份讲义会尽量按你读课件时真正需要的顺序来讲。

**目标不是把课件拆成表格，而是像复习书一样，一点一点说明：为什么讲这个、它解决什么问题、考试怎么抓。**

---

## 先说整份课件到底在解决什么问题

这一份课件要解决的是：**最基础的神经网络到底怎么从输入走到预测，再从错误走回参数更新。**

多层感知机不是只背几层 Linear，它的重点是把“线性变换、激活函数、损失函数、反向传播、优化器”连成一条训练流程。

读的时候先不要被代码吓到。你只需要先抓住：**前向传播得到预测，损失函数衡量错多少，反向传播算梯度，优化器改参数。**

---

## 建议阅读顺序

下面不是知识点清单，而是你复习时可以照着走的路线：

1. **多层感知机 - 课堂练习**：先读 Softmax、Sigmoid、多层感知机。

---

## 开始按课件一点一点讲

## 第 1 部分：多层感知机 - 课堂练习

### 这一节先不要急着背

这一节可以按 **Softmax -> Sigmoid -> 多层感知机** 的顺序读。第一个概念通常是入口，后面的概念要么是在补结构，要么是在解释训练时会遇到的问题。

### 1. 先讲 Softmax

先用最普通的话说，**Softmax** 就是：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。

它在这份课件里不是孤立出现的。你要把它理解成这一节用来解决问题的一块拼图：常考多分类输出、注意力权重为什么能加权求和。

如果按课件顺序串起来，可以这样看：**前面的概念 -> Softmax -> Sigmoid**。
也就是说，你不是在背一个词，而是在看知识点之间怎样往下推进。

考试写到这里，最稳的一句话是：**Softmax 输出非负且总和为 1。**

这里最容易错的是：Softmax 是对一组数整体归一化，不是逐个独立压缩。

### 2. 先讲 Sigmoid

先用最普通的话说，**Sigmoid** 就是：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。

它在这份课件里不是孤立出现的。你要把它理解成这一节用来解决问题的一块拼图：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。

如果按课件顺序串起来，可以这样看：**Softmax -> Sigmoid -> 多层感知机**。
也就是说，你不是在背一个词，而是在看知识点之间怎样往下推进。

考试写到这里，最稳的一句话是：**Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。**

这里最容易错的是：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。

### 3. 先讲 多层感知机

先用最普通的话说，**多层感知机** 就是：多层感知机是由输入层、隐藏层和输出层组成的前馈神经网络。

它在这份课件里不是孤立出现的。你要把它理解成这一节用来解决问题的一块拼图：常考隐藏层作用、激活函数必要性、训练流程。

如果按课件顺序串起来，可以这样看：**Sigmoid -> 多层感知机 -> 后面的概念**。
也就是说，你不是在背一个词，而是在看知识点之间怎样往下推进。

考试写到这里，最稳的一句话是：**多层感知机靠非线性激活增强表达能力。**

这里最容易错的是：多层感知机不是循环网络，也不保留时间状态。

### 这一节最后怎么记

- **Softmax**：Softmax 输出非负且总和为 1。
- **Sigmoid**：Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
- **多层感知机**：多层感知机靠非线性激活增强表达能力。

### 本节检查题

1. **选择题：关于 Softmax，下列说法错误的是：**
   A. 把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。
   B. Softmax 输出非负且总和为 1。
   C. 常考多分类输出、注意力权重为什么能加权求和。
   D. Softmax 输出不需要总和为 1。

   **答案：D。** A、B、C 都能和课件里的 Softmax 对上；D 是把概念说反或放错位置。

2. **填空题：** Sigmoid 的复习重点不是只写名称，而是写清它的作用：____。

   **参考答案：** 把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。

3. **简答题：为什么考试里不能只背 多层感知机 的定义？**

   **参考答案：** 因为 多层感知机 在课件中有明确的位置和作用：多层感知机是由输入层、隐藏层和输出层组成的前馈神经网络。 答题时还要补出它通常怎么考：常考隐藏层作用、激活函数必要性、训练流程。 最后要避开这个误区：多层感知机不是循环网络，也不保留时间状态。

---

## 考前总复盘

考前不要平均用力。你可以按这四个问题检查自己：

1. 这个概念为什么会在课件这里出现？
2. 它解决的是输入、模型结构、训练优化、输出损失，还是图/序列关系的问题？
3. 如果让我写简答，我能不能不用空话，写出一句准确解释？
4. 如果让我做选择题，我能不能看出选项是不是把概念放错位置、说反作用、混淆训练和推理？

## 打印建议

<style>@media print { @page { margin: 8mm; } body { font-size: 11pt; line-height: 1.35; } h1, h2, h3 { page-break-after: avoid; } }</style>
