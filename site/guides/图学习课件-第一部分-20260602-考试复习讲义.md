# 图学习课件_第一部分_20260602：期末考试复习讲义

> 使用方式：先通读“学习路线”，再按章节背“必记句子”，最后用每章自测题检查。这里不是课件原文搬运，而是按考试复习顺序重写。

## 一、这份课件先解决什么问题

这一类课件处理图结构数据。复习时先理解节点、边、邻接矩阵和任务类型，再进入图卷积、消息传递和图神经网络。

考试常考图数据结构如何表示、节点/边/图级任务怎么区分，以及邻居聚合为什么能利用结构信息。

## 二、学习路线

1. **什么是图？**：先抓 节点、Edge。
2. **遂图数据的基本结构**：先抓 Vertex、Edge。
3. **图数据的基本结构**：先抓 Vertex、Edge。
4. **少图的分类**：先抓 节点。
5. **图的度和邻居**：先抓 节点。
6. **邻接矩阵**：先抓 节点、邻接矩阵。
7. **实际的例子**：先抓 节点。
8. **图数据结构的学习优势**：先抓 图学习。
9. **图能做什么？**：先抓 节点。
10. **）节点级别任务——金融诈骗检测**：先抓 节点、节点分类。
11. **节点级别任务—目标检测**：先抓 节点。
12. **节点级别任务——目标检测**：先抓 节点。
13. **图学习应用**：先抓 图学习。
14. **图学习算法分类**：先抓 图学习、GAT、嵌入。
15. **图的基本概念**：先抓 图学习、节点、嵌入。
16. **图学习课件第一部分，第 30 页**：先抓 图学习、节点、邻接矩阵、度矩阵、拉普拉斯矩阵。
17. **图游走类算法**：先抓 节点。
18. **看 Word2vec: Skip Gram**：先抓 Softmax。
19. **Word2vec: Negative Sampling**：先抓 Sigmoid、Softmax。
20. **图游走类模型——DeepWalk**：先抓 节点、嵌入。
21. **）图游走类模型-**：先抓 节点。
22. **r node2vec： 对 DeepWalk 的改进**：先抓 随机游走。
23. **s nodezvec: bias random walk**：先抓 归一化、节点、随机游走。
24. **- nodezvec: bias random walk**：先抓 节点。
25. **w nodezvec: bias random walk**：先抓 随机游走。
26. **图学习的目的**：先抓 图学习、节点、节点分类、图分类。
27. **5 图神经网络（GNN）**：先抓 图神经网络、节点。
28. **图像上的卷积**：先抓 卷积。
29. **5 图卷积网络（GCN）**：先抓 节点、图卷积、GCN、消息传递、节点分类。
30. **图卷积网络（GCN）**：先抓 图卷积、GCN、节点、邻接矩阵、度矩阵。
31. **） 图卷积网络（GCN）-消息传递**：先抓 节点、图卷积、GCN、消息传递。
32. **5 图卷积网络（GCN）-消息传递**：先抓 节点、图卷积、GCN、消息传递、聚合。
33. **图学习课件第一部分，第 66 页**：先抓 图学习、节点、邻接矩阵、度矩阵、拉普拉斯矩阵。

## 三、章节详解

## 第 1 部分：什么是图？

### 1. 本节先看什么

这一节先把 **节点、Edge** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |
| Edge | 图中的边，表示两个节点之间的关系，可以有方向和权重。 | 常考 Edge 与邻接矩阵元素的对应关系。 | Edge 不是节点特征本身，而是节点之间的连接。 | Edge 表示关系，A_ij 常记录边或边权。 |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。
- **Edge**：图中的边，表示两个节点之间的关系，可以有方向和权重。 考试写作时要补一句：常考 Edge 与邻接矩阵元素的对应关系。 易错点是：Edge 不是节点特征本身，而是节点之间的连接。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Edge只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。
- Edge 表示关系，A_ij 常记录边或边权。

## 第 2 部分：遂图数据的基本结构

### 1. 本节先看什么

这一节先把 **Vertex、Edge** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| Vertex | 图中的节点，也就是图结构里的对象或实体。 | 常考 Vertex/Node 与 Edge 的区别。 | Vertex 是节点，不是边。 | Vertex/Node 表示对象；Edge 表示关系。 |
| Edge | 图中的边，表示两个节点之间的关系，可以有方向和权重。 | 常考 Edge 与邻接矩阵元素的对应关系。 | Edge 不是节点特征本身，而是节点之间的连接。 | Edge 表示关系，A_ij 常记录边或边权。 |

### 3. 像考试答案一样组织语言

- **Vertex**：图中的节点，也就是图结构里的对象或实体。 考试写作时要补一句：常考 Vertex/Node 与 Edge 的区别。 易错点是：Vertex 是节点，不是边。
- **Edge**：图中的边，表示两个节点之间的关系，可以有方向和权重。 考试写作时要补一句：常考 Edge 与邻接矩阵元素的对应关系。 易错点是：Edge 不是节点特征本身，而是节点之间的连接。

### 4. 本节自测

- 判断：Vertex只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Edge只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- Vertex/Node 表示对象；Edge 表示关系。
- Edge 表示关系，A_ij 常记录边或边权。

## 第 3 部分：图数据的基本结构

### 1. 本节先看什么

这一节先把 **Vertex、Edge** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| Vertex | 图中的节点，也就是图结构里的对象或实体。 | 常考 Vertex/Node 与 Edge 的区别。 | Vertex 是节点，不是边。 | Vertex/Node 表示对象；Edge 表示关系。 |
| Edge | 图中的边，表示两个节点之间的关系，可以有方向和权重。 | 常考 Edge 与邻接矩阵元素的对应关系。 | Edge 不是节点特征本身，而是节点之间的连接。 | Edge 表示关系，A_ij 常记录边或边权。 |

### 3. 像考试答案一样组织语言

- **Vertex**：图中的节点，也就是图结构里的对象或实体。 考试写作时要补一句：常考 Vertex/Node 与 Edge 的区别。 易错点是：Vertex 是节点，不是边。
- **Edge**：图中的边，表示两个节点之间的关系，可以有方向和权重。 考试写作时要补一句：常考 Edge 与邻接矩阵元素的对应关系。 易错点是：Edge 不是节点特征本身，而是节点之间的连接。

### 4. 本节自测

- 判断：Vertex只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Edge只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- Vertex/Node 表示对象；Edge 表示关系。
- Edge 表示关系，A_ij 常记录边或边权。

## 第 4 部分：少图的分类

### 1. 本节先看什么

这一节先把 **节点** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。

## 第 5 部分：图的度和邻居

### 1. 本节先看什么

这一节先把 **节点** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。

## 第 6 部分：邻接矩阵

### 1. 本节先看什么

这一节先把 **节点、邻接矩阵** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |
| 邻接矩阵 | 用矩阵记录节点之间是否相连或连接权重。 | 常考根据图画矩阵，或根据矩阵判断邻居。 | 无向图邻接矩阵通常对称；有向图不一定对称。 | A_ij 表示 i 到 j 是否有边或边权。 |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。
- **邻接矩阵**：用矩阵记录节点之间是否相连或连接权重。 考试写作时要补一句：常考根据图画矩阵，或根据矩阵判断邻居。 易错点是：无向图邻接矩阵通常对称；有向图不一定对称。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：邻接矩阵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。
- A_ij 表示 i 到 j 是否有边或边权。

## 第 7 部分：实际的例子

### 1. 本节先看什么

这一节先把 **节点** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。

## 第 8 部分：图数据结构的学习优势

### 1. 本节先看什么

这一节先把 **图学习** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 图学习 | 研究如何在图结构数据上学习表示和完成预测，核心是同时利用节点特征与关系结构。 | 常考图数据适用场景、节点/边/图级任务。 | 不要只看单个样本特征，图学习关键是关系。 | 图学习 = 特征信息 + 结构信息。 |

### 3. 像考试答案一样组织语言

- **图学习**：研究如何在图结构数据上学习表示和完成预测，核心是同时利用节点特征与关系结构。 考试写作时要补一句：常考图数据适用场景、节点/边/图级任务。 易错点是：不要只看单个样本特征，图学习关键是关系。

### 4. 本节自测

- 判断：图学习只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 图学习 = 特征信息 + 结构信息。

## 第 9 部分：图能做什么？

### 1. 本节先看什么

这一节先把 **节点** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。

## 第 10 部分：）节点级别任务——金融诈骗检测

### 1. 本节先看什么

这一节先把 **节点、节点分类** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |
| 节点分类 | ）节点级别任务——金融诈骗检测；A Semi-supervised Graph Attentive Network for Financial Fraud Detection | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | ）节点级别任务——金融诈骗检测；A Semi-supervised Graph Attentive Network for Financial Fraud Detection |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。
- **节点分类**：）节点级别任务——金融诈骗检测；A Semi-supervised Graph Attentive Network for Financial Fraud Detection 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：节点分类只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。
- ）节点级别任务——金融诈骗检测；A Semi-supervised Graph Attentive Network for Financial Fraud Detection

## 第 11 部分：节点级别任务—目标检测

### 1. 本节先看什么

这一节先把 **节点** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。

## 第 12 部分：节点级别任务——目标检测

### 1. 本节先看什么

这一节先把 **节点** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。

## 第 13 部分：图学习应用

### 1. 本节先看什么

这一节先把 **图学习** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 图学习 | 研究如何在图结构数据上学习表示和完成预测，核心是同时利用节点特征与关系结构。 | 常考图数据适用场景、节点/边/图级任务。 | 不要只看单个样本特征，图学习关键是关系。 | 图学习 = 特征信息 + 结构信息。 |

### 3. 像考试答案一样组织语言

- **图学习**：研究如何在图结构数据上学习表示和完成预测，核心是同时利用节点特征与关系结构。 考试写作时要补一句：常考图数据适用场景、节点/边/图级任务。 易错点是：不要只看单个样本特征，图学习关键是关系。

### 4. 本节自测

- 判断：图学习只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 图学习 = 特征信息 + 结构信息。

## 第 14 部分：图学习算法分类

### 1. 本节先看什么

这一节先把 **图学习、GAT、嵌入** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 图学习 | 研究如何在图结构数据上学习表示和完成预测，核心是同时利用节点特征与关系结构。 | 常考图数据适用场景、节点/边/图级任务。 | 不要只看单个样本特征，图学习关键是关系。 | 图学习 = 特征信息 + 结构信息。 |
| GAT | 图注意力网络，给不同邻居分配不同注意力权重再聚合。 | 常和 GCN 对比：GCN 多按固定归一化聚合，GAT 学习邻居重要性。 | GAT 的重点是邻居权重可学习，不是所有邻居同等重要。 | GAT：用注意力学习邻居权重。 |
| 嵌入 | DeepiVal；NodeZVe；UNE；MetapathZVec；MetapathZVwc, Metapath2Vwce, Mul | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | DeepiVal；NodeZVe；UNE；MetapathZVec；MetapathZVwc, Metapath2Vwce, Mul |

### 3. 像考试答案一样组织语言

- **图学习**：研究如何在图结构数据上学习表示和完成预测，核心是同时利用节点特征与关系结构。 考试写作时要补一句：常考图数据适用场景、节点/边/图级任务。 易错点是：不要只看单个样本特征，图学习关键是关系。
- **GAT**：图注意力网络，给不同邻居分配不同注意力权重再聚合。 考试写作时要补一句：常和 GCN 对比：GCN 多按固定归一化聚合，GAT 学习邻居重要性。 易错点是：GAT 的重点是邻居权重可学习，不是所有邻居同等重要。
- **嵌入**：DeepiVal；NodeZVe；UNE；MetapathZVec；MetapathZVwc, Metapath2Vwce, Mul 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。

### 4. 本节自测

- 判断：图学习只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：GAT只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：嵌入只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 图学习 = 特征信息 + 结构信息。
- GAT：用注意力学习邻居权重。
- DeepiVal；NodeZVe；UNE；MetapathZVec；MetapathZVwc, Metapath2Vwce, Mul

## 第 15 部分：图的基本概念

### 1. 本节先看什么

这一节先把 **图学习、节点、嵌入** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 图学习 | 研究如何在图结构数据上学习表示和完成预测，核心是同时利用节点特征与关系结构。 | 常考图数据适用场景、节点/边/图级任务。 | 不要只看单个样本特征，图学习关键是关系。 | 图学习 = 特征信息 + 结构信息。 |
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |
| 嵌入 | 两个基本元素：点、边；團是一种統一描述复杂事物约透害；图学习的基本概念；司以方便地处理朴；为什么选擇團学习 | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | 两个基本元素：点、边；團是一种統一描述复杂事物约透害；图学习的基本概念；司以方便地处理朴；为什么选擇團学习 |

### 3. 像考试答案一样组织语言

- **图学习**：研究如何在图结构数据上学习表示和完成预测，核心是同时利用节点特征与关系结构。 考试写作时要补一句：常考图数据适用场景、节点/边/图级任务。 易错点是：不要只看单个样本特征，图学习关键是关系。
- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。
- **嵌入**：两个基本元素：点、边；團是一种統一描述复杂事物约透害；图学习的基本概念；司以方便地处理朴；为什么选擇團学习 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。

### 4. 本节自测

- 判断：图学习只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：嵌入只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 图学习 = 特征信息 + 结构信息。
- 节点表示对象，边表示对象之间的关系。
- 两个基本元素：点、边；團是一种統一描述复杂事物约透害；图学习的基本概念；司以方便地处理朴；为什么选擇團学习

## 第 16 部分：图学习课件第一部分，第 30 页

### 1. 本节先看什么

这一节先把 **图学习、节点、邻接矩阵、度矩阵** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 图学习 | 研究如何在图结构数据上学习表示和完成预测，核心是同时利用节点特征与关系结构。 | 常考图数据适用场景、节点/边/图级任务。 | 不要只看单个样本特征，图学习关键是关系。 | 图学习 = 特征信息 + 结构信息。 |
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |
| 邻接矩阵 | 用矩阵记录节点之间是否相连或连接权重。 | 常考根据图画矩阵，或根据矩阵判断邻居。 | 无向图邻接矩阵通常对称；有向图不一定对称。 | A_ij 表示 i 到 j 是否有边或边权。 |
| 度矩阵 | 对角矩阵，对角线记录每个节点的度。 | 常和拉普拉斯矩阵 L=D-A 一起考。 | 度矩阵不是邻接矩阵，它只放在对角线上。 | D_ii = 节点 i 的度。 |
| 拉普拉斯矩阵 | 用度矩阵和邻接矩阵构造，反映图的连接结构。 | 常考 L=D-A 以及它在谱图方法中的作用。 | 不要把 L 和 A 混用。 | 图拉普拉斯：L = D - A。 |

### 3. 像考试答案一样组织语言

- **图学习**：研究如何在图结构数据上学习表示和完成预测，核心是同时利用节点特征与关系结构。 考试写作时要补一句：常考图数据适用场景、节点/边/图级任务。 易错点是：不要只看单个样本特征，图学习关键是关系。
- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。
- **邻接矩阵**：用矩阵记录节点之间是否相连或连接权重。 考试写作时要补一句：常考根据图画矩阵，或根据矩阵判断邻居。 易错点是：无向图邻接矩阵通常对称；有向图不一定对称。
- **度矩阵**：对角矩阵，对角线记录每个节点的度。 考试写作时要补一句：常和拉普拉斯矩阵 L=D-A 一起考。 易错点是：度矩阵不是邻接矩阵，它只放在对角线上。
- **拉普拉斯矩阵**：用度矩阵和邻接矩阵构造，反映图的连接结构。 考试写作时要补一句：常考 L=D-A 以及它在谱图方法中的作用。 易错点是：不要把 L 和 A 混用。

### 4. 本节自测

- 判断：图学习只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：邻接矩阵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：度矩阵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：拉普拉斯矩阵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 图学习 = 特征信息 + 结构信息。
- 节点表示对象，边表示对象之间的关系。
- A_ij 表示 i 到 j 是否有边或边权。
- D_ii = 节点 i 的度。
- 图拉普拉斯：L = D - A。

## 第 17 部分：图游走类算法

### 1. 本节先看什么

这一节先把 **节点** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。

## 第 18 部分：看 Word2vec: Skip Gram

### 1. 本节先看什么

这一节先把 **Softmax** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| Softmax | 把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 | 常考多分类输出、注意力权重为什么能加权求和。 | Softmax 是对一组数整体归一化，不是逐个独立压缩。 | Softmax 输出非负且总和为 1。 |

### 3. 像考试答案一样组织语言

- **Softmax**：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 考试写作时要补一句：常考多分类输出、注意力权重为什么能加权求和。 易错点是：Softmax 是对一组数整体归一化，不是逐个独立压缩。

### 4. 本节自测

- 判断：Softmax只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- Softmax 输出非负且总和为 1。

## 第 19 部分：Word2vec: Negative Sampling

### 1. 本节先看什么

这一节先把 **Sigmoid、Softmax** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| Sigmoid | 把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 | 常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 | Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。 | Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。 |
| Softmax | 把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 | 常考多分类输出、注意力权重为什么能加权求和。 | Softmax 是对一组数整体归一化，不是逐个独立压缩。 | Softmax 输出非负且总和为 1。 |

### 3. 像考试答案一样组织语言

- **Sigmoid**：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 考试写作时要补一句：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 易错点是：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- **Softmax**：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 考试写作时要补一句：常考多分类输出、注意力权重为什么能加权求和。 易错点是：Softmax 是对一组数整体归一化，不是逐个独立压缩。

### 4. 本节自测

- 判断：Sigmoid只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Softmax只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
- Softmax 输出非负且总和为 1。

## 第 20 部分：图游走类模型——DeepWalk

### 1. 本节先看什么

这一节先把 **节点、嵌入** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |
| 嵌入 | 图游走类模型——DeepWalk；将 NLP 领域的思想运用到图（网络）嵌入领域。；How？；DeepWalk；节点序列 ->句子 | 会结合序列输入、隐藏状态、长距离依赖或门控结构考。 | 不要把序列模型当成普通独立样本分类；时间步之间有信息传递。 | 图游走类模型——DeepWalk；将 NLP 领域的思想运用到图（网络）嵌入领域。；How？；DeepWalk；节点序列 ->句子 |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。
- **嵌入**：图游走类模型——DeepWalk；将 NLP 领域的思想运用到图（网络）嵌入领域。；How？；DeepWalk；节点序列 ->句子 考试写作时要补一句：会结合序列输入、隐藏状态、长距离依赖或门控结构考。 易错点是：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：嵌入只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。
- 图游走类模型——DeepWalk；将 NLP 领域的思想运用到图（网络）嵌入领域。；How？；DeepWalk；节点序列 ->句子

## 第 21 部分：）图游走类模型-

### 1. 本节先看什么

这一节先把 **节点** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。

## 第 22 部分：r node2vec： 对 DeepWalk 的改进

### 1. 本节先看什么

这一节先把 **随机游走** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 随机游走 | r node2vec： 对 DeepWalk 的改进；Max length = 3；DeepWalk；d ea；• 简单直接：DFS | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | r node2vec： 对 DeepWalk 的改进；Max length = 3；DeepWalk；d ea；• 简单直接：DFS |

### 3. 像考试答案一样组织语言

- **随机游走**：r node2vec： 对 DeepWalk 的改进；Max length = 3；DeepWalk；d ea；• 简单直接：DFS 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。

### 4. 本节自测

- 判断：随机游走只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- r node2vec： 对 DeepWalk 的改进；Max length = 3；DeepWalk；d ea；• 简单直接：DFS

## 第 23 部分：s nodezvec: bias random walk

### 1. 本节先看什么

这一节先把 **归一化、节点、随机游走** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 归一化 | 把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 | 常考图像预处理为什么要缩放像素值。 | 归一化不等于 BatchNorm，前者多是输入预处理。 | 图像常先把像素值缩放到 0-1。 |
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |
| 随机游走 | s nodezvec: bias random walk；归一化后的转移概率分布；Tux；一般的随机游走公式 P（Ci = xlci-1 =w）=；i （U,x）EE | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | s nodezvec: bias random walk；归一化后的转移概率分布；Tux；一般的随机游走公式 P（Ci = xlci-1 =w）=；i （U,x）EE |

### 3. 像考试答案一样组织语言

- **归一化**：把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 考试写作时要补一句：常考图像预处理为什么要缩放像素值。 易错点是：归一化不等于 BatchNorm，前者多是输入预处理。
- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。
- **随机游走**：s nodezvec: bias random walk；归一化后的转移概率分布；Tux；一般的随机游走公式 P（Ci = xlci-1 =w）=；i （U,x）EE 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。

### 4. 本节自测

- 判断：归一化只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：随机游走只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 图像常先把像素值缩放到 0-1。
- 节点表示对象，边表示对象之间的关系。
- s nodezvec: bias random walk；归一化后的转移概率分布；Tux；一般的随机游走公式 P（Ci = xlci-1 =w）=；i （U,x）EE

## 第 24 部分：- nodezvec: bias random walk

### 1. 本节先看什么

这一节先把 **节点** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。

## 第 25 部分：w nodezvec: bias random walk

### 1. 本节先看什么

这一节先把 **随机游走** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 随机游走 | w nodezvec: bias random walk；后irak -0；•p：控制随机游走以多大的概率“回头”；Tux = Cpa（t,x）• Wox Cpg（t,x）=；1 | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | w nodezvec: bias random walk；后irak -0；•p：控制随机游走以多大的概率“回头”；Tux = Cpa（t,x）• Wox Cpg（t,x）=；1 |

### 3. 像考试答案一样组织语言

- **随机游走**：w nodezvec: bias random walk；后irak -0；•p：控制随机游走以多大的概率“回头”；Tux = Cpa（t,x）• Wox Cpg（t,x）=；1 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。

### 4. 本节自测

- 判断：随机游走只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- w nodezvec: bias random walk；后irak -0；•p：控制随机游走以多大的概率“回头”；Tux = Cpa（t,x）• Wox Cpg（t,x）=；1

## 第 26 部分：图学习的目的

### 1. 本节先看什么

这一节先把 **图学习、节点、节点分类、图分类** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 图学习 | 研究如何在图结构数据上学习表示和完成预测，核心是同时利用节点特征与关系结构。 | 常考图数据适用场景、节点/边/图级任务。 | 不要只看单个样本特征，图学习关键是关系。 | 图学习 = 特征信息 + 结构信息。 |
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |
| 节点分类 | Graph；Pooling；关系预测、节点分类 | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | Graph；Pooling；关系预测、节点分类 |
| 图分类 | Graph；Pooling；关系预测、节点分类 | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | Graph；Pooling；关系预测、节点分类 |

### 3. 像考试答案一样组织语言

- **图学习**：研究如何在图结构数据上学习表示和完成预测，核心是同时利用节点特征与关系结构。 考试写作时要补一句：常考图数据适用场景、节点/边/图级任务。 易错点是：不要只看单个样本特征，图学习关键是关系。
- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。
- **节点分类**：Graph；Pooling；关系预测、节点分类 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。
- **图分类**：Graph；Pooling；关系预测、节点分类 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。

### 4. 本节自测

- 判断：图学习只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：节点分类只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：图分类只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 图学习 = 特征信息 + 结构信息。
- 节点表示对象，边表示对象之间的关系。
- Graph；Pooling；关系预测、节点分类
- Graph；Pooling；关系预测、节点分类

## 第 27 部分：5 图神经网络（GNN）

### 1. 本节先看什么

这一节先把 **图神经网络、节点** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 图神经网络 | 5 图神经网络（GNN）；MLP层；Layer N；Layer N+1；graph in | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | 5 图神经网络（GNN）；MLP层；Layer N；Layer N+1；graph in |
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |

### 3. 像考试答案一样组织语言

- **图神经网络**：5 图神经网络（GNN）；MLP层；Layer N；Layer N+1；graph in 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。
- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。

### 4. 本节自测

- 判断：图神经网络只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 5 图神经网络（GNN）；MLP层；Layer N；Layer N+1；graph in
- 节点表示对象，边表示对象之间的关系。

## 第 28 部分：图像上的卷积

### 1. 本节先看什么

这一节先把 **卷积** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 卷积 | 用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 | 常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 | 不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。 | 输出尺寸：(输入尺寸 + 2P - K) / S + 1。 |

### 3. 像考试答案一样组织语言

- **卷积**：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 考试写作时要补一句：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 易错点是：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。

### 4. 本节自测

- 判断：卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 输出尺寸：(输入尺寸 + 2P - K) / S + 1。

## 第 29 部分：5 图卷积网络（GCN）

### 1. 本节先看什么

这一节先把 **节点、图卷积、GCN、消息传递** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |
| 图卷积 | 5 图卷积网络（GCN）；如何从圈像卷积，类比到圈结构卷积；怎么用消息传递方式实现图卷积网络；怎么用多层图网络完成节点分类任务；SEMI-SUPERVISED CLASSIFICATION WITH GRAPH CONVOLUTIONAL NETWORKS （ICLR 2017） 5 图卷积网络（G... | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | 5 图卷积网络（GCN）；如何从圈像卷积，类比到圈结构卷积；怎么用消息传递方式实现图卷积网络；怎么用多层图网络完成节点分类任务；SEMI-SUPERVISED CLASSIFICATION WITH GRAPH CONVOLUTIONAL... |
| GCN | 图卷积网络，通过邻接关系聚合邻居节点特征，再更新节点表示。 | 常考 GCN 为什么能利用图结构、聚合邻居信息的基本流程。 | GCN 不只看节点自身特征，还看邻居。 | GCN：邻居聚合 + 线性变换 + 非线性。 |
| 消息传递 | 节点从邻居接收信息、聚合信息、更新自身表示，是很多图神经网络的统一视角。 | 常考消息、聚合、更新三个步骤。 | 不要只写公式，考试要说明信息沿边传播。 | 消息传递：邻居发消息，节点聚合并更新。 |
| 节点分类 | 5 图卷积网络（GCN）；如何从圈像卷积，类比到圈结构卷积；怎么用消息传递方式实现图卷积网络；怎么用多层图网络完成节点分类任务；SEMI-SUPERVISED CLASSIFICATION WITH GRAPH CONVOLUTIONAL NETWORKS （ICLR 2017） | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | 5 图卷积网络（GCN）；如何从圈像卷积，类比到圈结构卷积；怎么用消息传递方式实现图卷积网络；怎么用多层图网络完成节点分类任务；SEMI-SUPERVISED CLASSIFICATION WITH GRAPH CONVOLUTIONAL... |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。
- **图卷积**：5 图卷积网络（GCN）；如何从圈像卷积，类比到圈结构卷积；怎么用消息传递方式实现图卷积网络；怎么用多层图网络完成节点分类任务；SEMI-SUPERVISED CLASSIFICATION WITH GRAPH CONVOLUTIONAL NETWORKS （ICLR 2017） 5 图卷积网络（G... 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。
- **GCN**：图卷积网络，通过邻接关系聚合邻居节点特征，再更新节点表示。 考试写作时要补一句：常考 GCN 为什么能利用图结构、聚合邻居信息的基本流程。 易错点是：GCN 不只看节点自身特征，还看邻居。
- **消息传递**：节点从邻居接收信息、聚合信息、更新自身表示，是很多图神经网络的统一视角。 考试写作时要补一句：常考消息、聚合、更新三个步骤。 易错点是：不要只写公式，考试要说明信息沿边传播。
- **节点分类**：5 图卷积网络（GCN）；如何从圈像卷积，类比到圈结构卷积；怎么用消息传递方式实现图卷积网络；怎么用多层图网络完成节点分类任务；SEMI-SUPERVISED CLASSIFICATION WITH GRAPH CONVOLUTIONAL NETWORKS （ICLR 2017） 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：图卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：GCN只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：消息传递只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：节点分类只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。
- 5 图卷积网络（GCN）；如何从圈像卷积，类比到圈结构卷积；怎么用消息传递方式实现图卷积网络；怎么用多层图网络完成节点分类任务；SEMI-SUPERVISED CLASSIFICATION WITH GRAPH CONVOLUTIONAL...
- GCN：邻居聚合 + 线性变换 + 非线性。
- 消息传递：邻居发消息，节点聚合并更新。
- 5 图卷积网络（GCN）；如何从圈像卷积，类比到圈结构卷积；怎么用消息传递方式实现图卷积网络；怎么用多层图网络完成节点分类任务；SEMI-SUPERVISED CLASSIFICATION WITH GRAPH CONVOLUTIONAL...

## 第 30 部分：图卷积网络（GCN）

### 1. 本节先看什么

这一节先把 **图卷积、GCN、节点、邻接矩阵** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 图卷积 | 图卷积网络（GCN）；如何从图像卷积，类比到图结构卷积 图卷积网络（GCN）；men rlced；图像上的卷积操作；d neaby poe；W6T 图卷积网络（GCN）；图像上的卷积操作；W0Lv；w61；W7 | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | 图卷积网络（GCN）；如何从图像卷积，类比到图结构卷积 图卷积网络（GCN）；men rlced；图像上的卷积操作；d neaby poe；W6T 图卷积网络（GCN）；图像上的卷积操作；W0Lv；w61；W7 |
| GCN | 图卷积网络，通过邻接关系聚合邻居节点特征，再更新节点表示。 | 常考 GCN 为什么能利用图结构、聚合邻居信息的基本流程。 | GCN 不只看节点自身特征，还看邻居。 | GCN：邻居聚合 + 线性变换 + 非线性。 |
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |
| 邻接矩阵 | 用矩阵记录节点之间是否相连或连接权重。 | 常考根据图画矩阵，或根据矩阵判断邻居。 | 无向图邻接矩阵通常对称；有向图不一定对称。 | A_ij 表示 i 到 j 是否有边或边权。 |
| 度矩阵 | 对角矩阵，对角线记录每个节点的度。 | 常和拉普拉斯矩阵 L=D-A 一起考。 | 度矩阵不是邻接矩阵，它只放在对角线上。 | D_ii = 节点 i 的度。 |
| 消息传递 | 节点从邻居接收信息、聚合信息、更新自身表示，是很多图神经网络的统一视角。 | 常考消息、聚合、更新三个步骤。 | 不要只写公式，考试要说明信息沿边传播。 | 消息传递：邻居发消息，节点聚合并更新。 |
| 聚合 | 图卷积网络（GCN）；AH （） 将邻居的特征接收、聚合；是否所有的人对自己的评价都是有用的？；You；如何衡量邻属的重要性度 | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | 图卷积网络（GCN）；AH （） 将邻居的特征接收、聚合；是否所有的人对自己的评价都是有用的？；You；如何衡量邻属的重要性度 |

### 3. 像考试答案一样组织语言

- **图卷积**：图卷积网络（GCN）；如何从图像卷积，类比到图结构卷积 图卷积网络（GCN）；men rlced；图像上的卷积操作；d neaby poe；W6T 图卷积网络（GCN）；图像上的卷积操作；W0Lv；w61；W7 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。
- **GCN**：图卷积网络，通过邻接关系聚合邻居节点特征，再更新节点表示。 考试写作时要补一句：常考 GCN 为什么能利用图结构、聚合邻居信息的基本流程。 易错点是：GCN 不只看节点自身特征，还看邻居。
- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。
- **邻接矩阵**：用矩阵记录节点之间是否相连或连接权重。 考试写作时要补一句：常考根据图画矩阵，或根据矩阵判断邻居。 易错点是：无向图邻接矩阵通常对称；有向图不一定对称。
- **度矩阵**：对角矩阵，对角线记录每个节点的度。 考试写作时要补一句：常和拉普拉斯矩阵 L=D-A 一起考。 易错点是：度矩阵不是邻接矩阵，它只放在对角线上。
- **消息传递**：节点从邻居接收信息、聚合信息、更新自身表示，是很多图神经网络的统一视角。 考试写作时要补一句：常考消息、聚合、更新三个步骤。 易错点是：不要只写公式，考试要说明信息沿边传播。
- **聚合**：图卷积网络（GCN）；AH （） 将邻居的特征接收、聚合；是否所有的人对自己的评价都是有用的？；You；如何衡量邻属的重要性度 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。

### 4. 本节自测

- 判断：图卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：GCN只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：邻接矩阵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：度矩阵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：消息传递只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：聚合只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 图卷积网络（GCN）；如何从图像卷积，类比到图结构卷积 图卷积网络（GCN）；men rlced；图像上的卷积操作；d neaby poe；W6T 图卷积网络（GCN）；图像上的卷积操作；W0Lv；w61；W7
- GCN：邻居聚合 + 线性变换 + 非线性。
- 节点表示对象，边表示对象之间的关系。
- A_ij 表示 i 到 j 是否有边或边权。
- D_ii = 节点 i 的度。
- 消息传递：邻居发消息，节点聚合并更新。
- 图卷积网络（GCN）；AH （） 将邻居的特征接收、聚合；是否所有的人对自己的评价都是有用的？；You；如何衡量邻属的重要性度

## 第 31 部分：） 图卷积网络（GCN）-消息传递

### 1. 本节先看什么

这一节先把 **节点、图卷积、GCN、消息传递** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |
| 图卷积 | ） 图卷积网络（GCN）-消息传递；Message Passing；发送Send；1.边上的源节点，往目标节点发送特征；0号 | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | ） 图卷积网络（GCN）-消息传递；Message Passing；发送Send；1.边上的源节点，往目标节点发送特征；0号 |
| GCN | 图卷积网络，通过邻接关系聚合邻居节点特征，再更新节点表示。 | 常考 GCN 为什么能利用图结构、聚合邻居信息的基本流程。 | GCN 不只看节点自身特征，还看邻居。 | GCN：邻居聚合 + 线性变换 + 非线性。 |
| 消息传递 | 节点从邻居接收信息、聚合信息、更新自身表示，是很多图神经网络的统一视角。 | 常考消息、聚合、更新三个步骤。 | 不要只写公式，考试要说明信息沿边传播。 | 消息传递：邻居发消息，节点聚合并更新。 |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。
- **图卷积**：） 图卷积网络（GCN）-消息传递；Message Passing；发送Send；1.边上的源节点，往目标节点发送特征；0号 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。
- **GCN**：图卷积网络，通过邻接关系聚合邻居节点特征，再更新节点表示。 考试写作时要补一句：常考 GCN 为什么能利用图结构、聚合邻居信息的基本流程。 易错点是：GCN 不只看节点自身特征，还看邻居。
- **消息传递**：节点从邻居接收信息、聚合信息、更新自身表示，是很多图神经网络的统一视角。 考试写作时要补一句：常考消息、聚合、更新三个步骤。 易错点是：不要只写公式，考试要说明信息沿边传播。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：图卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：GCN只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：消息传递只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。
- ） 图卷积网络（GCN）-消息传递；Message Passing；发送Send；1.边上的源节点，往目标节点发送特征；0号
- GCN：邻居聚合 + 线性变换 + 非线性。
- 消息传递：邻居发消息，节点聚合并更新。

## 第 32 部分：5 图卷积网络（GCN）-消息传递

### 1. 本节先看什么

这一节先把 **节点、图卷积、GCN、消息传递** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |
| 图卷积 | 5 图卷积网络（GCN）-消息传递；Message Passing；接收Recv；2. 目标节点对收到的特征进行聚合；0号 | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | 5 图卷积网络（GCN）-消息传递；Message Passing；接收Recv；2. 目标节点对收到的特征进行聚合；0号 |
| GCN | 图卷积网络，通过邻接关系聚合邻居节点特征，再更新节点表示。 | 常考 GCN 为什么能利用图结构、聚合邻居信息的基本流程。 | GCN 不只看节点自身特征，还看邻居。 | GCN：邻居聚合 + 线性变换 + 非线性。 |
| 消息传递 | 节点从邻居接收信息、聚合信息、更新自身表示，是很多图神经网络的统一视角。 | 常考消息、聚合、更新三个步骤。 | 不要只写公式，考试要说明信息沿边传播。 | 消息传递：邻居发消息，节点聚合并更新。 |
| 聚合 | 5 图卷积网络（GCN）-消息传递；Message Passing；接收Recv；2. 目标节点对收到的特征进行聚合；0号 | 会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 | 不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。 | 5 图卷积网络（GCN）-消息传递；Message Passing；接收Recv；2. 目标节点对收到的特征进行聚合；0号 |

### 3. 像考试答案一样组织语言

- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。
- **图卷积**：5 图卷积网络（GCN）-消息传递；Message Passing；接收Recv；2. 目标节点对收到的特征进行聚合；0号 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。
- **GCN**：图卷积网络，通过邻接关系聚合邻居节点特征，再更新节点表示。 考试写作时要补一句：常考 GCN 为什么能利用图结构、聚合邻居信息的基本流程。 易错点是：GCN 不只看节点自身特征，还看邻居。
- **消息传递**：节点从邻居接收信息、聚合信息、更新自身表示，是很多图神经网络的统一视角。 考试写作时要补一句：常考消息、聚合、更新三个步骤。 易错点是：不要只写公式，考试要说明信息沿边传播。
- **聚合**：5 图卷积网络（GCN）-消息传递；Message Passing；接收Recv；2. 目标节点对收到的特征进行聚合；0号 考试写作时要补一句：会结合节点、边、邻接矩阵、图任务类型或邻居聚合流程考。 易错点是：不要只看节点自身特征；图学习的重点是把关系结构一起纳入模型。

### 4. 本节自测

- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：图卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：GCN只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：消息传递只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：聚合只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 节点表示对象，边表示对象之间的关系。
- 5 图卷积网络（GCN）-消息传递；Message Passing；接收Recv；2. 目标节点对收到的特征进行聚合；0号
- GCN：邻居聚合 + 线性变换 + 非线性。
- 消息传递：邻居发消息，节点聚合并更新。
- 5 图卷积网络（GCN）-消息传递；Message Passing；接收Recv；2. 目标节点对收到的特征进行聚合；0号

## 第 33 部分：图学习课件第一部分，第 66 页

### 1. 本节先看什么

这一节先把 **图学习、节点、邻接矩阵、度矩阵** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 图学习 | 研究如何在图结构数据上学习表示和完成预测，核心是同时利用节点特征与关系结构。 | 常考图数据适用场景、节点/边/图级任务。 | 不要只看单个样本特征，图学习关键是关系。 | 图学习 = 特征信息 + 结构信息。 |
| 节点 | 图中的对象或实体，可以带有节点特征。 | 常考节点分类任务：预测每个节点的类别。 | 节点特征和边关系是两类信息，不要混在一起。 | 节点表示对象，边表示对象之间的关系。 |
| 邻接矩阵 | 用矩阵记录节点之间是否相连或连接权重。 | 常考根据图画矩阵，或根据矩阵判断邻居。 | 无向图邻接矩阵通常对称；有向图不一定对称。 | A_ij 表示 i 到 j 是否有边或边权。 |
| 度矩阵 | 对角矩阵，对角线记录每个节点的度。 | 常和拉普拉斯矩阵 L=D-A 一起考。 | 度矩阵不是邻接矩阵，它只放在对角线上。 | D_ii = 节点 i 的度。 |
| 拉普拉斯矩阵 | 用度矩阵和邻接矩阵构造，反映图的连接结构。 | 常考 L=D-A 以及它在谱图方法中的作用。 | 不要把 L 和 A 混用。 | 图拉普拉斯：L = D - A。 |

### 3. 像考试答案一样组织语言

- **图学习**：研究如何在图结构数据上学习表示和完成预测，核心是同时利用节点特征与关系结构。 考试写作时要补一句：常考图数据适用场景、节点/边/图级任务。 易错点是：不要只看单个样本特征，图学习关键是关系。
- **节点**：图中的对象或实体，可以带有节点特征。 考试写作时要补一句：常考节点分类任务：预测每个节点的类别。 易错点是：节点特征和边关系是两类信息，不要混在一起。
- **邻接矩阵**：用矩阵记录节点之间是否相连或连接权重。 考试写作时要补一句：常考根据图画矩阵，或根据矩阵判断邻居。 易错点是：无向图邻接矩阵通常对称；有向图不一定对称。
- **度矩阵**：对角矩阵，对角线记录每个节点的度。 考试写作时要补一句：常和拉普拉斯矩阵 L=D-A 一起考。 易错点是：度矩阵不是邻接矩阵，它只放在对角线上。
- **拉普拉斯矩阵**：用度矩阵和邻接矩阵构造，反映图的连接结构。 考试写作时要补一句：常考 L=D-A 以及它在谱图方法中的作用。 易错点是：不要把 L 和 A 混用。

### 4. 本节自测

- 判断：图学习只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：节点只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：邻接矩阵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：度矩阵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：拉普拉斯矩阵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 图学习 = 特征信息 + 结构信息。
- 节点表示对象，边表示对象之间的关系。
- A_ij 表示 i 到 j 是否有边或边权。
- D_ii = 节点 i 的度。
- 图拉普拉斯：L = D - A。

## 四、考前总复盘

考前不要平均用力。优先检查下面这些问题：

1. 每个核心概念能不能用一句话说明“它是什么”。
2. 能不能说出它在模型结构、训练流程或数据处理中的位置。
3. 能不能说出一个最常见的错误说法。
4. 遇到选择题时，能不能判断选项是在混淆概念、夸大作用，还是写反了训练/推理阶段。

## 五、打印建议

<style>@media print { @page { margin: 8mm; } body { font-size: 11pt; line-height: 1.35; } h1, h2, h3 { page-break-after: avoid; } table { font-size: 9pt; border-collapse: collapse; } th, td { padding: 3px 5px; border: 1px solid #ddd; vertical-align: top; } }</style>
