# 多层感知机2-学生分发版-终版2026：期末考试复习讲义

> 使用方式：先通读“学习路线”，再按章节背“必记句子”，最后用每章自测题检查。这里不是课件原文搬运，而是按考试复习顺序重写。

## 一、这份课件先解决什么问题

这一类课件是深度学习入门骨架：先理解线性变换和激活函数，再理解损失函数、反向传播、优化器和训练流程。

考试常围绕“为什么需要非线性”“分类输出层和损失函数怎么匹配”“训练循环每一步做什么”来考。

## 二、学习路线

1. **第三章 分类问题与多层感知机**：先抓 Sigmoid、Softmax、交叉熵、多层感知机、Logistic。
2. **第三章 分类问题与多层感知机 / 第一节 分类问题概述**：先抓 损失函数、Sigmoid、Softmax、交叉熵、激活函数。
3. **第三章 分类问题与多层感知机 / 第二节 Logistic回归（二分类）**：先抓 Sigmoid、Logistic、交叉熵、损失函数、BCEWithLogitsLoss。
4. **第三章 分类问题与多层感知机 / 第三节 Softmax回归（多分类）**：先抓 Softmax、梯度下降、Sigmoid、交叉熵。
5. **第三章 分类问题与多层感知机 / 第四节 Logistic vs Softmax：统一视角**：先抓 损失函数、Logistic、Sigmoid、Softmax、激活函数。
6. **第三章 分类问题与多层感知机 / 第五节 多层感知机（MLP），也叫做全连接神经网络**：先抓 激活函数、MLP、Logistic、多层感知机、Sigmoid。

## 三、章节详解

## 第 1 部分：第三章 分类问题与多层感知机

### 1. 本节先看什么

这一节先把 **Sigmoid、Softmax、交叉熵、多层感知机** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| Sigmoid | 把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 | 常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 | Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。 | Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。 |
| Softmax | 把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 | 常考多分类输出、注意力权重为什么能加权求和。 | Softmax 是对一组数整体归一化，不是逐个独立压缩。 | Softmax 输出非负且总和为 1。 |
| 交叉熵 | 第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds... |
| 多层感知机 | 第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds... |
| Logistic | 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失)，PyTorch实现；3. Softmax回归：多分类原理，Softmax vs Hardmax，交叉熵损失；4. Logistic vs... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失)，PyTorch实现；3. Softmax回归：多分类原理，Softmax v... |
| MLP | 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失)，PyTorch实现；3. Softmax回归：多分类原理，Softmax vs Hardmax，交叉熵损失；4. Logistic vs... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失)，PyTorch实现；3. Softmax回归：多分类原理，Softmax v... |
| 激活函数 | 给线性变换加入非线性，使网络能拟合非线性关系。 | 常考没有激活函数时多层线性网络仍等价于线性模型。 | 不要把激活函数说成只改变维度，它主要提供非线性。 | 仿射变换 + 激活函数 = 神经网络层的基本模式。 |
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |
| BCEWithLogitsLoss | 一、分类问题基础；回归 vs 分类：连续值 vs 离散类别；二分类 vs 多分类：Sigmoid/BCE vs Softmax/CE；二、Logistic回归；Sigmoid：$\sigma(z) = \frac{1}{1+e^{-z}}$，将$(-\infty, +\infty)$映射到$(0,1... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 一、分类问题基础；回归 vs 分类：连续值 vs 离散类别；二分类 vs 多分类：Sigmoid/BCE vs Softmax/CE；二、Logistic回归；Sigmoid：$\sigma(z) = \frac{1}{1+e^{-z}}$... |
| CrossEntropyLoss | 一、分类问题基础；回归 vs 分类：连续值 vs 离散类别；二分类 vs 多分类：Sigmoid/BCE vs Softmax/CE；二、Logistic回归；Sigmoid：$\sigma(z) = \frac{1}{1+e^{-z}}$，将$(-\infty, +\infty)$映射到$(0,1... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 一、分类问题基础；回归 vs 分类：连续值 vs 离散类别；二分类 vs 多分类：Sigmoid/BCE vs Softmax/CE；二、Logistic回归；Sigmoid：$\sigma(z) = \frac{1}{1+e^{-z}}$... |

### 3. 像考试答案一样组织语言

- **Sigmoid**：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 考试写作时要补一句：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 易错点是：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- **Softmax**：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 考试写作时要补一句：常考多分类输出、注意力权重为什么能加权求和。 易错点是：Softmax 是对一组数整体归一化，不是逐个独立压缩。
- **交叉熵**：第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **多层感知机**：第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **Logistic**：1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失)，PyTorch实现；3. Softmax回归：多分类原理，Softmax vs Hardmax，交叉熵损失；4. Logistic vs... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **MLP**：1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失)，PyTorch实现；3. Softmax回归：多分类原理，Softmax vs Hardmax，交叉熵损失；4. Logistic vs... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **激活函数**：给线性变换加入非线性，使网络能拟合非线性关系。 考试写作时要补一句：常考没有激活函数时多层线性网络仍等价于线性模型。 易错点是：不要把激活函数说成只改变维度，它主要提供非线性。
- **损失函数**：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 考试写作时要补一句：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 易错点是：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。

### 4. 本节自测

- 判断：Sigmoid只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Softmax只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：交叉熵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：多层感知机只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Logistic只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：MLP只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：激活函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：损失函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
- Softmax 输出非负且总和为 1。
- 第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds...
- 第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds...
- 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失)，PyTorch实现；3. Softmax回归：多分类原理，Softmax v...
- 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失)，PyTorch实现；3. Softmax回归：多分类原理，Softmax v...
- 仿射变换 + 激活函数 = 神经网络层的基本模式。
- 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
- 一、分类问题基础；回归 vs 分类：连续值 vs 离散类别；二分类 vs 多分类：Sigmoid/BCE vs Softmax/CE；二、Logistic回归；Sigmoid：$\sigma(z) = \frac{1}{1+e^{-z}}$...
- 一、分类问题基础；回归 vs 分类：连续值 vs 离散类别；二分类 vs 多分类：Sigmoid/BCE vs Softmax/CE；二、Logistic回归；Sigmoid：$\sigma(z) = \frac{1}{1+e^{-z}}$...

## 第 2 部分：第三章 分类问题与多层感知机 / 第一节 分类问题概述

### 1. 本节先看什么

这一节先把 **损失函数、Sigmoid、Softmax、交叉熵** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |
| Sigmoid | 把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 | 常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 | Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。 | Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。 |
| Softmax | 把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 | 常考多分类输出、注意力权重为什么能加权求和。 | Softmax 是对一组数整体归一化，不是逐个独立压缩。 | Softmax 输出非负且总和为 1。 |
| 交叉熵 | 第一节 分类问题概述；回归 vs 分类；给定数据点集合 ${(x^{(i)}, t^{(i)})}$，目标是找到映射 $f: \mathbb{R}^m \rightarrow \Omega$。；任务类型 / 标签空间 $\Omega$ / 典型问题；回归 / 连续值 / 房价预测、温度预测 | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 第一节 分类问题概述；回归 vs 分类；给定数据点集合 ${(x^{(i)}, t^{(i)})}$，目标是找到映射 $f: \mathbb{R}^m \rightarrow \Omega$。；任务类型 / 标签空间 $\Omega$ /... |
| 激活函数 | 给线性变换加入非线性，使网络能拟合非线性关系。 | 常考没有激活函数时多层线性网络仍等价于线性模型。 | 不要把激活函数说成只改变维度，它主要提供非线性。 | 仿射变换 + 激活函数 = 神经网络层的基本模式。 |

### 3. 像考试答案一样组织语言

- **损失函数**：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 考试写作时要补一句：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 易错点是：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- **Sigmoid**：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 考试写作时要补一句：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 易错点是：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- **Softmax**：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 考试写作时要补一句：常考多分类输出、注意力权重为什么能加权求和。 易错点是：Softmax 是对一组数整体归一化，不是逐个独立压缩。
- **交叉熵**：第一节 分类问题概述；回归 vs 分类；给定数据点集合 ${(x^{(i)}, t^{(i)})}$，目标是找到映射 $f: \mathbb{R}^m \rightarrow \Omega$。；任务类型 | 标签空间 $\Omega$ | 典型问题；回归 | 连续值 | 房价预测、温度预测 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **激活函数**：给线性变换加入非线性，使网络能拟合非线性关系。 考试写作时要补一句：常考没有激活函数时多层线性网络仍等价于线性模型。 易错点是：不要把激活函数说成只改变维度，它主要提供非线性。

### 4. 本节自测

- 判断：损失函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Sigmoid只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Softmax只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：交叉熵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：激活函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
- Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
- Softmax 输出非负且总和为 1。
- 第一节 分类问题概述；回归 vs 分类；给定数据点集合 ${(x^{(i)}, t^{(i)})}$，目标是找到映射 $f: \mathbb{R}^m \rightarrow \Omega$。；任务类型 | 标签空间 $\Omega$ |...
- 仿射变换 + 激活函数 = 神经网络层的基本模式。

## 第 3 部分：第三章 分类问题与多层感知机 / 第二节 Logistic回归（二分类）

### 1. 本节先看什么

这一节先把 **Sigmoid、Logistic、交叉熵、损失函数** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| Sigmoid | 把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 | 常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 | Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。 | Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。 |
| Logistic | 第二节 Logistic回归（二分类）；从线性回归到Logistic回归；线性回归的局限：对于二分类（标签0或1），线性回归预测值可能是任意实数，无法解释为概率。；解决方案：引入Logits和Sigmoid；Step 1: Odds Ratio（几率比） | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 第二节 Logistic回归（二分类）；从线性回归到Logistic回归；线性回归的局限：对于二分类（标签0或1），线性回归预测值可能是任意实数，无法解释为概率。；解决方案：引入Logits和Sigmoid；Step 1: Odds Rat... |
| 交叉熵 | 二元交叉熵损失（BCE Loss）；$$L = -\frac{1}{N} \sum_{i=1}^{N} \left[ y_i \log \hat{y}_i + (1-y_i)\log(1-\hat{y}_i) \right]$$；当 $y_i = 1$：损失为 $-\log \hat{y}_i$，希... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 二元交叉熵损失（BCE Loss）；$$L = -\frac{1}{N} \sum_{i=1}^{N} \left[ y_i \log \hat{y}_i + (1-y_i)\log(1-\hat{y}_i) \right]$$；当 $y_... |
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |
| BCEWithLogitsLoss | PyTorch最佳实践：BCEWithLogitsLoss；数值稳定性问题：直接 Sigmoid + BCELoss 当logits很大时会溢出。；解决方案：BCEWithLogitsLoss 内部合并 sigmoid和log运算。；组合 / 模型最后一层 / 损失函数 / 数值稳定性 / 计算效率... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | PyTorch最佳实践：BCEWithLogitsLoss；数值稳定性问题：直接 Sigmoid + BCELoss 当logits很大时会溢出。；解决方案：BCEWithLogitsLoss 内部合并 sigmoid和log运算。；组合... |

### 3. 像考试答案一样组织语言

- **Sigmoid**：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 考试写作时要补一句：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 易错点是：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- **Logistic**：第二节 Logistic回归（二分类）；从线性回归到Logistic回归；线性回归的局限：对于二分类（标签0或1），线性回归预测值可能是任意实数，无法解释为概率。；解决方案：引入Logits和Sigmoid；Step 1: Odds Ratio（几率比） 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **交叉熵**：二元交叉熵损失（BCE Loss）；$$L = -\frac{1}{N} \sum_{i=1}^{N} \left[ y_i \log \hat{y}_i + (1-y_i)\log(1-\hat{y}_i) \right]$$；当 $y_i = 1$：损失为 $-\log \hat{y}_i$，希... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **损失函数**：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 考试写作时要补一句：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 易错点是：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- **BCEWithLogitsLoss**：PyTorch最佳实践：BCEWithLogitsLoss；数值稳定性问题：直接 Sigmoid + BCELoss 当logits很大时会溢出。；解决方案：BCEWithLogitsLoss 内部合并 sigmoid和log运算。；组合 | 模型最后一层 | 损失函数 | 数值稳定性 | 计算效率... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。

### 4. 本节自测

- 判断：Sigmoid只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Logistic只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：交叉熵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：损失函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：BCEWithLogitsLoss只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
- 第二节 Logistic回归（二分类）；从线性回归到Logistic回归；线性回归的局限：对于二分类（标签0或1），线性回归预测值可能是任意实数，无法解释为概率。；解决方案：引入Logits和Sigmoid；Step 1: Odds Rat...
- 二元交叉熵损失（BCE Loss）；$$L = -\frac{1}{N} \sum_{i=1}^{N} \left[ y_i \log \hat{y}_i + (1-y_i)\log(1-\hat{y}_i) \right]$$；当 $y_...
- 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
- PyTorch最佳实践：BCEWithLogitsLoss；数值稳定性问题：直接 Sigmoid + BCELoss 当logits很大时会溢出。；解决方案：BCEWithLogitsLoss 内部合并 sigmoid和log运算。；组合...

## 第 4 部分：第三章 分类问题与多层感知机 / 第三节 Softmax回归（多分类）

### 1. 本节先看什么

这一节先把 **Softmax、梯度下降、Sigmoid、交叉熵** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| Softmax | 把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 | 常考多分类输出、注意力权重为什么能加权求和。 | Softmax 是对一组数整体归一化，不是逐个独立压缩。 | Softmax 输出非负且总和为 1。 |
| 梯度下降 | 使用Softmax的原因；为什么叫softmax（软分类）；hardmax最大的问题（取最大值的问题）是不能求导，无法用梯度下降训练神经网络 | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 使用Softmax的原因；为什么叫softmax（软分类）；hardmax最大的问题（取最大值的问题）是不能求导，无法用梯度下降训练神经网络 |
| Sigmoid | 把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 | 常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 | Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。 | Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。 |
| 交叉熵 | 交叉熵损失（Cross-Entropy Loss）；$$L = -\frac{1}{N} \sum_{n=1}^{N} \log h_{k^*}(x^{(n)})$$；其中 $k^*$ 是样本的真实类别。；由于独热编码的简化：；原始形式：$L = -\frac{1}{N} \sum_{n} \sum... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 交叉熵损失（Cross-Entropy Loss）；$$L = -\frac{1}{N} \sum_{n=1}^{N} \log h_{k^*}(x^{(n)})$$；其中 $k^*$ 是样本的真实类别。；由于独热编码的简化：；原始形式：$... |

### 3. 像考试答案一样组织语言

- **Softmax**：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 考试写作时要补一句：常考多分类输出、注意力权重为什么能加权求和。 易错点是：Softmax 是对一组数整体归一化，不是逐个独立压缩。
- **梯度下降**：使用Softmax的原因；为什么叫softmax（软分类）；hardmax最大的问题（取最大值的问题）是不能求导，无法用梯度下降训练神经网络 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **Sigmoid**：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 考试写作时要补一句：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 易错点是：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- **交叉熵**：交叉熵损失（Cross-Entropy Loss）；$$L = -\frac{1}{N} \sum_{n=1}^{N} \log h_{k^*}(x^{(n)})$$；其中 $k^*$ 是样本的真实类别。；由于独热编码的简化：；原始形式：$L = -\frac{1}{N} \sum_{n} \sum... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。

### 4. 本节自测

- 判断：Softmax只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：梯度下降只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Sigmoid只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：交叉熵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- Softmax 输出非负且总和为 1。
- 使用Softmax的原因；为什么叫softmax（软分类）；hardmax最大的问题（取最大值的问题）是不能求导，无法用梯度下降训练神经网络
- Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
- 交叉熵损失（Cross-Entropy Loss）；$$L = -\frac{1}{N} \sum_{n=1}^{N} \log h_{k^*}(x^{(n)})$$；其中 $k^*$ 是样本的真实类别。；由于独热编码的简化：；原始形式：$...

## 第 5 部分：第三章 分类问题与多层感知机 / 第四节 Logistic vs Softmax：统一视角

### 1. 本节先看什么

这一节先把 **损失函数、Logistic、Sigmoid、Softmax** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |
| Logistic | 第四节 Logistic vs Softmax：统一视角；Logistic是Softmax的特例；当 $K=2$ 时，Softmax退化为Sigmoid：；$$h_1(x) = \frac{e^{z_1}}{e^{z_1} + e^{z_2}} = \frac{1}{1 + e^{-(z_1-z_2... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 第四节 Logistic vs Softmax：统一视角；Logistic是Softmax的特例；当 $K=2$ 时，Softmax退化为Sigmoid：；$$h_1(x) = \frac{e^{z_1}}{e^{z_1} + e^{z_2... |
| Sigmoid | 把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 | 常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 | Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。 | Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。 |
| Softmax | 把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 | 常考多分类输出、注意力权重为什么能加权求和。 | Softmax 是对一组数整体归一化，不是逐个独立压缩。 | Softmax 输出非负且总和为 1。 |
| 激活函数 | 给线性变换加入非线性，使网络能拟合非线性关系。 | 常考没有激活函数时多层线性网络仍等价于线性模型。 | 不要把激活函数说成只改变维度，它主要提供非线性。 | 仿射变换 + 激活函数 = 神经网络层的基本模式。 |

### 3. 像考试答案一样组织语言

- **损失函数**：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 考试写作时要补一句：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 易错点是：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- **Logistic**：第四节 Logistic vs Softmax：统一视角；Logistic是Softmax的特例；当 $K=2$ 时，Softmax退化为Sigmoid：；$$h_1(x) = \frac{e^{z_1}}{e^{z_1} + e^{z_2}} = \frac{1}{1 + e^{-(z_1-z_2... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **Sigmoid**：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 考试写作时要补一句：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 易错点是：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- **Softmax**：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 考试写作时要补一句：常考多分类输出、注意力权重为什么能加权求和。 易错点是：Softmax 是对一组数整体归一化，不是逐个独立压缩。
- **激活函数**：给线性变换加入非线性，使网络能拟合非线性关系。 考试写作时要补一句：常考没有激活函数时多层线性网络仍等价于线性模型。 易错点是：不要把激活函数说成只改变维度，它主要提供非线性。

### 4. 本节自测

- 判断：损失函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Logistic只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Sigmoid只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Softmax只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：激活函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
- 第四节 Logistic vs Softmax：统一视角；Logistic是Softmax的特例；当 $K=2$ 时，Softmax退化为Sigmoid：；$$h_1(x) = \frac{e^{z_1}}{e^{z_1} + e^{z_2...
- Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
- Softmax 输出非负且总和为 1。
- 仿射变换 + 激活函数 = 神经网络层的基本模式。

## 第 6 部分：第三章 分类问题与多层感知机 / 第五节 多层感知机（MLP），也叫做全连接神经网络

### 1. 本节先看什么

这一节先把 **激活函数、MLP、Logistic、多层感知机** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 激活函数 | 给线性变换加入非线性，使网络能拟合非线性关系。 | 常考没有激活函数时多层线性网络仍等价于线性模型。 | 不要把激活函数说成只改变维度，它主要提供非线性。 | 仿射变换 + 激活函数 = 神经网络层的基本模式。 |
| MLP | 第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间的神经元不连接 常见激活函数对比；激活函数 / 公式 / 输... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间... |
| Logistic | 第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间的神经元不连接 | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间... |
| 多层感知机 | 第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间的神经元不连接 | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间... |
| Sigmoid | 把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 | 常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 | Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。 | Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。 |
| 梯度消失 | 反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。 | 常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。 | 梯度消失不是过拟合；它是参数难以有效更新的问题。 | 多个小于 1 的因子连乘，会让梯度趋近 0。 |
| 仿射变换 | 激活函数的本质作用；仿射变换（线性变换+平移）：旋转、缩放、平移特征空间；$$u_j^{(l)} = \sum_{i} w_{ji}^{(l)} y_i^{(l-1)} + b_j^{(l)}$$；神经元的操作仅限于仿射变换（线性组合+平移），不包含激活函数。；激活函数：对特征空间进行非线性扭曲 | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 激活函数的本质作用；仿射变换（线性变换+平移）：旋转、缩放、平移特征空间；$$u_j^{(l)} = \sum_{i} w_{ji}^{(l)} y_i^{(l-1)} + b_j^{(l)}$$；神经元的操作仅限于仿射变换（线性组合+平移... |

### 3. 像考试答案一样组织语言

- **激活函数**：给线性变换加入非线性，使网络能拟合非线性关系。 考试写作时要补一句：常考没有激活函数时多层线性网络仍等价于线性模型。 易错点是：不要把激活函数说成只改变维度，它主要提供非线性。
- **MLP**：第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间的神经元不连接 常见激活函数对比；激活函数 | 公式 | 输... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **Logistic**：第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间的神经元不连接 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **多层感知机**：第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间的神经元不连接 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **Sigmoid**：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 考试写作时要补一句：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 易错点是：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- **梯度消失**：反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。 考试写作时要补一句：常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。 易错点是：梯度消失不是过拟合；它是参数难以有效更新的问题。
- **仿射变换**：激活函数的本质作用；仿射变换（线性变换+平移）：旋转、缩放、平移特征空间；$$u_j^{(l)} = \sum_{i} w_{ji}^{(l)} y_i^{(l-1)} + b_j^{(l)}$$；神经元的操作仅限于仿射变换（线性组合+平移），不包含激活函数。；激活函数：对特征空间进行非线性扭曲 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。

### 4. 本节自测

- 判断：激活函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：MLP只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Logistic只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：多层感知机只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Sigmoid只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：梯度消失只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：仿射变换只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 仿射变换 + 激活函数 = 神经网络层的基本模式。
- 第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间...
- 第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间...
- 第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间...
- Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
- 多个小于 1 的因子连乘，会让梯度趋近 0。
- 激活函数的本质作用；仿射变换（线性变换+平移）：旋转、缩放、平移特征空间；$$u_j^{(l)} = \sum_{i} w_{ji}^{(l)} y_i^{(l-1)} + b_j^{(l)}$$；神经元的操作仅限于仿射变换（线性组合+平移...

## 四、考前总复盘

考前不要平均用力。优先检查下面这些问题：

1. 每个核心概念能不能用一句话说明“它是什么”。
2. 能不能说出它在模型结构、训练流程或数据处理中的位置。
3. 能不能说出一个最常见的错误说法。
4. 遇到选择题时，能不能判断选项是在混淆概念、夸大作用，还是写反了训练/推理阶段。

## 五、打印建议

<style>@media print { @page { margin: 8mm; } body { font-size: 11pt; line-height: 1.35; } h1, h2, h3 { page-break-after: avoid; } table { font-size: 9pt; border-collapse: collapse; } th, td { padding: 3px 5px; border: 1px solid #ddd; vertical-align: top; } }</style>
