# 卷积神经网络3-学生分发版-终版2026(1)：期末考试复习讲义

> 使用方式：先通读“学习路线”，再按章节背“必记句子”，最后用每章自测题检查。这里不是课件原文搬运，而是按考试复习顺序重写。

## 一、这份课件先解决什么问题

这一类课件主要解决图像数据如何进入神经网络的问题。复习时先抓住“空间结构不能被随便展平”这个动机，再理解卷积、池化、感受野、典型 CNN 架构和训练配置。

考试通常不会只问名词，而是把输出尺寸、参数共享、局部连接、训练技巧和损失函数匹配放在一起判断。

## 二、学习路线

1. **第六章 CNN高级主题与应用**：先抓 卷积、VGG、ResNet、LeNet、GoogLeNet。
2. **第六章 CNN高级主题与应用 / 第1节 CNN神经网络训练完整实践**：先抓 ImageFolder、数据增强、卷积、Dropout、学习率。
3. **第六章 CNN高级主题与应用 / 第2节 现代卷积神经网络**：先抓 卷积、卷积核、Dropout、VGG、LeNet。
4. **第六章 CNN高级主题与应用 / 第3节 迁移学习（Transfer Learning）**：先抓 迁移学习、AlexNet、ResNet、VGG、卷积。
5. **第六章 CNN高级主题与应用 / 第3节：迁移学习**：先抓 学习率、卷积、卷积核、标准化、LeNet。

## 三、章节详解

## 第 1 部分：第六章 CNN高级主题与应用

### 1. 本节先看什么

这一节先把 **卷积、VGG、ResNet、LeNet** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 卷积 | 用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 | 常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 | 不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。 | 输出尺寸：(输入尺寸 + 2P - K) / S + 1。 |
| VGG | 大量使用 3x3 小卷积核堆叠构建深层网络。 | 常考小卷积核堆叠为什么能扩大感受野并减少参数。 | 不要把 VGG 的特点写成复杂多分支结构。 | VGG：多层小卷积核堆叠。 |
| ResNet | 用残差/跳跃连接让网络学习 F(x)+x，缓解深层网络退化和梯度传播困难。 | 常考残差连接为什么能训练更深网络。 | 残差连接不是单纯增加层数，而是改变信息和梯度路径。 | ResNet 核心：y = F(x) + x。 |
| LeNet | 第六章 CNN高级主题与应用；1. CNN神经网络训练完整实践（RPS图像分类）；2. 现代卷积神经网络（VGG / GoogLeNet / ResNet）；3. 迁移学习（Transfer Learning） 三、GoogLeNet（Inception）网络；ILSVRC2014冠军🏆：GoogL... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第六章 CNN高级主题与应用；1. CNN神经网络训练完整实践（RPS图像分类）；2. 现代卷积神经网络（VGG / GoogLeNet / ResNet）；3. 迁移学习（Transfer Learning） 三、GoogLeNet（In... |
| GoogLeNet | 第六章 CNN高级主题与应用；1. CNN神经网络训练完整实践（RPS图像分类）；2. 现代卷积神经网络（VGG / GoogLeNet / ResNet）；3. 迁移学习（Transfer Learning） 三、GoogLeNet（Inception）网络；ILSVRC2014冠军🏆：GoogL... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第六章 CNN高级主题与应用；1. CNN神经网络训练完整实践（RPS图像分类）；2. 现代卷积神经网络（VGG / GoogLeNet / ResNet）；3. 迁移学习（Transfer Learning） 三、GoogLeNet（In... |
| Inception | 三、GoogLeNet（Inception）网络；ILSVRC2014冠军🏆：GoogLenet/Inception-v1；GoogLeNet核心创新；GoogLeNet（Inception v1）是2014年ImageNet分类竞赛的冠军。与VGG-16相比，它在网络更深（22层）的同时，参数量只... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 三、GoogLeNet（Inception）网络；ILSVRC2014冠军🏆：GoogLenet/Inception-v1；GoogLeNet核心创新；GoogLeNet（Inception v1）是2014年ImageNet分类竞赛的冠军... |
| 池化 | 对局部区域做最大值或平均值汇总，降低空间尺寸并增强一定平移不变性。 | 常考池化作用、是否有可学习参数、与卷积的区别。 | 池化不是全连接，也不是用来增加参数量。 | 池化：降采样、压缩空间尺寸、保留显著信息。 |
| 感受野 | 某一层一个神经元能看到原始输入图像的区域大小。 | 常考层数、卷积核大小、步长如何影响感受野。 | 步长变大通常会让前一层对应感受野扩大，不是缩小。 | 层越深，感受野通常越大；多层小卷积能逐步扩大感受野。 |
| 梯度消失 | 反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。 | 常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。 | 梯度消失不是过拟合；它是参数难以有效更新的问题。 | 多个小于 1 的因子连乘，会让梯度趋近 0。 |
| 残差连接 | GoogLeNet与其他网络的参数对比；网络 / 参数量 / 准确率 / 优势；VGG-16 / 138M / 71.5% / 结构简单；GoogLeNet / 6.8M / 69.8% / 参数少20倍；ResNet-50 / 25.6M / 76.0% / 残差连接 四、ResNet（残差网络）... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | GoogLeNet与其他网络的参数对比；网络 / 参数量 / 准确率 / 优势；VGG-16 / 138M / 71.5% / 结构简单；GoogLeNet / 6.8M / 69.8% / 参数少20倍；ResNet-50 / 25.6M... |
| 迁移学习 | 第六章 CNN高级主题与应用；1. CNN神经网络训练完整实践（RPS图像分类）；2. 现代卷积神经网络（VGG / GoogLeNet / ResNet）；3. 迁移学习（Transfer Learning） 五、CNN架构的参数和性能对比；CNN 架构对比：准确率 vs 参数量 vs 计算量；核... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第六章 CNN高级主题与应用；1. CNN神经网络训练完整实践（RPS图像分类）；2. 现代卷积神经网络（VGG / GoogLeNet / ResNet）；3. 迁移学习（Transfer Learning） 五、CNN架构的参数和性能对... |
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |
| 学习率 | 控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 | 常考学习率过大/过小的训练现象，以及学习率调度的目的。 | 学习率不是越大越好；也不是模型学到的参数。 | 学习率决定更新步长。 |
| Batch | 一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 | 常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 | Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。 | Batch 是一批样本；Epoch 是全训练集完整训练一遍。 |
| 交叉熵 | Inception模块；Inception模块包含四个并行分支：；1. 1×1卷积分支：降维 + 特征提取；2. 3×3卷积分支：中等感受野；3. 5×5卷积分支：大感受野 | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | Inception模块；Inception模块包含四个并行分支：；1. 1×1卷积分支：降维 + 特征提取；2. 3×3卷积分支：中等感受野；3. 5×5卷积分支：大感受野 |
| 归一化 | 把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 | 常考图像预处理为什么要缩放像素值。 | 归一化不等于 BatchNorm，前者多是输入预处理。 | 图像常先把像素值缩放到 0-1。 |

### 3. 像考试答案一样组织语言

- **卷积**：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 考试写作时要补一句：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 易错点是：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- **VGG**：大量使用 3x3 小卷积核堆叠构建深层网络。 考试写作时要补一句：常考小卷积核堆叠为什么能扩大感受野并减少参数。 易错点是：不要把 VGG 的特点写成复杂多分支结构。
- **ResNet**：用残差/跳跃连接让网络学习 F(x)+x，缓解深层网络退化和梯度传播困难。 考试写作时要补一句：常考残差连接为什么能训练更深网络。 易错点是：残差连接不是单纯增加层数，而是改变信息和梯度路径。
- **LeNet**：第六章 CNN高级主题与应用；1. CNN神经网络训练完整实践（RPS图像分类）；2. 现代卷积神经网络（VGG / GoogLeNet / ResNet）；3. 迁移学习（Transfer Learning） 三、GoogLeNet（Inception）网络；ILSVRC2014冠军🏆：GoogL... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **GoogLeNet**：第六章 CNN高级主题与应用；1. CNN神经网络训练完整实践（RPS图像分类）；2. 现代卷积神经网络（VGG / GoogLeNet / ResNet）；3. 迁移学习（Transfer Learning） 三、GoogLeNet（Inception）网络；ILSVRC2014冠军🏆：GoogL... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **Inception**：三、GoogLeNet（Inception）网络；ILSVRC2014冠军🏆：GoogLenet/Inception-v1；GoogLeNet核心创新；GoogLeNet（Inception v1）是2014年ImageNet分类竞赛的冠军。与VGG-16相比，它在网络更深（22层）的同时，参数量只... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **池化**：对局部区域做最大值或平均值汇总，降低空间尺寸并增强一定平移不变性。 考试写作时要补一句：常考池化作用、是否有可学习参数、与卷积的区别。 易错点是：池化不是全连接，也不是用来增加参数量。
- **感受野**：某一层一个神经元能看到原始输入图像的区域大小。 考试写作时要补一句：常考层数、卷积核大小、步长如何影响感受野。 易错点是：步长变大通常会让前一层对应感受野扩大，不是缩小。

### 4. 本节自测

- 判断：卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：VGG只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：ResNet只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：LeNet只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：GoogLeNet只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Inception只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：池化只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：感受野只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 输出尺寸：(输入尺寸 + 2P - K) / S + 1。
- VGG：多层小卷积核堆叠。
- ResNet 核心：y = F(x) + x。
- 第六章 CNN高级主题与应用；1. CNN神经网络训练完整实践（RPS图像分类）；2. 现代卷积神经网络（VGG / GoogLeNet / ResNet）；3. 迁移学习（Transfer Learning） 三、GoogLeNet（In...
- 第六章 CNN高级主题与应用；1. CNN神经网络训练完整实践（RPS图像分类）；2. 现代卷积神经网络（VGG / GoogLeNet / ResNet）；3. 迁移学习（Transfer Learning） 三、GoogLeNet（In...
- 三、GoogLeNet（Inception）网络；ILSVRC2014冠军🏆：GoogLenet/Inception-v1；GoogLeNet核心创新；GoogLeNet（Inception v1）是2014年ImageNet分类竞赛的冠军...
- 池化：降采样、压缩空间尺寸、保留显著信息。
- 层越深，感受野通常越大；多层小卷积能逐步扩大感受野。
- 多个小于 1 的因子连乘，会让梯度趋近 0。
- GoogLeNet与其他网络的参数对比；网络 | 参数量 | 准确率 | 优势；VGG-16 | 138M | 71.5% | 结构简单；GoogLeNet | 6.8M | 69.8% | 参数少20倍；ResNet-50 | 25.6M...

## 第 2 部分：第六章 CNN高级主题与应用 / 第1节 CNN神经网络训练完整实践

### 1. 本节先看什么

这一节先把 **ImageFolder、数据增强、卷积、Dropout** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| ImageFolder | ImageFolder简介；ImageFolder是PyTorch提供的通用数据集类，用于加载按文件夹组织的自定义图像数据集。；文件夹结构要求：；dataset_root/ ← 根目录；├── class_a/ ← 类别A的文件夹 Step 1：数据下载与ImageFolder加载；数据下载：使用自... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | ImageFolder简介；ImageFolder是PyTorch提供的通用数据集类，用于加载按文件夹组织的自定义图像数据集。；文件夹结构要求：；dataset_root/ ← 根目录；├── class_a/ ← 类别A的文件夹 Step... |
| 数据增强 | Step 2：数据标准化与数据增强；数据划分（保存索引复用）：；1. 只划分一次：使用random_split获取训练/验证索引；2. 保存索引：train_idx, val_idx供后续复用；3. 计算统计量：使用训练集索引计算mean/std 🤔思考：为什么验证集的Loss和Accuracy在上... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | Step 2：数据标准化与数据增强；数据划分（保存索引复用）：；1. 只划分一次：使用random_split获取训练/验证索引；2. 保存索引：train_idx, val_idx供后续复用；3. 计算统计量：使用训练集索引计算mean/... |
| 卷积 | 用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 | 常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 | 不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。 | 输出尺寸：(输入尺寸 + 2P - K) / S + 1。 |
| Dropout | 训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 | 常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 | Dropout 不是提高模型容量，而是正则化。 | Dropout：训练随机失活，推理正常使用。 |
| 学习率 | 控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 | 常考学习率过大/过小的训练现象，以及学习率调度的目的。 | 学习率不是越大越好；也不是模型学到的参数。 | 学习率决定更新步长。 |
| Epoch | 训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。 | 常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。 | Epoch 不是一次参数更新；一次更新通常对应一个 batch。 | 1 个 Epoch = 全部训练样本被用过一遍。 |
| Adam | 结合动量和自适应学习率思想，为不同参数调整更新幅度。 | 常和 SGD、Momentum、RMSProp 对比优化器特点。 | Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。 | Adam = 一阶动量 + 二阶矩估计。 |
| 标准化 | 把数据按均值和方差调整到更稳定的尺度，帮助模型训练。 | 常考归一化和标准化的区别、为什么图像输入要预处理。 | 不要把标准化简单等同于除以 255；标准化通常涉及均值和标准差。 | 标准化常写作 (x-mean)/std。 |
| 池化 | 对局部区域做最大值或平均值汇总，降低空间尺寸并增强一定平移不变性。 | 常考池化作用、是否有可学习参数、与卷积的区别。 | 池化不是全连接，也不是用来增加参数量。 | 池化：降采样、压缩空间尺寸、保留显著信息。 |
| 优化器 | Step 4：优化器配置与学习率调度器；优化器 / 特点 / 适用场景；Adam / 自适应学习率，收敛快，对超参数不敏感 / 推荐，大多数情况首选；SGD + Momentum / 可能需要更细致的学习率调整，但泛化性能有时更好 / 追求最终精度，配合学习率调度；使用ReduceLROnPlate... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | Step 4：优化器配置与学习率调度器；优化器 / 特点 / 适用场景；Adam / 自适应学习率，收敛快，对超参数不敏感 / 推荐，大多数情况首选；SGD + Momentum / 可能需要更细致的学习率调整，但泛化性能有时更好 / 追求... |
| Momentum | Step 4：优化器配置与学习率调度器；优化器 / 特点 / 适用场景；Adam / 自适应学习率，收敛快，对超参数不敏感 / 推荐，大多数情况首选；SGD + Momentum / 可能需要更细致的学习率调整，但泛化性能有时更好 / 追求最终精度，配合学习率调度；使用ReduceLROnPlate... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | Step 4：优化器配置与学习率调度器；优化器 / 特点 / 适用场景；Adam / 自适应学习率，收敛快，对超参数不敏感 / 推荐，大多数情况首选；SGD + Momentum / 可能需要更细致的学习率调整，但泛化性能有时更好 / 追求... |
| 超参数 | Step 4：优化器配置与学习率调度器；优化器 / 特点 / 适用场景；Adam / 自适应学习率，收敛快，对超参数不敏感 / 推荐，大多数情况首选；SGD + Momentum / 可能需要更细致的学习率调整，但泛化性能有时更好 / 追求最终精度，配合学习率调度；使用ReduceLROnPlate... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | Step 4：优化器配置与学习率调度器；优化器 / 特点 / 适用场景；Adam / 自适应学习率，收敛快，对超参数不敏感 / 推荐，大多数情况首选；SGD + Momentum / 可能需要更细致的学习率调整，但泛化性能有时更好 / 追求... |

### 3. 像考试答案一样组织语言

- **ImageFolder**：ImageFolder简介；ImageFolder是PyTorch提供的通用数据集类，用于加载按文件夹组织的自定义图像数据集。；文件夹结构要求：；dataset_root/ ← 根目录；├── class_a/ ← 类别A的文件夹 Step 1：数据下载与ImageFolder加载；数据下载：使用自... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **数据增强**：Step 2：数据标准化与数据增强；数据划分（保存索引复用）：；1. 只划分一次：使用random_split获取训练/验证索引；2. 保存索引：train_idx, val_idx供后续复用；3. 计算统计量：使用训练集索引计算mean/std 🤔思考：为什么验证集的Loss和Accuracy在上... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **卷积**：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 考试写作时要补一句：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 易错点是：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- **Dropout**：训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 考试写作时要补一句：常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 易错点是：Dropout 不是提高模型容量，而是正则化。
- **学习率**：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 考试写作时要补一句：常考学习率过大/过小的训练现象，以及学习率调度的目的。 易错点是：学习率不是越大越好；也不是模型学到的参数。
- **Epoch**：训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。 考试写作时要补一句：常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。 易错点是：Epoch 不是一次参数更新；一次更新通常对应一个 batch。
- **Adam**：结合动量和自适应学习率思想，为不同参数调整更新幅度。 考试写作时要补一句：常和 SGD、Momentum、RMSProp 对比优化器特点。 易错点是：Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。
- **标准化**：把数据按均值和方差调整到更稳定的尺度，帮助模型训练。 考试写作时要补一句：常考归一化和标准化的区别、为什么图像输入要预处理。 易错点是：不要把标准化简单等同于除以 255；标准化通常涉及均值和标准差。

### 4. 本节自测

- 判断：ImageFolder只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：数据增强只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Dropout只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：学习率只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Epoch只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Adam只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：标准化只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- ImageFolder简介；ImageFolder是PyTorch提供的通用数据集类，用于加载按文件夹组织的自定义图像数据集。；文件夹结构要求：；dataset_root/ ← 根目录；├── class_a/ ← 类别A的文件夹 Step...
- Step 2：数据标准化与数据增强；数据划分（保存索引复用）：；1. 只划分一次：使用random_split获取训练/验证索引；2. 保存索引：train_idx, val_idx供后续复用；3. 计算统计量：使用训练集索引计算mean/...
- 输出尺寸：(输入尺寸 + 2P - K) / S + 1。
- Dropout：训练随机失活，推理正常使用。
- 学习率决定更新步长。
- 1 个 Epoch = 全部训练样本被用过一遍。
- Adam = 一阶动量 + 二阶矩估计。
- 标准化常写作 (x-mean)/std。
- 池化：降采样、压缩空间尺寸、保留显著信息。
- Step 4：优化器配置与学习率调度器；优化器 | 特点 | 适用场景；Adam | 自适应学习率，收敛快，对超参数不敏感 | 推荐，大多数情况首选；SGD + Momentum | 可能需要更细致的学习率调整，但泛化性能有时更好 | 追求...

## 第 3 部分：第六章 CNN高级主题与应用 / 第2节 现代卷积神经网络

### 1. 本节先看什么

这一节先把 **卷积、卷积核、Dropout、VGG** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 卷积 | 用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 | 常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 | 不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。 | 输出尺寸：(输入尺寸 + 2P - K) / S + 1。 |
| 卷积核 | 卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。 | 常考卷积核大小、通道数、参数共享和输出尺寸计算。 | 卷积核参数会训练更新，不是手工固定模板。 | 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。 |
| Dropout | 训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 | 常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 | Dropout 不是提高模型容量，而是正则化。 | Dropout：训练随机失活，推理正常使用。 |
| VGG | 大量使用 3x3 小卷积核堆叠构建深层网络。 | 常考小卷积核堆叠为什么能扩大感受野并减少参数。 | 不要把 VGG 的特点写成复杂多分支结构。 | VGG：多层小卷积核堆叠。 |
| LeNet | 网络 / 年份 / 层数 / 参数量 / ImageNet成绩 /作者/机构/特点；LeNet-5 / 1998 / 5层 / 60K / 早于ImageNet（始于2009） /Yann LeCun (AT&T Bell Labs)/开创性CNN架构；AlexNet / 2012 / 8层 / 6... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 网络 / 年份 / 层数 / 参数量 / ImageNet成绩 /作者/机构/特点；LeNet-5 / 1998 / 5层 / 60K / 早于ImageNet（始于2009） /Yann LeCun (AT&T Bell Labs)/开创... |
| AlexNet | 网络 / 年份 / 层数 / 参数量 / ImageNet成绩 /作者/机构/特点；LeNet-5 / 1998 / 5层 / 60K / 早于ImageNet（始于2009） /Yann LeCun (AT&T Bell Labs)/开创性CNN架构；AlexNet / 2012 / 8层 / 6... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 网络 / 年份 / 层数 / 参数量 / ImageNet成绩 /作者/机构/特点；LeNet-5 / 1998 / 5层 / 60K / 早于ImageNet（始于2009） /Yann LeCun (AT&T Bell Labs)/开创... |
| GoogLeNet | 网络 / 年份 / 层数 / 参数量 / ImageNet成绩 /作者/机构/特点；LeNet-5 / 1998 / 5层 / 60K / 早于ImageNet（始于2009） /Yann LeCun (AT&T Bell Labs)/开创性CNN架构；AlexNet / 2012 / 8层 / 6... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 网络 / 年份 / 层数 / 参数量 / ImageNet成绩 /作者/机构/特点；LeNet-5 / 1998 / 5层 / 60K / 早于ImageNet（始于2009） /Yann LeCun (AT&T Bell Labs)/开创... |
| Inception | 网络 / 年份 / 层数 / 参数量 / ImageNet成绩 /作者/机构/特点；LeNet-5 / 1998 / 5层 / 60K / 早于ImageNet（始于2009） /Yann LeCun (AT&T Bell Labs)/开创性CNN架构；AlexNet / 2012 / 8层 / 6... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 网络 / 年份 / 层数 / 参数量 / ImageNet成绩 /作者/机构/特点；LeNet-5 / 1998 / 5层 / 60K / 早于ImageNet（始于2009） /Yann LeCun (AT&T Bell Labs)/开创... |
| ResNet | 用残差/跳跃连接让网络学习 F(x)+x，缓解深层网络退化和梯度传播困难。 | 常考残差连接为什么能训练更深网络。 | 残差连接不是单纯增加层数，而是改变信息和梯度路径。 | ResNet 核心：y = F(x) + x。 |
| 残差连接 | 网络 / 年份 / 层数 / 参数量 / ImageNet成绩 /作者/机构/特点；LeNet-5 / 1998 / 5层 / 60K / 早于ImageNet（始于2009） /Yann LeCun (AT&T Bell Labs)/开创性CNN架构；AlexNet / 2012 / 8层 / 6... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 网络 / 年份 / 层数 / 参数量 / ImageNet成绩 /作者/机构/特点；LeNet-5 / 1998 / 5层 / 60K / 早于ImageNet（始于2009） /Yann LeCun (AT&T Bell Labs)/开创... |
| Softmax | 把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 | 常考多分类输出、注意力权重为什么能加权求和。 | Softmax 是对一组数整体归一化，不是逐个独立压缩。 | Softmax 输出非负且总和为 1。 |
| 迁移学习 | 二、VGG网络；VGG网络架构特点；VGG（Visual Geometry Group）由牛津大学于2014年提出，其核心思想是：使用小卷积核（3×3）的堆叠替代大卷积核。；核心创新：3×3卷积的堆叠；VGG-16详细架构 | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 二、VGG网络；VGG网络架构特点；VGG（Visual Geometry Group）由牛津大学于2014年提出，其核心思想是：使用小卷积核（3×3）的堆叠替代大卷积核。；核心创新：3×3卷积的堆叠；VGG-16详细架构 |

### 3. 像考试答案一样组织语言

- **卷积**：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 考试写作时要补一句：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 易错点是：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- **卷积核**：卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。 考试写作时要补一句：常考卷积核大小、通道数、参数共享和输出尺寸计算。 易错点是：卷积核参数会训练更新，不是手工固定模板。
- **Dropout**：训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 考试写作时要补一句：常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 易错点是：Dropout 不是提高模型容量，而是正则化。
- **VGG**：大量使用 3x3 小卷积核堆叠构建深层网络。 考试写作时要补一句：常考小卷积核堆叠为什么能扩大感受野并减少参数。 易错点是：不要把 VGG 的特点写成复杂多分支结构。
- **LeNet**：网络 | 年份 | 层数 | 参数量 | ImageNet成绩 |作者/机构|特点；LeNet-5 | 1998 | 5层 | 60K | 早于ImageNet（始于2009） |Yann LeCun (AT&T Bell Labs)|开创性CNN架构；AlexNet | 2012 | 8层 | 6... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **AlexNet**：网络 | 年份 | 层数 | 参数量 | ImageNet成绩 |作者/机构|特点；LeNet-5 | 1998 | 5层 | 60K | 早于ImageNet（始于2009） |Yann LeCun (AT&T Bell Labs)|开创性CNN架构；AlexNet | 2012 | 8层 | 6... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **GoogLeNet**：网络 | 年份 | 层数 | 参数量 | ImageNet成绩 |作者/机构|特点；LeNet-5 | 1998 | 5层 | 60K | 早于ImageNet（始于2009） |Yann LeCun (AT&T Bell Labs)|开创性CNN架构；AlexNet | 2012 | 8层 | 6... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **Inception**：网络 | 年份 | 层数 | 参数量 | ImageNet成绩 |作者/机构|特点；LeNet-5 | 1998 | 5层 | 60K | 早于ImageNet（始于2009） |Yann LeCun (AT&T Bell Labs)|开创性CNN架构；AlexNet | 2012 | 8层 | 6... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。

### 4. 本节自测

- 判断：卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：卷积核只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Dropout只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：VGG只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：LeNet只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：AlexNet只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：GoogLeNet只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Inception只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 输出尺寸：(输入尺寸 + 2P - K) / S + 1。
- 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。
- Dropout：训练随机失活，推理正常使用。
- VGG：多层小卷积核堆叠。
- 网络 | 年份 | 层数 | 参数量 | ImageNet成绩 |作者/机构|特点；LeNet-5 | 1998 | 5层 | 60K | 早于ImageNet（始于2009） |Yann LeCun (AT&T Bell Labs)|开创...
- 网络 | 年份 | 层数 | 参数量 | ImageNet成绩 |作者/机构|特点；LeNet-5 | 1998 | 5层 | 60K | 早于ImageNet（始于2009） |Yann LeCun (AT&T Bell Labs)|开创...
- 网络 | 年份 | 层数 | 参数量 | ImageNet成绩 |作者/机构|特点；LeNet-5 | 1998 | 5层 | 60K | 早于ImageNet（始于2009） |Yann LeCun (AT&T Bell Labs)|开创...
- 网络 | 年份 | 层数 | 参数量 | ImageNet成绩 |作者/机构|特点；LeNet-5 | 1998 | 5层 | 60K | 早于ImageNet（始于2009） |Yann LeCun (AT&T Bell Labs)|开创...
- ResNet 核心：y = F(x) + x。
- 网络 | 年份 | 层数 | 参数量 | ImageNet成绩 |作者/机构|特点；LeNet-5 | 1998 | 5层 | 60K | 早于ImageNet（始于2009） |Yann LeCun (AT&T Bell Labs)|开创...

## 第 4 部分：第六章 CNN高级主题与应用 / 第3节 迁移学习（Transfer Learning）

### 1. 本节先看什么

这一节先把 **迁移学习、AlexNet、ResNet、VGG** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 迁移学习 | 第3节 迁移学习（Transfer Learning） 什么是迁移学习？；迁移学习（Transfer Learning）是一种利用已有知识解决新问题的方法：“站在巨人的肩膀上“；1. 一些大型科技公司使用海量数据和算力训练出强大的模型；2. 训练完成后，发布模型的架构和预训练权重；3. 我们可以将这... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第3节 迁移学习（Transfer Learning） 什么是迁移学习？；迁移学习（Transfer Learning）是一种利用已有知识解决新问题的方法：“站在巨人的肩膀上“；1. 一些大型科技公司使用海量数据和算力训练出强大的模型；2.... |
| AlexNet | ImageNet与预训练模型；ImageNet是一个大型图像数据库，包含超过1400万张图像，涵盖2万多个类别。；ImageNet Large Scale Visual Recognition Challenge (ILSVRC)：；2010-2017年举办的图像分类竞赛；催生了AlexNet、VG... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | ImageNet与预训练模型；ImageNet是一个大型图像数据库，包含超过1400万张图像，涵盖2万多个类别。；ImageNet Large Scale Visual Recognition Challenge (ILSVRC)：；201... |
| ResNet | 用残差/跳跃连接让网络学习 F(x)+x，缓解深层网络退化和梯度传播困难。 | 常考残差连接为什么能训练更深网络。 | 残差连接不是单纯增加层数，而是改变信息和梯度路径。 | ResNet 核心：y = F(x) + x。 |
| VGG | 大量使用 3x3 小卷积核堆叠构建深层网络。 | 常考小卷积核堆叠为什么能扩大感受野并减少参数。 | 不要把 VGG 的特点写成复杂多分支结构。 | VGG：多层小卷积核堆叠。 |
| 卷积 | 用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 | 常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 | 不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。 | 输出尺寸：(输入尺寸 + 2P - K) / S + 1。 |
| Inception | ImageNet与预训练模型；ImageNet是一个大型图像数据库，包含超过1400万张图像，涵盖2万多个类别。；ImageNet Large Scale Visual Recognition Challenge (ILSVRC)：；2010-2017年举办的图像分类竞赛；催生了AlexNet、VG... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | ImageNet与预训练模型；ImageNet是一个大型图像数据库，包含超过1400万张图像，涵盖2万多个类别。；ImageNet Large Scale Visual Recognition Challenge (ILSVRC)：；201... |
| 标准化 | 把数据按均值和方差调整到更稳定的尺度，帮助模型训练。 | 常考归一化和标准化的区别、为什么图像输入要预处理。 | 不要把标准化简单等同于除以 255；标准化通常涉及均值和标准差。 | 标准化常写作 (x-mean)/std。 |
| Epoch | 训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。 | 常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。 | Epoch 不是一次参数更新；一次更新通常对应一个 batch。 | 1 个 Epoch = 全部训练样本被用过一遍。 |
| 学习率 | 控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 | 常考学习率过大/过小的训练现象，以及学习率调度的目的。 | 学习率不是越大越好；也不是模型学到的参数。 | 学习率决定更新步长。 |
| 卷积核 | 卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。 | 常考卷积核大小、通道数、参数共享和输出尺寸计算。 | 卷积核参数会训练更新，不是手工固定模板。 | 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。 |
| Dropout | 训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 | 常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 | Dropout 不是提高模型容量，而是正则化。 | Dropout：训练随机失活，推理正常使用。 |
| 残差连接 | ImageNet与预训练模型；ImageNet是一个大型图像数据库，包含超过1400万张图像，涵盖2万多个类别。；ImageNet Large Scale Visual Recognition Challenge (ILSVRC)：；2010-2017年举办的图像分类竞赛；催生了AlexNet、VG... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | ImageNet与预训练模型；ImageNet是一个大型图像数据库，包含超过1400万张图像，涵盖2万多个类别。；ImageNet Large Scale Visual Recognition Challenge (ILSVRC)：；201... |
| Momentum | 迁移学习要根据数据集大小和与ImageNet的相似度来选择策略：；场景 / 数据集大小 / 与ImageNet相似度 / 推荐策略；场景1 / 小（ 10万） / 高 / 用较小学习率微调所有层；场景4 / 大（> 10万） / 低 / 从预训练权重开始微调所有层（完全从头训练一般没必要）；如何做到... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 迁移学习要根据数据集大小和与ImageNet的相似度来选择策略：；场景 / 数据集大小 / 与ImageNet相似度 / 推荐策略；场景1 / 小（ 10万） / 高 / 用较小学习率微调所有层；场景4 / 大（> 10万） / 低 / 从... |
| Adam | 结合动量和自适应学习率思想，为不同参数调整更新幅度。 | 常和 SGD、Momentum、RMSProp 对比优化器特点。 | Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。 | Adam = 一阶动量 + 二阶矩估计。 |

### 3. 像考试答案一样组织语言

- **迁移学习**：第3节 迁移学习（Transfer Learning） 什么是迁移学习？；迁移学习（Transfer Learning）是一种利用已有知识解决新问题的方法：“站在巨人的肩膀上“；1. 一些大型科技公司使用海量数据和算力训练出强大的模型；2. 训练完成后，发布模型的架构和预训练权重；3. 我们可以将这... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **AlexNet**：ImageNet与预训练模型；ImageNet是一个大型图像数据库，包含超过1400万张图像，涵盖2万多个类别。；ImageNet Large Scale Visual Recognition Challenge (ILSVRC)：；2010-2017年举办的图像分类竞赛；催生了AlexNet、VG... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **ResNet**：用残差/跳跃连接让网络学习 F(x)+x，缓解深层网络退化和梯度传播困难。 考试写作时要补一句：常考残差连接为什么能训练更深网络。 易错点是：残差连接不是单纯增加层数，而是改变信息和梯度路径。
- **VGG**：大量使用 3x3 小卷积核堆叠构建深层网络。 考试写作时要补一句：常考小卷积核堆叠为什么能扩大感受野并减少参数。 易错点是：不要把 VGG 的特点写成复杂多分支结构。
- **卷积**：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 考试写作时要补一句：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 易错点是：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- **Inception**：ImageNet与预训练模型；ImageNet是一个大型图像数据库，包含超过1400万张图像，涵盖2万多个类别。；ImageNet Large Scale Visual Recognition Challenge (ILSVRC)：；2010-2017年举办的图像分类竞赛；催生了AlexNet、VG... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **标准化**：把数据按均值和方差调整到更稳定的尺度，帮助模型训练。 考试写作时要补一句：常考归一化和标准化的区别、为什么图像输入要预处理。 易错点是：不要把标准化简单等同于除以 255；标准化通常涉及均值和标准差。
- **Epoch**：训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。 考试写作时要补一句：常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。 易错点是：Epoch 不是一次参数更新；一次更新通常对应一个 batch。

### 4. 本节自测

- 判断：迁移学习只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：AlexNet只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：ResNet只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：VGG只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Inception只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：标准化只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Epoch只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 第3节 迁移学习（Transfer Learning） 什么是迁移学习？；迁移学习（Transfer Learning）是一种利用已有知识解决新问题的方法：“站在巨人的肩膀上“；1. 一些大型科技公司使用海量数据和算力训练出强大的模型；2....
- ImageNet与预训练模型；ImageNet是一个大型图像数据库，包含超过1400万张图像，涵盖2万多个类别。；ImageNet Large Scale Visual Recognition Challenge (ILSVRC)：；201...
- ResNet 核心：y = F(x) + x。
- VGG：多层小卷积核堆叠。
- 输出尺寸：(输入尺寸 + 2P - K) / S + 1。
- ImageNet与预训练模型；ImageNet是一个大型图像数据库，包含超过1400万张图像，涵盖2万多个类别。；ImageNet Large Scale Visual Recognition Challenge (ILSVRC)：；201...
- 标准化常写作 (x-mean)/std。
- 1 个 Epoch = 全部训练样本被用过一遍。
- 学习率决定更新步长。
- 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。

## 第 5 部分：第六章 CNN高级主题与应用 / 第3节：迁移学习

### 1. 本节先看什么

这一节先把 **学习率、卷积、卷积核、标准化** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 学习率 | 控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 | 常考学习率过大/过小的训练现象，以及学习率调度的目的。 | 学习率不是越大越好；也不是模型学到的参数。 | 学习率决定更新步长。 |
| 卷积 | 用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 | 常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 | 不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。 | 输出尺寸：(输入尺寸 + 2P - K) / S + 1。 |
| 卷积核 | 卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。 | 常考卷积核大小、通道数、参数共享和输出尺寸计算。 | 卷积核参数会训练更新，不是手工固定模板。 | 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。 |
| 标准化 | 把数据按均值和方差调整到更稳定的尺度，帮助模型训练。 | 常考归一化和标准化的区别、为什么图像输入要预处理。 | 不要把标准化简单等同于除以 255；标准化通常涉及均值和标准差。 | 标准化常写作 (x-mean)/std。 |
| LeNet | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维 → 极简全连接层"的结构，在保证表达能力的同时大幅减少参数... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维... |
| ImageFolder | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维 → 极简全连接层"的结构，在保证表达能力的同时大幅减少参数... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维... |
| 数据增强 | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维 → 极简全连接层"的结构，在保证表达能力的同时大幅减少参数... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维... |
| 优化器 | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维 → 极简全连接层"的结构，在保证表达能力的同时大幅减少参数... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维... |
| Adam | 结合动量和自适应学习率思想，为不同参数调整更新幅度。 | 常和 SGD、Momentum、RMSProp 对比优化器特点。 | Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。 | Adam = 一阶动量 + 二阶矩估计。 |
| VGG | 大量使用 3x3 小卷积核堆叠构建深层网络。 | 常考小卷积核堆叠为什么能扩大感受野并减少参数。 | 不要把 VGG 的特点写成复杂多分支结构。 | VGG：多层小卷积核堆叠。 |
| GoogLeNet | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维 → 极简全连接层"的结构，在保证表达能力的同时大幅减少参数... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维... |
| Inception | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维 → 极简全连接层"的结构，在保证表达能力的同时大幅减少参数... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维... |
| ResNet | 用残差/跳跃连接让网络学习 F(x)+x，缓解深层网络退化和梯度传播困难。 | 常考残差连接为什么能训练更深网络。 | 残差连接不是单纯增加层数，而是改变信息和梯度路径。 | ResNet 核心：y = F(x) + x。 |
| 残差连接 | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维 → 极简全连接层"的结构，在保证表达能力的同时大幅减少参数... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维... |
| 迁移学习 | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维 → 极简全连接层"的结构，在保证表达能力的同时大幅减少参数... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维... |

### 3. 像考试答案一样组织语言

- **学习率**：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 考试写作时要补一句：常考学习率过大/过小的训练现象，以及学习率调度的目的。 易错点是：学习率不是越大越好；也不是模型学到的参数。
- **卷积**：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 考试写作时要补一句：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 易错点是：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- **卷积核**：卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。 考试写作时要补一句：常考卷积核大小、通道数、参数共享和输出尺寸计算。 易错点是：卷积核参数会训练更新，不是手工固定模板。
- **标准化**：把数据按均值和方差调整到更稳定的尺度，帮助模型训练。 考试写作时要补一句：常考归一化和标准化的区别、为什么图像输入要预处理。 易错点是：不要把标准化简单等同于除以 255；标准化通常涉及均值和标准差。
- **LeNet**：第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维 → 极简全连接层"的结构，在保证表达能力的同时大幅减少参数... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **ImageFolder**：第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维 → 极简全连接层"的结构，在保证表达能力的同时大幅减少参数... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **数据增强**：第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维 → 极简全连接层"的结构，在保证表达能力的同时大幅减少参数... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **优化器**：第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维 → 极简全连接层"的结构，在保证表达能力的同时大幅减少参数... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。

### 4. 本节自测

- 判断：学习率只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：卷积核只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：标准化只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：LeNet只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：ImageFolder只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：数据增强只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：优化器只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 学习率决定更新步长。
- 输出尺寸：(输入尺寸 + 2P - K) / S + 1。
- 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。
- 标准化常写作 (x-mean)/std。
- 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维...
- 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维...
- 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维...
- 第1节：CNN神经网络训练完整流程；使用ImageFolder加载按文件夹组织的图像数据，通过数据增强（翻转、旋转等）提升训练集多样性，验证/测试集仅做标准化。；网络采用"卷积块提取特征 → Global Average Pooling降维...
- Adam = 一阶动量 + 二阶矩估计。
- VGG：多层小卷积核堆叠。

## 四、考前总复盘

考前不要平均用力。优先检查下面这些问题：

1. 每个核心概念能不能用一句话说明“它是什么”。
2. 能不能说出它在模型结构、训练流程或数据处理中的位置。
3. 能不能说出一个最常见的错误说法。
4. 遇到选择题时，能不能判断选项是在混淆概念、夸大作用，还是写反了训练/推理阶段。

## 五、打印建议

<style>@media print { @page { margin: 8mm; } body { font-size: 11pt; line-height: 1.35; } h1, h2, h3 { page-break-after: avoid; } table { font-size: 9pt; border-collapse: collapse; } th, td { padding: 3px 5px; border: 1px solid #ddd; vertical-align: top; } }</style>
