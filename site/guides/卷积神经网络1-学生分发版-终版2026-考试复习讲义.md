# 卷积神经网络1-学生分发版-终版2026：期末考试复习讲义

> 使用方式：先通读“学习路线”，再按章节背“必记句子”，最后用每章自测题检查。这里不是课件原文搬运，而是按考试复习顺序重写。

## 一、这份课件先解决什么问题

这一类课件主要解决图像数据如何进入神经网络的问题。复习时先抓住“空间结构不能被随便展平”这个动机，再理解卷积、池化、感受野、典型 CNN 架构和训练配置。

考试通常不会只问名词，而是把输出尺寸、参数共享、局部连接、训练技巧和损失函数匹配放在一起判断。

## 二、学习路线

1. **第四章 卷积神经网络基础与入门实践**：先抓 卷积、感受野、池化、卷积核、损失函数。
2. **第四章 卷积神经网络基础与入门实践 / 第1节 卷积神经网络简介**：先抓 卷积、卷积核、池化、Same、互相关。
3. **第四章 卷积神经网络基础与入门实践 / 第2节 图像表示与通道概念**：先抓 卷积、标准化、卷积核、激活函数、归一化。
4. **第四章 卷积神经网络基础与入门实践 / 第3节 典型的卷积神经网络架构**：先抓 卷积、池化、卷积核、Dropout、激活函数。
5. **第四章 卷积神经网络基础与入门实践 / 第4节 网络配置与损失函数应匹配**：先抓 交叉熵、损失函数、BCEWithLogitsLoss、CrossEntropyLoss、类别不平衡。
6. **第四章 卷积神经网络基础与入门实践 / 第5节 PyTorch实现CNN图像分类（完整流程）**：先抓 WeightedRandomSampler、类别不平衡、归一化、标准化、学习率。

## 三、章节详解

## 第 1 部分：第四章 卷积神经网络基础与入门实践

### 1. 本节先看什么

这一节先把 **卷积、感受野、池化、卷积核** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 卷积 | 用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 | 常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 | 不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。 | 输出尺寸：(输入尺寸 + 2P - K) / S + 1。 |
| 感受野 | 某一层一个神经元能看到原始输入图像的区域大小。 | 常考层数、卷积核大小、步长如何影响感受野。 | 步长变大通常会让前一层对应感受野扩大，不是缩小。 | 层越深，感受野通常越大；多层小卷积能逐步扩大感受野。 |
| 池化 | 对局部区域做最大值或平均值汇总，降低空间尺寸并增强一定平移不变性。 | 常考池化作用、是否有可学习参数、与卷积的区别。 | 池化不是全连接，也不是用来增加参数量。 | 池化：降采样、压缩空间尺寸、保留显著信息。 |
| 卷积核 | 卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。 | 常考卷积核大小、通道数、参数共享和输出尺寸计算。 | 卷积核参数会训练更新，不是手工固定模板。 | 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。 |
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |
| 互相关 | 形式接近卷积，但不翻转卷积核；深度学习框架里的卷积层通常实际做互相关。 | 常考 CNN 中是否需要翻转卷积核，或为什么工程上用互相关。 | 不要因为名字叫卷积层，就默认一定执行数学卷积的核翻转。 | CNN 训练会学习卷积核参数，因此互相关也能学到有效特征。 |
| Valid | 不填充边界，只在卷积核完全落入输入时计算，输出尺寸会变小。 | 常和 Same、Full 对比考输出大小。 | Valid 不是大量填充，恰好是不填充。 | Valid：无填充，输出通常最小。 |
| Same | 通过适当填充，让卷积输出空间尺寸尽量和输入保持一致。 | 常考给定输入、卷积核、步长时需要多少 padding。 | Same 的目标是保持尺寸，不是扩大特征图。 | 步长为 1 时，Same 卷积常让 H、W 不变。 |
| Full | 在边界外进行更充分的填充，使卷积核可以覆盖到输入边缘外侧，输出会变大。 | 常和 Valid、Same 放在一起判断哪种输出最大。 | Full 不是常规 CNN 默认选择，考试多用于概念区分。 | Full：充分填充，输出通常最大。 |
| NCHW | 第四章 卷积神经网络基础与入门实践；1. 卷积神经网络简介；从生物视觉到CNN；卷积运算与互相关运算；卷积的三种形式（Valid、Same、Full） | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第四章 卷积神经网络基础与入门实践；1. 卷积神经网络简介；从生物视觉到CNN；卷积运算与互相关运算；卷积的三种形式（Valid、Same、Full） |
| NHWC | 第四章 卷积神经网络基础与入门实践；1. 卷积神经网络简介；从生物视觉到CNN；卷积运算与互相关运算；卷积的三种形式（Valid、Same、Full） | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第四章 卷积神经网络基础与入门实践；1. 卷积神经网络简介；从生物视觉到CNN；卷积运算与互相关运算；卷积的三种形式（Valid、Same、Full） |
| 归一化 | 把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 | 常考图像预处理为什么要缩放像素值。 | 归一化不等于 BatchNorm，前者多是输入预处理。 | 图像常先把像素值缩放到 0-1。 |
| LeNet | 第四章 卷积神经网络基础与入门实践；1. 卷积神经网络简介；从生物视觉到CNN；卷积运算与互相关运算；卷积的三种形式（Valid、Same、Full） | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第四章 卷积神经网络基础与入门实践；1. 卷积神经网络简介；从生物视觉到CNN；卷积运算与互相关运算；卷积的三种形式（Valid、Same、Full） |
| AlexNet | 第四章 卷积神经网络基础与入门实践；1. 卷积神经网络简介；从生物视觉到CNN；卷积运算与互相关运算；卷积的三种形式（Valid、Same、Full） | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第四章 卷积神经网络基础与入门实践；1. 卷积神经网络简介；从生物视觉到CNN；卷积运算与互相关运算；卷积的三种形式（Valid、Same、Full） |
| WeightedRandomSampler | 第四章 卷积神经网络基础与入门实践；1. 卷积神经网络简介；从生物视觉到CNN；卷积运算与互相关运算；卷积的三种形式（Valid、Same、Full） | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第四章 卷积神经网络基础与入门实践；1. 卷积神经网络简介；从生物视觉到CNN；卷积运算与互相关运算；卷积的三种形式（Valid、Same、Full） |
| 类别不平衡 | 第四章 卷积神经网络基础与入门实践；1. 卷积神经网络简介；从生物视觉到CNN；卷积运算与互相关运算；卷积的三种形式（Valid、Same、Full） | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第四章 卷积神经网络基础与入门实践；1. 卷积神经网络简介；从生物视觉到CNN；卷积运算与互相关运算；卷积的三种形式（Valid、Same、Full） |

### 3. 像考试答案一样组织语言

- **卷积**：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 考试写作时要补一句：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 易错点是：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- **感受野**：某一层一个神经元能看到原始输入图像的区域大小。 考试写作时要补一句：常考层数、卷积核大小、步长如何影响感受野。 易错点是：步长变大通常会让前一层对应感受野扩大，不是缩小。
- **池化**：对局部区域做最大值或平均值汇总，降低空间尺寸并增强一定平移不变性。 考试写作时要补一句：常考池化作用、是否有可学习参数、与卷积的区别。 易错点是：池化不是全连接，也不是用来增加参数量。
- **卷积核**：卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。 考试写作时要补一句：常考卷积核大小、通道数、参数共享和输出尺寸计算。 易错点是：卷积核参数会训练更新，不是手工固定模板。
- **损失函数**：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 考试写作时要补一句：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 易错点是：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- **互相关**：形式接近卷积，但不翻转卷积核；深度学习框架里的卷积层通常实际做互相关。 考试写作时要补一句：常考 CNN 中是否需要翻转卷积核，或为什么工程上用互相关。 易错点是：不要因为名字叫卷积层，就默认一定执行数学卷积的核翻转。
- **Valid**：不填充边界，只在卷积核完全落入输入时计算，输出尺寸会变小。 考试写作时要补一句：常和 Same、Full 对比考输出大小。 易错点是：Valid 不是大量填充，恰好是不填充。
- **Same**：通过适当填充，让卷积输出空间尺寸尽量和输入保持一致。 考试写作时要补一句：常考给定输入、卷积核、步长时需要多少 padding。 易错点是：Same 的目标是保持尺寸，不是扩大特征图。

### 4. 本节自测

- 判断：卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：感受野只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：池化只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：卷积核只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：损失函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：互相关只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Valid只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Same只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 输出尺寸：(输入尺寸 + 2P - K) / S + 1。
- 层越深，感受野通常越大；多层小卷积能逐步扩大感受野。
- 池化：降采样、压缩空间尺寸、保留显著信息。
- 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。
- 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
- CNN 训练会学习卷积核参数，因此互相关也能学到有效特征。
- Valid：无填充，输出通常最小。
- 步长为 1 时，Same 卷积常让 H、W 不变。
- Full：充分填充，输出通常最大。
- 第四章 卷积神经网络基础与入门实践；1. 卷积神经网络简介；从生物视觉到CNN；卷积运算与互相关运算；卷积的三种形式（Valid、Same、Full）

## 第 2 部分：第四章 卷积神经网络基础与入门实践 / 第1节 卷积神经网络简介

### 1. 本节先看什么

这一节先把 **卷积、卷积核、池化、Same** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 卷积 | 用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 | 常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 | 不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。 | 输出尺寸：(输入尺寸 + 2P - K) / S + 1。 |
| 卷积核 | 卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。 | 常考卷积核大小、通道数、参数共享和输出尺寸计算。 | 卷积核参数会训练更新，不是手工固定模板。 | 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。 |
| 池化 | 对局部区域做最大值或平均值汇总，降低空间尺寸并增强一定平移不变性。 | 常考池化作用、是否有可学习参数、与卷积的区别。 | 池化不是全连接，也不是用来增加参数量。 | 池化：降采样、压缩空间尺寸、保留显著信息。 |
| Same | 通过适当填充，让卷积输出空间尺寸尽量和输入保持一致。 | 常考给定输入、卷积核、步长时需要多少 padding。 | Same 的目标是保持尺寸，不是扩大特征图。 | 步长为 1 时，Same 卷积常让 H、W 不变。 |
| 互相关 | 形式接近卷积，但不翻转卷积核；深度学习框架里的卷积层通常实际做互相关。 | 常考 CNN 中是否需要翻转卷积核，或为什么工程上用互相关。 | 不要因为名字叫卷积层，就默认一定执行数学卷积的核翻转。 | CNN 训练会学习卷积核参数，因此互相关也能学到有效特征。 |
| 步长 | 卷积核或池化窗口每次移动的间隔。 | 常考步长变大时输出尺寸如何变化。 | 步长越大，输出空间尺寸通常越小。 | Stride 控制滑动间隔。 |
| 填充 | 在输入边界补值，让卷积能处理边缘或保持输出尺寸。 | 常考 Same 卷积为何需要 padding。 | 填充不是增加有效信息，而是控制边界和尺寸。 | Padding 用于控制输出尺寸和边界信息。 |
| Valid | 不填充边界，只在卷积核完全落入输入时计算，输出尺寸会变小。 | 常和 Same、Full 对比考输出大小。 | Valid 不是大量填充，恰好是不填充。 | Valid：无填充，输出通常最小。 |
| Full | 在边界外进行更充分的填充，使卷积核可以覆盖到输入边缘外侧，输出会变大。 | 常和 Valid、Same 放在一起判断哪种输出最大。 | Full 不是常规 CNN 默认选择，考试多用于概念区分。 | Full：充分填充，输出通常最大。 |
| Batch | 一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 | 常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 | Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。 | Batch 是一批样本；Epoch 是全训练集完整训练一遍。 |
| 归一化 | 把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 | 常考图像预处理为什么要缩放像素值。 | 归一化不等于 BatchNorm，前者多是输入预处理。 | 图像常先把像素值缩放到 0-1。 |
| 反向传播 | 卷积核：不是人工设计的，是学习得到的；CNN的革命性：；最后的卷积核，是机器自己学习得到的；CNN中的卷积核初始是随机的，通过反向传播自动学习最优的权重。随着网络深度的增加，特征逐渐从底层向高层抽象；注意：这种层次性是连续且渐进的，并非每一层都有清晰可分的语义边界，不同任务、不同网络中的特征分布也会... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 卷积核：不是人工设计的，是学习得到的；CNN的革命性：；最后的卷积核，是机器自己学习得到的；CNN中的卷积核初始是随机的，通过反向传播自动学习最优的权重。随着网络深度的增加，特征逐渐从底层向高层抽象；注意：这种层次性是连续且渐进的，并非每一... |
| 感受野 | 某一层一个神经元能看到原始输入图像的区域大小。 | 常考层数、卷积核大小、步长如何影响感受野。 | 步长变大通常会让前一层对应感受野扩大，不是缩小。 | 层越深，感受野通常越大；多层小卷积能逐步扩大感受野。 |

### 3. 像考试答案一样组织语言

- **卷积**：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 考试写作时要补一句：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 易错点是：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- **卷积核**：卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。 考试写作时要补一句：常考卷积核大小、通道数、参数共享和输出尺寸计算。 易错点是：卷积核参数会训练更新，不是手工固定模板。
- **池化**：对局部区域做最大值或平均值汇总，降低空间尺寸并增强一定平移不变性。 考试写作时要补一句：常考池化作用、是否有可学习参数、与卷积的区别。 易错点是：池化不是全连接，也不是用来增加参数量。
- **Same**：通过适当填充，让卷积输出空间尺寸尽量和输入保持一致。 考试写作时要补一句：常考给定输入、卷积核、步长时需要多少 padding。 易错点是：Same 的目标是保持尺寸，不是扩大特征图。
- **互相关**：形式接近卷积，但不翻转卷积核；深度学习框架里的卷积层通常实际做互相关。 考试写作时要补一句：常考 CNN 中是否需要翻转卷积核，或为什么工程上用互相关。 易错点是：不要因为名字叫卷积层，就默认一定执行数学卷积的核翻转。
- **步长**：卷积核或池化窗口每次移动的间隔。 考试写作时要补一句：常考步长变大时输出尺寸如何变化。 易错点是：步长越大，输出空间尺寸通常越小。
- **填充**：在输入边界补值，让卷积能处理边缘或保持输出尺寸。 考试写作时要补一句：常考 Same 卷积为何需要 padding。 易错点是：填充不是增加有效信息，而是控制边界和尺寸。
- **Valid**：不填充边界，只在卷积核完全落入输入时计算，输出尺寸会变小。 考试写作时要补一句：常和 Same、Full 对比考输出大小。 易错点是：Valid 不是大量填充，恰好是不填充。

### 4. 本节自测

- 判断：卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：卷积核只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：池化只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Same只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：互相关只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：步长只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：填充只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Valid只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 输出尺寸：(输入尺寸 + 2P - K) / S + 1。
- 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。
- 池化：降采样、压缩空间尺寸、保留显著信息。
- 步长为 1 时，Same 卷积常让 H、W 不变。
- CNN 训练会学习卷积核参数，因此互相关也能学到有效特征。
- Stride 控制滑动间隔。
- Padding 用于控制输出尺寸和边界信息。
- Valid：无填充，输出通常最小。
- Full：充分填充，输出通常最大。
- Batch 是一批样本；Epoch 是全训练集完整训练一遍。

## 第 3 部分：第四章 卷积神经网络基础与入门实践 / 第2节 图像表示与通道概念

### 1. 本节先看什么

这一节先把 **卷积、标准化、卷积核、激活函数** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 卷积 | 用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 | 常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 | 不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。 | 输出尺寸：(输入尺寸 + 2P - K) / S + 1。 |
| 标准化 | 把数据按均值和方差调整到更稳定的尺度，帮助模型训练。 | 常考归一化和标准化的区别、为什么图像输入要预处理。 | 不要把标准化简单等同于除以 255；标准化通常涉及均值和标准差。 | 标准化常写作 (x-mean)/std。 |
| 卷积核 | 卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。 | 常考卷积核大小、通道数、参数共享和输出尺寸计算。 | 卷积核参数会训练更新，不是手工固定模板。 | 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。 |
| 激活函数 | 给线性变换加入非线性，使网络能拟合非线性关系。 | 常考没有激活函数时多层线性网络仍等价于线性模型。 | 不要把激活函数说成只改变维度，它主要提供非线性。 | 仿射变换 + 激活函数 = 神经网络层的基本模式。 |
| 归一化 | 把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 | 常考图像预处理为什么要缩放像素值。 | 归一化不等于 BatchNorm，前者多是输入预处理。 | 图像常先把像素值缩放到 0-1。 |
| Batch | 一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 | 常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 | Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。 | Batch 是一批样本；Epoch 是全训练集完整训练一遍。 |
| 梯度下降 | 数据预处理：从归一化到标准化；为什么需要预处理？；机器学习：归一化是在涉及距离计算的时候，做处理就会把他映射到[0, 1]之间。；图像像素值范围是 [0, 255]，直接输入神经网络会带来问题：；梯度下降效率低：大数值导致梯度更新不稳定 第二层：进阶处理 — Z-Score标准化；📌 深度学习中更常... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 数据预处理：从归一化到标准化；为什么需要预处理？；机器学习：归一化是在涉及距离计算的时候，做处理就会把他映射到[0, 1]之间。；图像像素值范围是 [0, 255]，直接输入神经网络会带来问题：；梯度下降效率低：大数值导致梯度更新不稳定 第... |
| NCHW | 数据格式：NCHW vs NHWC；不同框架使用不同的图像数据格式：；格式 / 维度顺序 / 使用框架；NCHW / (Batch（批量大小）, Channel（3）, Height, Width) / PyTorch；NHWC / (Batch, Height, Width, Channel) /... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 数据格式：NCHW vs NHWC；不同框架使用不同的图像数据格式：；格式 / 维度顺序 / 使用框架；NCHW / (Batch（批量大小）, Channel（3）, Height, Width) / PyTorch；NHWC / (Ba... |
| NHWC | 数据格式：NCHW vs NHWC；不同框架使用不同的图像数据格式：；格式 / 维度顺序 / 使用框架；NCHW / (Batch（批量大小）, Channel（3）, Height, Width) / PyTorch；NHWC / (Batch, Height, Width, Channel) /... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 数据格式：NCHW vs NHWC；不同框架使用不同的图像数据格式：；格式 / 维度顺序 / 使用框架；NCHW / (Batch（批量大小）, Channel（3）, Height, Width) / PyTorch；NHWC / (Ba... |
| 数据增强 | 数据格式：NCHW vs NHWC；不同框架使用不同的图像数据格式：；格式 / 维度顺序 / 使用框架；NCHW / (Batch（批量大小）, Channel（3）, Height, Width) / PyTorch；NHWC / (Batch, Height, Width, Channel) /... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 数据格式：NCHW vs NHWC；不同框架使用不同的图像数据格式：；格式 / 维度顺序 / 使用框架；NCHW / (Batch（批量大小）, Channel（3）, Height, Width) / PyTorch；NHWC / (Ba... |
| 填充 | 在输入边界补值，让卷积能处理边缘或保持输出尺寸。 | 常考 Same 卷积为何需要 padding。 | 填充不是增加有效信息，而是控制边界和尺寸。 | Padding 用于控制输出尺寸和边界信息。 |
| Sigmoid | 把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 | 常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 | Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。 | Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。 |

### 3. 像考试答案一样组织语言

- **卷积**：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 考试写作时要补一句：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 易错点是：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- **标准化**：把数据按均值和方差调整到更稳定的尺度，帮助模型训练。 考试写作时要补一句：常考归一化和标准化的区别、为什么图像输入要预处理。 易错点是：不要把标准化简单等同于除以 255；标准化通常涉及均值和标准差。
- **卷积核**：卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。 考试写作时要补一句：常考卷积核大小、通道数、参数共享和输出尺寸计算。 易错点是：卷积核参数会训练更新，不是手工固定模板。
- **激活函数**：给线性变换加入非线性，使网络能拟合非线性关系。 考试写作时要补一句：常考没有激活函数时多层线性网络仍等价于线性模型。 易错点是：不要把激活函数说成只改变维度，它主要提供非线性。
- **归一化**：把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 考试写作时要补一句：常考图像预处理为什么要缩放像素值。 易错点是：归一化不等于 BatchNorm，前者多是输入预处理。
- **Batch**：一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 考试写作时要补一句：常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 易错点是：Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。
- **梯度下降**：数据预处理：从归一化到标准化；为什么需要预处理？；机器学习：归一化是在涉及距离计算的时候，做处理就会把他映射到[0, 1]之间。；图像像素值范围是 [0, 255]，直接输入神经网络会带来问题：；梯度下降效率低：大数值导致梯度更新不稳定 第二层：进阶处理 — Z-Score标准化；📌 深度学习中更常... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **NCHW**：数据格式：NCHW vs NHWC；不同框架使用不同的图像数据格式：；格式 | 维度顺序 | 使用框架；NCHW | (Batch（批量大小）, Channel（3）, Height, Width) | PyTorch；NHWC | (Batch, Height, Width, Channel) |... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。

### 4. 本节自测

- 判断：卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：标准化只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：卷积核只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：激活函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：归一化只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Batch只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：梯度下降只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：NCHW只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 输出尺寸：(输入尺寸 + 2P - K) / S + 1。
- 标准化常写作 (x-mean)/std。
- 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。
- 仿射变换 + 激活函数 = 神经网络层的基本模式。
- 图像常先把像素值缩放到 0-1。
- Batch 是一批样本；Epoch 是全训练集完整训练一遍。
- 数据预处理：从归一化到标准化；为什么需要预处理？；机器学习：归一化是在涉及距离计算的时候，做处理就会把他映射到[0, 1]之间。；图像像素值范围是 [0, 255]，直接输入神经网络会带来问题：；梯度下降效率低：大数值导致梯度更新不稳定 第...
- 数据格式：NCHW vs NHWC；不同框架使用不同的图像数据格式：；格式 | 维度顺序 | 使用框架；NCHW | (Batch（批量大小）, Channel（3）, Height, Width) | PyTorch；NHWC | (Ba...
- 数据格式：NCHW vs NHWC；不同框架使用不同的图像数据格式：；格式 | 维度顺序 | 使用框架；NCHW | (Batch（批量大小）, Channel（3）, Height, Width) | PyTorch；NHWC | (Ba...
- 数据格式：NCHW vs NHWC；不同框架使用不同的图像数据格式：；格式 | 维度顺序 | 使用框架；NCHW | (Batch（批量大小）, Channel（3）, Height, Width) | PyTorch；NHWC | (Ba...

## 第 4 部分：第四章 卷积神经网络基础与入门实践 / 第3节 典型的卷积神经网络架构

### 1. 本节先看什么

这一节先把 **卷积、池化、卷积核、Dropout** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 卷积 | 用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 | 常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 | 不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。 | 输出尺寸：(输入尺寸 + 2P - K) / S + 1。 |
| 池化 | 对局部区域做最大值或平均值汇总，降低空间尺寸并增强一定平移不变性。 | 常考池化作用、是否有可学习参数、与卷积的区别。 | 池化不是全连接，也不是用来增加参数量。 | 池化：降采样、压缩空间尺寸、保留显著信息。 |
| 卷积核 | 卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。 | 常考卷积核大小、通道数、参数共享和输出尺寸计算。 | 卷积核参数会训练更新，不是手工固定模板。 | 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。 |
| Dropout | 训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 | 常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 | Dropout 不是提高模型容量，而是正则化。 | Dropout：训练随机失活，推理正常使用。 |
| 激活函数 | 给线性变换加入非线性，使网络能拟合非线性关系。 | 常考没有激活函数时多层线性网络仍等价于线性模型。 | 不要把激活函数说成只改变维度，它主要提供非线性。 | 仿射变换 + 激活函数 = 神经网络层的基本模式。 |
| AlexNet | AlexNet的三层结构；AlexNet架构包含三个主要部分：features、avgpool和classifier：；部分 / 功能 / 包含层；features / 特征提取器 / 5个卷积块（卷积+ReLU+池化）；avgpool / 自适应池化 / AdaptiveAvgPool2d(6,6... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | AlexNet的三层结构；AlexNet架构包含三个主要部分：features、avgpool和classifier：；部分 / 功能 / 包含层；features / 特征提取器 / 5个卷积块（卷积+ReLU+池化）；avgpool /... |

### 3. 像考试答案一样组织语言

- **卷积**：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 考试写作时要补一句：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 易错点是：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- **池化**：对局部区域做最大值或平均值汇总，降低空间尺寸并增强一定平移不变性。 考试写作时要补一句：常考池化作用、是否有可学习参数、与卷积的区别。 易错点是：池化不是全连接，也不是用来增加参数量。
- **卷积核**：卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。 考试写作时要补一句：常考卷积核大小、通道数、参数共享和输出尺寸计算。 易错点是：卷积核参数会训练更新，不是手工固定模板。
- **Dropout**：训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 考试写作时要补一句：常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 易错点是：Dropout 不是提高模型容量，而是正则化。
- **激活函数**：给线性变换加入非线性，使网络能拟合非线性关系。 考试写作时要补一句：常考没有激活函数时多层线性网络仍等价于线性模型。 易错点是：不要把激活函数说成只改变维度，它主要提供非线性。
- **AlexNet**：AlexNet的三层结构；AlexNet架构包含三个主要部分：features、avgpool和classifier：；部分 | 功能 | 包含层；features | 特征提取器 | 5个卷积块（卷积+ReLU+池化）；avgpool | 自适应池化 | AdaptiveAvgPool2d(6,6... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。

### 4. 本节自测

- 判断：卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：池化只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：卷积核只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Dropout只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：激活函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：AlexNet只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 输出尺寸：(输入尺寸 + 2P - K) / S + 1。
- 池化：降采样、压缩空间尺寸、保留显著信息。
- 卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。
- Dropout：训练随机失活，推理正常使用。
- 仿射变换 + 激活函数 = 神经网络层的基本模式。
- AlexNet的三层结构；AlexNet架构包含三个主要部分：features、avgpool和classifier：；部分 | 功能 | 包含层；features | 特征提取器 | 5个卷积块（卷积+ReLU+池化）；avgpool |...

## 第 5 部分：第四章 卷积神经网络基础与入门实践 / 第4节 网络配置与损失函数应匹配

### 1. 本节先看什么

这一节先把 **交叉熵、损失函数、BCEWithLogitsLoss、CrossEntropyLoss** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 交叉熵 | 交叉熵损失（Cross-Entropy Loss） 对于多分类问题，使用交叉熵损失：；$$L = -\frac{1}{N} \sum_{n=1}^{N} \log h_{k^*}(x^{(n)})$$；对于每个样本，只看正确样本的概率，再取负对数，最后取平均。 | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 交叉熵损失（Cross-Entropy Loss） 对于多分类问题，使用交叉熵损失：；$$L = -\frac{1}{N} \sum_{n=1}^{N} \log h_{k^*}(x^{(n)})$$；对于每个样本，只看正确样本的概率，再取... |
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |
| BCEWithLogitsLoss | 网络架构与损失函数匹配表；BCE Loss / BCE With Logits Loss / NLL Loss / Cross-Entropy Loss；Input (each data point) / probability / logit / array of log probabilitie... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 网络架构与损失函数匹配表；BCE Loss / BCE With Logits Loss / NLL Loss / Cross-Entropy Loss；Input (each data point) / probability / log... |
| CrossEntropyLoss | 网络架构与损失函数匹配表；BCE Loss / BCE With Logits Loss / NLL Loss / Cross-Entropy Loss；Input (each data point) / probability / logit / array of log probabilitie... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 网络架构与损失函数匹配表；BCE Loss / BCE With Logits Loss / NLL Loss / Cross-Entropy Loss；Input (each data point) / probability / log... |
| 类别不平衡 | 网络架构与损失函数匹配表；BCE Loss / BCE With Logits Loss / NLL Loss / Cross-Entropy Loss；Input (each data point) / probability / logit / array of log probabilitie... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 网络架构与损失函数匹配表；BCE Loss / BCE With Logits Loss / NLL Loss / Cross-Entropy Loss；Input (each data point) / probability / log... |
| Sigmoid | 把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 | 常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 | Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。 | Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。 |
| Softmax | 把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 | 常考多分类输出、注意力权重为什么能加权求和。 | Softmax 是对一组数整体归一化，不是逐个独立压缩。 | Softmax 输出非负且总和为 1。 |
| 激活函数 | 给线性变换加入非线性，使网络能拟合非线性关系。 | 常考没有激活函数时多层线性网络仍等价于线性模型。 | 不要把激活函数说成只改变维度，它主要提供非线性。 | 仿射变换 + 激活函数 = 神经网络层的基本模式。 |
| 归一化 | 把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 | 常考图像预处理为什么要缩放像素值。 | 归一化不等于 BatchNorm，前者多是输入预处理。 | 图像常先把像素值缩放到 0-1。 |
| WeightedRandomSampler | 处理数据集类别不平衡问题的策略；为什么需要weight和pos_weight？；在实际数据集中，不同类别的样本数量往往不均衡：；场景 / 类别分布示例 / 导致的问题；医疗诊断 / 正常: 9900, 患病: 100 / 模型全部预测为"正常"也能达到99%准确率 | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 处理数据集类别不平衡问题的策略；为什么需要weight和pos_weight？；在实际数据集中，不同类别的样本数量往往不均衡：；场景 / 类别分布示例 / 导致的问题；医疗诊断 / 正常: 9900, 患病: 100 / 模型全部预测为"正... |

### 3. 像考试答案一样组织语言

- **交叉熵**：交叉熵损失（Cross-Entropy Loss） 对于多分类问题，使用交叉熵损失：；$$L = -\frac{1}{N} \sum_{n=1}^{N} \log h_{k^*}(x^{(n)})$$；对于每个样本，只看正确样本的概率，再取负对数，最后取平均。 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **损失函数**：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 考试写作时要补一句：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 易错点是：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- **BCEWithLogitsLoss**：网络架构与损失函数匹配表；BCE Loss | BCE With Logits Loss | NLL Loss | Cross-Entropy Loss；Input (each data point) | probability | logit | array of log probabilitie... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **CrossEntropyLoss**：网络架构与损失函数匹配表；BCE Loss | BCE With Logits Loss | NLL Loss | Cross-Entropy Loss；Input (each data point) | probability | logit | array of log probabilitie... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **类别不平衡**：网络架构与损失函数匹配表；BCE Loss | BCE With Logits Loss | NLL Loss | Cross-Entropy Loss；Input (each data point) | probability | logit | array of log probabilitie... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **Sigmoid**：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 考试写作时要补一句：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 易错点是：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- **Softmax**：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 考试写作时要补一句：常考多分类输出、注意力权重为什么能加权求和。 易错点是：Softmax 是对一组数整体归一化，不是逐个独立压缩。
- **激活函数**：给线性变换加入非线性，使网络能拟合非线性关系。 考试写作时要补一句：常考没有激活函数时多层线性网络仍等价于线性模型。 易错点是：不要把激活函数说成只改变维度，它主要提供非线性。

### 4. 本节自测

- 判断：交叉熵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：损失函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：BCEWithLogitsLoss只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：CrossEntropyLoss只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：类别不平衡只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Sigmoid只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Softmax只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：激活函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 交叉熵损失（Cross-Entropy Loss） 对于多分类问题，使用交叉熵损失：；$$L = -\frac{1}{N} \sum_{n=1}^{N} \log h_{k^*}(x^{(n)})$$；对于每个样本，只看正确样本的概率，再取...
- 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
- 网络架构与损失函数匹配表；BCE Loss | BCE With Logits Loss | NLL Loss | Cross-Entropy Loss；Input (each data point) | probability | log...
- 网络架构与损失函数匹配表；BCE Loss | BCE With Logits Loss | NLL Loss | Cross-Entropy Loss；Input (each data point) | probability | log...
- 网络架构与损失函数匹配表；BCE Loss | BCE With Logits Loss | NLL Loss | Cross-Entropy Loss；Input (each data point) | probability | log...
- Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
- Softmax 输出非负且总和为 1。
- 仿射变换 + 激活函数 = 神经网络层的基本模式。
- 图像常先把像素值缩放到 0-1。
- 处理数据集类别不平衡问题的策略；为什么需要weight和pos_weight？；在实际数据集中，不同类别的样本数量往往不均衡：；场景 | 类别分布示例 | 导致的问题；医疗诊断 | 正常: 9900, 患病: 100 | 模型全部预测为"正...

## 第 6 部分：第四章 卷积神经网络基础与入门实践 / 第5节 PyTorch实现CNN图像分类（完整流程）

### 1. 本节先看什么

这一节先把 **WeightedRandomSampler、类别不平衡、归一化、标准化** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| WeightedRandomSampler | 本节课我们将使用参考书中的完整流程实现CNN图像分类，包括：；1. 数据集生成：使用线条图像生成数据集；2. 数据集分割：训练/验证/测试集划分；3. 数据变换：Compose组合多个变换；4. 数据加载：使用WeightedRandomSampler处理类别不平衡 步骤5：WeightedRand... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 本节课我们将使用参考书中的完整流程实现CNN图像分类，包括：；1. 数据集生成：使用线条图像生成数据集；2. 数据集分割：训练/验证/测试集划分；3. 数据变换：Compose组合多个变换；4. 数据加载：使用WeightedRandomS... |
| 类别不平衡 | 本节课我们将使用参考书中的完整流程实现CNN图像分类，包括：；1. 数据集生成：使用线条图像生成数据集；2. 数据集分割：训练/验证/测试集划分；3. 数据变换：Compose组合多个变换；4. 数据加载：使用WeightedRandomSampler处理类别不平衡 步骤5：WeightedRand... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 本节课我们将使用参考书中的完整流程实现CNN图像分类，包括：；1. 数据集生成：使用线条图像生成数据集；2. 数据集分割：训练/验证/测试集划分；3. 数据变换：Compose组合多个变换；4. 数据加载：使用WeightedRandomS... |
| 归一化 | 把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 | 常考图像预处理为什么要缩放像素值。 | 归一化不等于 BatchNorm，前者多是输入预处理。 | 图像常先把像素值缩放到 0-1。 |
| 标准化 | 把数据按均值和方差调整到更稳定的尺度，帮助模型训练。 | 常考归一化和标准化的区别、为什么图像输入要预处理。 | 不要把标准化简单等同于除以 255；标准化通常涉及均值和标准差。 | 标准化常写作 (x-mean)/std。 |
| 学习率 | 控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 | 常考学习率过大/过小的训练现象，以及学习率调度的目的。 | 学习率不是越大越好；也不是模型学到的参数。 | 学习率决定更新步长。 |
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |
| Softmax | 把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 | 常考多分类输出、注意力权重为什么能加权求和。 | Softmax 是对一组数整体归一化，不是逐个独立压缩。 | Softmax 输出非负且总和为 1。 |
| 交叉熵 | 1. CNN基础概念；生物视觉启发（Hubel & Wiesel）；局部连接与权值共享；卷积运算与互相关的关系；卷积的三种形式（Valid、Same、Full） | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 1. CNN基础概念；生物视觉启发（Hubel & Wiesel）；局部连接与权值共享；卷积运算与互相关的关系；卷积的三种形式（Valid、Same、Full） |
| 卷积 | 用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 | 常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 | 不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。 | 输出尺寸：(输入尺寸 + 2P - K) / S + 1。 |
| 互相关 | 形式接近卷积，但不翻转卷积核；深度学习框架里的卷积层通常实际做互相关。 | 常考 CNN 中是否需要翻转卷积核，或为什么工程上用互相关。 | 不要因为名字叫卷积层，就默认一定执行数学卷积的核翻转。 | CNN 训练会学习卷积核参数，因此互相关也能学到有效特征。 |
| Valid | 不填充边界，只在卷积核完全落入输入时计算，输出尺寸会变小。 | 常和 Same、Full 对比考输出大小。 | Valid 不是大量填充，恰好是不填充。 | Valid：无填充，输出通常最小。 |
| Same | 通过适当填充，让卷积输出空间尺寸尽量和输入保持一致。 | 常考给定输入、卷积核、步长时需要多少 padding。 | Same 的目标是保持尺寸，不是扩大特征图。 | 步长为 1 时，Same 卷积常让 H、W 不变。 |
| Full | 在边界外进行更充分的填充，使卷积核可以覆盖到输入边缘外侧，输出会变大。 | 常和 Valid、Same 放在一起判断哪种输出最大。 | Full 不是常规 CNN 默认选择，考试多用于概念区分。 | Full：充分填充，输出通常最大。 |
| 池化 | 对局部区域做最大值或平均值汇总，降低空间尺寸并增强一定平移不变性。 | 常考池化作用、是否有可学习参数、与卷积的区别。 | 池化不是全连接，也不是用来增加参数量。 | 池化：降采样、压缩空间尺寸、保留显著信息。 |
| NCHW | 1. CNN基础概念；生物视觉启发（Hubel & Wiesel）；局部连接与权值共享；卷积运算与互相关的关系；卷积的三种形式（Valid、Same、Full） | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 1. CNN基础概念；生物视觉启发（Hubel & Wiesel）；局部连接与权值共享；卷积运算与互相关的关系；卷积的三种形式（Valid、Same、Full） |
| NHWC | 1. CNN基础概念；生物视觉启发（Hubel & Wiesel）；局部连接与权值共享；卷积运算与互相关的关系；卷积的三种形式（Valid、Same、Full） | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 1. CNN基础概念；生物视觉启发（Hubel & Wiesel）；局部连接与权值共享；卷积运算与互相关的关系；卷积的三种形式（Valid、Same、Full） |

### 3. 像考试答案一样组织语言

- **WeightedRandomSampler**：本节课我们将使用参考书中的完整流程实现CNN图像分类，包括：；1. 数据集生成：使用线条图像生成数据集；2. 数据集分割：训练/验证/测试集划分；3. 数据变换：Compose组合多个变换；4. 数据加载：使用WeightedRandomSampler处理类别不平衡 步骤5：WeightedRand... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **类别不平衡**：本节课我们将使用参考书中的完整流程实现CNN图像分类，包括：；1. 数据集生成：使用线条图像生成数据集；2. 数据集分割：训练/验证/测试集划分；3. 数据变换：Compose组合多个变换；4. 数据加载：使用WeightedRandomSampler处理类别不平衡 步骤5：WeightedRand... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **归一化**：把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 考试写作时要补一句：常考图像预处理为什么要缩放像素值。 易错点是：归一化不等于 BatchNorm，前者多是输入预处理。
- **标准化**：把数据按均值和方差调整到更稳定的尺度，帮助模型训练。 考试写作时要补一句：常考归一化和标准化的区别、为什么图像输入要预处理。 易错点是：不要把标准化简单等同于除以 255；标准化通常涉及均值和标准差。
- **学习率**：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 考试写作时要补一句：常考学习率过大/过小的训练现象，以及学习率调度的目的。 易错点是：学习率不是越大越好；也不是模型学到的参数。
- **损失函数**：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 考试写作时要补一句：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 易错点是：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- **Softmax**：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。 考试写作时要补一句：常考多分类输出、注意力权重为什么能加权求和。 易错点是：Softmax 是对一组数整体归一化，不是逐个独立压缩。
- **交叉熵**：1. CNN基础概念；生物视觉启发（Hubel & Wiesel）；局部连接与权值共享；卷积运算与互相关的关系；卷积的三种形式（Valid、Same、Full） 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。

### 4. 本节自测

- 判断：WeightedRandomSampler只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：类别不平衡只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：归一化只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：标准化只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：学习率只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：损失函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Softmax只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：交叉熵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 本节课我们将使用参考书中的完整流程实现CNN图像分类，包括：；1. 数据集生成：使用线条图像生成数据集；2. 数据集分割：训练/验证/测试集划分；3. 数据变换：Compose组合多个变换；4. 数据加载：使用WeightedRandomS...
- 本节课我们将使用参考书中的完整流程实现CNN图像分类，包括：；1. 数据集生成：使用线条图像生成数据集；2. 数据集分割：训练/验证/测试集划分；3. 数据变换：Compose组合多个变换；4. 数据加载：使用WeightedRandomS...
- 图像常先把像素值缩放到 0-1。
- 标准化常写作 (x-mean)/std。
- 学习率决定更新步长。
- 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
- Softmax 输出非负且总和为 1。
- 1. CNN基础概念；生物视觉启发（Hubel & Wiesel）；局部连接与权值共享；卷积运算与互相关的关系；卷积的三种形式（Valid、Same、Full）
- 输出尺寸：(输入尺寸 + 2P - K) / S + 1。
- CNN 训练会学习卷积核参数，因此互相关也能学到有效特征。

## 四、考前总复盘

考前不要平均用力。优先检查下面这些问题：

1. 每个核心概念能不能用一句话说明“它是什么”。
2. 能不能说出它在模型结构、训练流程或数据处理中的位置。
3. 能不能说出一个最常见的错误说法。
4. 遇到选择题时，能不能判断选项是在混淆概念、夸大作用，还是写反了训练/推理阶段。

## 五、打印建议

<style>@media print { @page { margin: 8mm; } body { font-size: 11pt; line-height: 1.35; } h1, h2, h3 { page-break-after: avoid; } table { font-size: 9pt; border-collapse: collapse; } th, td { padding: 3px 5px; border: 1px solid #ddd; vertical-align: top; } }</style>
