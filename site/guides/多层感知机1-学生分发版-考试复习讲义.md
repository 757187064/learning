# 多层感知机1_学生分发版：期末考试复习讲义

> 使用方式：先通读“学习路线”，再按章节背“必记句子”，最后用每章自测题检查。这里不是课件原文搬运，而是按考试复习顺序重写。

## 一、这份课件先解决什么问题

这一类课件是深度学习入门骨架：先理解线性变换和激活函数，再理解损失函数、反向传播、优化器和训练流程。

考试常围绕“为什么需要非线性”“分类输出层和损失函数怎么匹配”“训练循环每一步做什么”来考。

## 二、学习路线

1. **第二章 神经网络训练流程 / 第一节 梯度下降法**：先抓 梯度下降、Epoch、Batch、损失函数、反向传播。
2. **第二章 神经网络训练流程 / 第二节 模型训练流程的优化**：先抓 自动微分、损失函数、Batch、反向传播、计算图。
3. **第二章 神经网络训练流程 / 第三节 自动微分和计算图**：先抓 梯度下降、损失函数、反向传播、计算图、自动微分。
4. **第二章 神经网络训练流程 / 第四节 构建神经网络训练代码**：先抓 优化器、Epoch、损失函数、Dropout、Batch。

## 三、章节详解

## 第 1 部分：第二章 神经网络训练流程 / 第一节 梯度下降法

### 1. 本节先看什么

这一节先把 **梯度下降、Epoch、Batch、损失函数** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 梯度下降 | 1. 梯度下降法的定义；[ ] Gradient descent 的基本定义；是一种无约束数学优化的方法，是一种一阶迭代优化算法，其用于寻找可微函数的局部最小值；是一种迭代技术，试图从初始猜测开始，为给定模型，数据点，和损失函数找到的最佳可能的参数；[ ] 无约束数学优化 vs 有约束数学优化（SV... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 梯度下降法的定义；[ ] Gradient descent 的基本定义；是一种无约束数学优化的方法，是一种一阶迭代优化算法，其用于寻找可微函数的局部最小值；是一种迭代技术，试图从初始猜测开始，为给定模型，数据点，和损失函数找到的最佳可... |
| Epoch | 训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。 | 常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。 | Epoch 不是一次参数更新；一次更新通常对应一个 batch。 | 1 个 Epoch = 全部训练样本被用过一遍。 |
| Batch | 一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 | 常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 | Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。 | Batch 是一批样本；Epoch 是全训练集完整训练一遍。 |
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |
| 反向传播 | 1. 梯度下降法的定义；[ ] Gradient descent 的基本定义；是一种无约束数学优化的方法，是一种一阶迭代优化算法，其用于寻找可微函数的局部最小值；是一种迭代技术，试图从初始猜测开始，为给定模型，数据点，和损失函数找到的最佳可能的参数；[ ] 无约束数学优化 vs 有约束数学优化（SV... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 梯度下降法的定义；[ ] Gradient descent 的基本定义；是一种无约束数学优化的方法，是一种一阶迭代优化算法，其用于寻找可微函数的局部最小值；是一种迭代技术，试图从初始猜测开始，为给定模型，数据点，和损失函数找到的最佳可... |
| 学习率 | 控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 | 常考学习率过大/过小的训练现象，以及学习率调度的目的。 | 学习率不是越大越好；也不是模型学到的参数。 | 学习率决定更新步长。 |
| 超参数 | Step 4: 更新参数；使用梯度去更新参数，需要考虑超参数：学习率；参数更新公式：数据减去学习率乘以梯度得到新数据 | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | Step 4: 更新参数；使用梯度去更新参数，需要考虑超参数：学习率；参数更新公式：数据减去学习率乘以梯度得到新数据 |

### 3. 像考试答案一样组织语言

- **梯度下降**：1. 梯度下降法的定义；[ ] Gradient descent 的基本定义；是一种无约束数学优化的方法，是一种一阶迭代优化算法，其用于寻找可微函数的局部最小值；是一种迭代技术，试图从初始猜测开始，为给定模型，数据点，和损失函数找到的最佳可能的参数；[ ] 无约束数学优化 vs 有约束数学优化（SV... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **Epoch**：训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。 考试写作时要补一句：常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。 易错点是：Epoch 不是一次参数更新；一次更新通常对应一个 batch。
- **Batch**：一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 考试写作时要补一句：常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 易错点是：Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。
- **损失函数**：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 考试写作时要补一句：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 易错点是：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- **反向传播**：1. 梯度下降法的定义；[ ] Gradient descent 的基本定义；是一种无约束数学优化的方法，是一种一阶迭代优化算法，其用于寻找可微函数的局部最小值；是一种迭代技术，试图从初始猜测开始，为给定模型，数据点，和损失函数找到的最佳可能的参数；[ ] 无约束数学优化 vs 有约束数学优化（SV... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **学习率**：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 考试写作时要补一句：常考学习率过大/过小的训练现象，以及学习率调度的目的。 易错点是：学习率不是越大越好；也不是模型学到的参数。
- **超参数**：Step 4: 更新参数；使用梯度去更新参数，需要考虑超参数：学习率；参数更新公式：数据减去学习率乘以梯度得到新数据 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。

### 4. 本节自测

- 判断：梯度下降只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Epoch只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Batch只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：损失函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：反向传播只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：学习率只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：超参数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 1. 梯度下降法的定义；[ ] Gradient descent 的基本定义；是一种无约束数学优化的方法，是一种一阶迭代优化算法，其用于寻找可微函数的局部最小值；是一种迭代技术，试图从初始猜测开始，为给定模型，数据点，和损失函数找到的最佳可...
- 1 个 Epoch = 全部训练样本被用过一遍。
- Batch 是一批样本；Epoch 是全训练集完整训练一遍。
- 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
- 1. 梯度下降法的定义；[ ] Gradient descent 的基本定义；是一种无约束数学优化的方法，是一种一阶迭代优化算法，其用于寻找可微函数的局部最小值；是一种迭代技术，试图从初始猜测开始，为给定模型，数据点，和损失函数找到的最佳可...
- 学习率决定更新步长。
- Step 4: 更新参数；使用梯度去更新参数，需要考虑超参数：学习率；参数更新公式：数据减去学习率乘以梯度得到新数据

## 第 2 部分：第二章 神经网络训练流程 / 第二节 模型训练流程的优化

### 1. 本节先看什么

这一节先把 **自动微分、损失函数、Batch、反向传播** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 自动微分 | 1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（Tensor）的概念 深度学习中为什么要使用GPU；张量 维... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（... |
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |
| Batch | 一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 | 常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 | Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。 | Batch 是一批样本；Epoch 是全训练集完整训练一遍。 |
| 反向传播 | 1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（Tensor）的概念 | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（... |
| 计算图 | 1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（Tensor）的概念 | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（... |
| 优化器 | 1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（Tensor）的概念 | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（... |

### 3. 像考试答案一样组织语言

- **自动微分**：1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（Tensor）的概念 深度学习中为什么要使用GPU；张量 维... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **损失函数**：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 考试写作时要补一句：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 易错点是：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- **Batch**：一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 考试写作时要补一句：常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 易错点是：Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。
- **反向传播**：1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（Tensor）的概念 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **计算图**：1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（Tensor）的概念 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **优化器**：1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（Tensor）的概念 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。

### 4. 本节自测

- 判断：自动微分只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：损失函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Batch只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：反向传播只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：计算图只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：优化器只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（...
- 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
- Batch 是一批样本；Epoch 是全训练集完整训练一遍。
- 1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（...
- 1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（...
- 1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（...

## 第 3 部分：第二章 神经网络训练流程 / 第三节 自动微分和计算图

### 1. 本节先看什么

这一节先把 **梯度下降、损失函数、反向传播、计算图** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 梯度下降 | 1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 神经网络的训练本质是优化问题：找到一组权重（Weights），使得损失函数（Loss）最小。；梯度指... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 神经网络的训练本质是优化问题：找到一组权... |
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |
| 反向传播 | 1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 作业3:深度学习框架的自动微分实现；参考资料：/Users/pandamas/work/03深度学习... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 作业3:深度学习框架的自动微分实现；参考... |
| 计算图 | 1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 作业3:深度学习框架的自动微分实现；参考资料：/Users/pandamas/work/03深度学习... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 作业3:深度学习框架的自动微分实现；参考... |
| 自动微分 | 作业3:深度学习框架的自动微分实现；参考资料：/Users/pandamas/work/03深度学习讲习班/参考资料/Build Framework/part01.ipynb；本作业旨在通过阅读简化版自动微分框架的实现代码，深入理解深度学习框架中自动微分的核心原理。；请仔细阅读参考资料 notebo... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 作业3:深度学习框架的自动微分实现；参考资料：/Users/pandamas/work/03深度学习讲习班/参考资料/Build Framework/part01.ipynb；本作业旨在通过阅读简化版自动微分框架的实现代码，深入理解深度学习... |

### 3. 像考试答案一样组织语言

- **梯度下降**：1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 神经网络的训练本质是优化问题：找到一组权重（Weights），使得损失函数（Loss）最小。；梯度指... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **损失函数**：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 考试写作时要补一句：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 易错点是：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- **反向传播**：1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 作业3:深度学习框架的自动微分实现；参考资料：/Users/pandamas/work/03深度学习... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **计算图**：1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 作业3:深度学习框架的自动微分实现；参考资料：/Users/pandamas/work/03深度学习... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **自动微分**：作业3:深度学习框架的自动微分实现；参考资料：/Users/pandamas/work/03深度学习讲习班/参考资料/Build Framework/part01.ipynb；本作业旨在通过阅读简化版自动微分框架的实现代码，深入理解深度学习框架中自动微分的核心原理。；请仔细阅读参考资料 notebo... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。

### 4. 本节自测

- 判断：梯度下降只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：损失函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：反向传播只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：计算图只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：自动微分只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 神经网络的训练本质是优化问题：找到一组权...
- 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
- 1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 作业3:深度学习框架的自动微分实现；参考...
- 1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 作业3:深度学习框架的自动微分实现；参考...
- 作业3:深度学习框架的自动微分实现；参考资料：/Users/pandamas/work/03深度学习讲习班/参考资料/Build Framework/part01.ipynb；本作业旨在通过阅读简化版自动微分框架的实现代码，深入理解深度学习...

## 第 4 部分：第二章 神经网络训练流程 / 第四节 构建神经网络训练代码

### 1. 本节先看什么

这一节先把 **优化器、Epoch、损失函数、Dropout** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 优化器 | 1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer.zero_grad()；2. 常见优化算法 至此，我们已经... | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer... |
| Epoch | 训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。 | 常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。 | Epoch 不是一次参数更新；一次更新通常对应一个 batch。 | 1 个 Epoch = 全部训练样本被用过一遍。 |
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |
| Dropout | 训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 | 常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 | Dropout 不是提高模型容量，而是正则化。 | Dropout：训练随机失活，推理正常使用。 |
| Batch | 一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 | 常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 | Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。 | Batch 是一批样本；Epoch 是全训练集完整训练一遍。 |
| 梯度下降 | 1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer.zero_grad()；2. 常见优化算法 | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer... |
| 学习率 | 控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 | 常考学习率过大/过小的训练现象，以及学习率调度的目的。 | 学习率不是越大越好；也不是模型学到的参数。 | 学习率决定更新步长。 |
| CrossEntropyLoss | 1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer.zero_grad()；2. 常见优化算法 | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer... |
| Xavier | 1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer.zero_grad()；2. 常见优化算法 | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer... |
| BatchNorm | 在小批量上标准化中间激活，再用可学习参数恢复表达能力，帮助训练更稳定。 | 常考训练/推理阶段统计量不同，以及放在卷积/全连接和激活附近。 | 推理阶段一般使用训练中累计的均值方差，不依赖当前 batch。 | BatchNorm：标准化激活，稳定训练，加快收敛。 |
| 权重衰减 | 1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer.zero_grad()；2. 常见优化算法 | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | 1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer... |
| RMSProp | 用梯度平方的指数滑动平均调整学习率，缓解 Adagrad 学习率过快衰减。 | 常考它和 Adagrad 的区别。 | 不要写成简单固定学习率下降。 | RMSProp 使用指数滑动平均保存近期梯度尺度。 |
| Adam | 结合动量和自适应学习率思想，为不同参数调整更新幅度。 | 常和 SGD、Momentum、RMSProp 对比优化器特点。 | Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。 | Adam = 一阶动量 + 二阶矩估计。 |
| 归一化 | 把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 | 常考图像预处理为什么要缩放像素值。 | 归一化不等于 BatchNorm，前者多是输入预处理。 | 图像常先把像素值缩放到 0-1。 |
| 反向传播 | cpu负责加载数据 预处理 组装批次；gpu负责前向传播 计算损失 反向传播 更新参数；gpu存放本批次需要的已经与处理以后的数据 以及模型参数 梯度 优化器状态等；使用TensorDataset简化 | 会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 | 不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。 | cpu负责加载数据 预处理 组装批次；gpu负责前向传播 计算损失 反向传播 更新参数；gpu存放本批次需要的已经与处理以后的数据 以及模型参数 梯度 优化器状态等；使用TensorDataset简化 |

### 3. 像考试答案一样组织语言

- **优化器**：1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer.zero_grad()；2. 常见优化算法 至此，我们已经... 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **Epoch**：训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。 考试写作时要补一句：常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。 易错点是：Epoch 不是一次参数更新；一次更新通常对应一个 batch。
- **损失函数**：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 考试写作时要补一句：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 易错点是：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- **Dropout**：训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 考试写作时要补一句：常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 易错点是：Dropout 不是提高模型容量，而是正则化。
- **Batch**：一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 考试写作时要补一句：常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 易错点是：Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。
- **梯度下降**：1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer.zero_grad()；2. 常见优化算法 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- **学习率**：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 考试写作时要补一句：常考学习率过大/过小的训练现象，以及学习率调度的目的。 易错点是：学习率不是越大越好；也不是模型学到的参数。
- **CrossEntropyLoss**：1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer.zero_grad()；2. 常见优化算法 考试写作时要补一句：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。 易错点是：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。

### 4. 本节自测

- 判断：优化器只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Epoch只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：损失函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Dropout只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Batch只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：梯度下降只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：学习率只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：CrossEntropyLoss只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer...
- 1 个 Epoch = 全部训练样本被用过一遍。
- 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
- Dropout：训练随机失活，推理正常使用。
- Batch 是一批样本；Epoch 是全训练集完整训练一遍。
- 1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer...
- 学习率决定更新步长。
- 1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer...
- 1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer...
- BatchNorm：标准化激活，稳定训练，加快收敛。

## 四、考前总复盘

考前不要平均用力。优先检查下面这些问题：

1. 每个核心概念能不能用一句话说明“它是什么”。
2. 能不能说出它在模型结构、训练流程或数据处理中的位置。
3. 能不能说出一个最常见的错误说法。
4. 遇到选择题时，能不能判断选项是在混淆概念、夸大作用，还是写反了训练/推理阶段。

## 五、打印建议

<style>@media print { @page { margin: 8mm; } body { font-size: 11pt; line-height: 1.35; } h1, h2, h3 { page-break-after: avoid; } table { font-size: 9pt; border-collapse: collapse; } th, td { padding: 3px 5px; border: 1px solid #ddd; vertical-align: top; } }</style>
