# 卷积神经网络2-学生分发版-终版2026：期末考试复习讲义

> 使用方式：先通读“学习路线”，再按章节背“必记句子”，最后用每章自测题检查。这里不是课件原文搬运，而是按考试复习顺序重写。

## 一、这份课件先解决什么问题

这一类课件主要解决图像数据如何进入神经网络的问题。复习时先抓住“空间结构不能被随便展平”这个动机，再理解卷积、池化、感受野、典型 CNN 架构和训练配置。

考试通常不会只问名词，而是把输出尺寸、参数共享、局部连接、训练技巧和损失函数匹配放在一起判断。

## 二、学习路线

1. **第五章 神经网络训练技巧深度解析**：先抓 学习率、梯度消失、梯度爆炸、优化器、超参数。
2. **第五章 神经网络训练技巧深度解析 / 第1节 梯度消失、梯度爆炸及其解决方案**：先抓 归一化、学习率、Batch、激活函数、梯度爆炸。
3. **第五章 神经网络训练技巧深度解析 / 第2节 优化器改进与学习率调整**：先抓 学习率、Adam、优化器、RMSProp、Momentum。
4. **第五章 神经网络训练技巧深度解析 / 第3节 处理过拟合的综合策略**：先抓 Dropout、数据增强、卷积、权重衰减、损失函数。
5. **第五章 神经网络训练技巧深度解析 / 第4节 超参数选取实践指南**：先抓 学习率、Dropout、权重衰减、Batch、优化器。

## 三、章节详解

## 第 1 部分：第五章 神经网络训练技巧深度解析

### 1. 本节先看什么

这一节先把 **学习率、梯度消失、梯度爆炸、优化器** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 学习率 | 控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 | 常考学习率过大/过小的训练现象，以及学习率调度的目的。 | 学习率不是越大越好；也不是模型学到的参数。 | 学习率决定更新步长。 |
| 梯度消失 | 反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。 | 常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。 | 梯度消失不是过拟合；它是参数难以有效更新的问题。 | 多个小于 1 的因子连乘，会让梯度趋近 0。 |
| 梯度爆炸 | 梯度在反向传播中变得过大，使参数更新剧烈，训练损失可能震荡或变成 NaN。 | 常考梯度裁剪为什么能缓解 RNN 训练不稳定。 | Dropout 主要缓解过拟合，不是解决梯度爆炸的标准答案。 | 梯度裁剪限制梯度范数，防止更新步子过大。 |
| 优化器 | 第五章 神经网络训练技巧深度解析；1. 梯度消失、梯度爆炸及其解决方案；2. 优化器改进与学习率调整；3. 过拟合处理；4. 超参数选取实践指南 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见... | 会结合序列输入、隐藏状态、长距离依赖或门控结构考。 | 不要把序列模型当成普通独立样本分类；时间步之间有信息传递。 | 第五章 神经网络训练技巧深度解析；1. 梯度消失、梯度爆炸及其解决方案；2. 优化器改进与学习率调整；3. 过拟合处理；4. 超参数选取实践指南 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致... |
| 超参数 | 第五章 神经网络训练技巧深度解析；1. 梯度消失、梯度爆炸及其解决方案；2. 优化器改进与学习率调整；3. 过拟合处理；4. 超参数选取实践指南 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见... | 会结合序列输入、隐藏状态、长距离依赖或门控结构考。 | 不要把序列模型当成普通独立样本分类；时间步之间有信息传递。 | 第五章 神经网络训练技巧深度解析；1. 梯度消失、梯度爆炸及其解决方案；2. 优化器改进与学习率调整；3. 过拟合处理；4. 超参数选取实践指南 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致... |
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |
| Batch | 一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 | 常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 | Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。 | Batch 是一批样本；Epoch 是全训练集完整训练一遍。 |
| Sigmoid | 把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 | 常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 | Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。 | Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。 |
| 激活函数 | 给线性变换加入非线性，使网络能拟合非线性关系。 | 常考没有激活函数时多层线性网络仍等价于线性模型。 | 不要把激活函数说成只改变维度，它主要提供非线性。 | 仿射变换 + 激活函数 = 神经网络层的基本模式。 |
| 卷积 | 用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 | 常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 | 不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。 | 输出尺寸：(输入尺寸 + 2P - K) / S + 1。 |
| 归一化 | 把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 | 常考图像预处理为什么要缩放像素值。 | 归一化不等于 BatchNorm，前者多是输入预处理。 | 图像常先把像素值缩放到 0-1。 |
| 梯度裁剪 | 当梯度范数超过阈值时按比例缩小，常用于稳定 RNN 等容易梯度爆炸的训练。 | 常考它处理的是梯度爆炸，而不是梯度消失。 | 裁剪不是把梯度直接清零，而是限制其大小。 | Gradient Clipping：限制梯度大小，稳定训练。 |
| Dropout | 训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 | 常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 | Dropout 不是提高模型容量，而是正则化。 | Dropout：训练随机失活，推理正常使用。 |
| 数据增强 | 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Clipping（范数裁剪）、Backward Hooks（RNN... | 会结合序列输入、隐藏状态、长距离依赖或门控结构考。 | 不要把序列模型当成普通独立样本分类；时间步之间有信息传递。 | 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Cli... |
| 权重衰减 | 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Clipping（范数裁剪）、Backward Hooks（RNN... | 会结合序列输入、隐藏状态、长距离依赖或门控结构考。 | 不要把序列模型当成普通独立样本分类；时间步之间有信息传递。 | 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Cli... |
| Momentum | 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Clipping（范数裁剪）、Backward Hooks（RNN... | 会结合序列输入、隐藏状态、长距离依赖或门控结构考。 | 不要把序列模型当成普通独立样本分类；时间步之间有信息传递。 | 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Cli... |

### 3. 像考试答案一样组织语言

- **学习率**：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 考试写作时要补一句：常考学习率过大/过小的训练现象，以及学习率调度的目的。 易错点是：学习率不是越大越好；也不是模型学到的参数。
- **梯度消失**：反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。 考试写作时要补一句：常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。 易错点是：梯度消失不是过拟合；它是参数难以有效更新的问题。
- **梯度爆炸**：梯度在反向传播中变得过大，使参数更新剧烈，训练损失可能震荡或变成 NaN。 考试写作时要补一句：常考梯度裁剪为什么能缓解 RNN 训练不稳定。 易错点是：Dropout 主要缓解过拟合，不是解决梯度爆炸的标准答案。
- **优化器**：第五章 神经网络训练技巧深度解析；1. 梯度消失、梯度爆炸及其解决方案；2. 优化器改进与学习率调整；3. 过拟合处理；4. 超参数选取实践指南 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见... 考试写作时要补一句：会结合序列输入、隐藏状态、长距离依赖或门控结构考。 易错点是：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- **超参数**：第五章 神经网络训练技巧深度解析；1. 梯度消失、梯度爆炸及其解决方案；2. 优化器改进与学习率调整；3. 过拟合处理；4. 超参数选取实践指南 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见... 考试写作时要补一句：会结合序列输入、隐藏状态、长距离依赖或门控结构考。 易错点是：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- **损失函数**：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 考试写作时要补一句：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 易错点是：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- **Batch**：一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 考试写作时要补一句：常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 易错点是：Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。
- **Sigmoid**：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 考试写作时要补一句：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 易错点是：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。

### 4. 本节自测

- 判断：学习率只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：梯度消失只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：梯度爆炸只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：优化器只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：超参数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：损失函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Batch只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Sigmoid只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 学习率决定更新步长。
- 多个小于 1 的因子连乘，会让梯度趋近 0。
- 梯度裁剪限制梯度范数，防止更新步子过大。
- 第五章 神经网络训练技巧深度解析；1. 梯度消失、梯度爆炸及其解决方案；2. 优化器改进与学习率调整；3. 过拟合处理；4. 超参数选取实践指南 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致...
- 第五章 神经网络训练技巧深度解析；1. 梯度消失、梯度爆炸及其解决方案；2. 优化器改进与学习率调整；3. 过拟合处理；4. 超参数选取实践指南 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致...
- 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
- Batch 是一批样本；Epoch 是全训练集完整训练一遍。
- Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
- 仿射变换 + 激活函数 = 神经网络层的基本模式。
- 输出尺寸：(输入尺寸 + 2P - K) / S + 1。

## 第 2 部分：第五章 神经网络训练技巧深度解析 / 第1节 梯度消失、梯度爆炸及其解决方案

### 1. 本节先看什么

这一节先把 **归一化、学习率、Batch、激活函数** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 归一化 | 把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 | 常考图像预处理为什么要缩放像素值。 | 归一化不等于 BatchNorm，前者多是输入预处理。 | 图像常先把像素值缩放到 0-1。 |
| 学习率 | 控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 | 常考学习率过大/过小的训练现象，以及学习率调度的目的。 | 学习率不是越大越好；也不是模型学到的参数。 | 学习率决定更新步长。 |
| Batch | 一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 | 常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 | Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。 | Batch 是一批样本；Epoch 是全训练集完整训练一遍。 |
| 激活函数 | 给线性变换加入非线性，使网络能拟合非线性关系。 | 常考没有激活函数时多层线性网络仍等价于线性模型。 | 不要把激活函数说成只改变维度，它主要提供非线性。 | 仿射变换 + 激活函数 = 神经网络层的基本模式。 |
| 梯度爆炸 | 梯度在反向传播中变得过大，使参数更新剧烈，训练损失可能震荡或变成 NaN。 | 常考梯度裁剪为什么能缓解 RNN 训练不稳定。 | Dropout 主要缓解过拟合，不是解决梯度爆炸的标准答案。 | 梯度裁剪限制梯度范数，防止更新步子过大。 |
| 梯度消失 | 反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。 | 常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。 | 梯度消失不是过拟合；它是参数难以有效更新的问题。 | 多个小于 1 的因子连乘，会让梯度趋近 0。 |
| 梯度裁剪 | 当梯度范数超过阈值时按比例缩小，常用于稳定 RNN 等容易梯度爆炸的训练。 | 常考它处理的是梯度爆炸，而不是梯度消失。 | 裁剪不是把梯度直接清零，而是限制其大小。 | Gradient Clipping：限制梯度大小，稳定训练。 |
| 卷积 | 用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 | 常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 | 不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。 | 输出尺寸：(输入尺寸 + 2P - K) / S + 1。 |
| 反向传播 | 反向传播使用链式法则，梯度会逐层相乘；如果每层的梯度都小于1，多次相乘后会指数级减小；结果：靠近输入层的权重几乎不更新，模型无法学习；模型训练停滞，损失不再下降；浅层（靠近输入）的特征无法得到有效学习 与梯度消失相反，梯度爆炸是指在反向传播过程中，梯度变得越来越大，最终导致数值溢出。；训练过程中损失... | 会结合序列输入、隐藏状态、长距离依赖或门控结构考。 | 不要把序列模型当成普通独立样本分类；时间步之间有信息传递。 | 反向传播使用链式法则，梯度会逐层相乘；如果每层的梯度都小于1，多次相乘后会指数级减小；结果：靠近输入层的权重几乎不更新，模型无法学习；模型训练停滞，损失不再下降；浅层（靠近输入）的特征无法得到有效学习 与梯度消失相反，梯度爆炸是指在反向传播... |
| 标准化 | 把数据按均值和方差调整到更稳定的尺度，帮助模型训练。 | 常考归一化和标准化的区别、为什么图像输入要预处理。 | 不要把标准化简单等同于除以 255；标准化通常涉及均值和标准差。 | 标准化常写作 (x-mean)/std。 |
| Sigmoid | 把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。 | 常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。 | Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。 | Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。 |
| ResNet | 用残差/跳跃连接让网络学习 F(x)+x，缓解深层网络退化和梯度传播困难。 | 常考残差连接为什么能训练更深网络。 | 残差连接不是单纯增加层数，而是改变信息和梯度路径。 | ResNet 核心：y = F(x) + x。 |
| Inception | BN在CNN中的位置；通常位置：并无定论；通常应用在非线性层之前（也可以放在激活函数之后），根据实践经验（Inception中的操作）；在激活函数之后理论上也是合理（对下一层的输入进行仿射变换以减轻ICS）；BN层的作用更倾向于使loss surface平滑，以改进模型训练 BasicConv2d结... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | BN在CNN中的位置；通常位置：并无定论；通常应用在非线性层之前（也可以放在激活函数之后），根据实践经验（Inception中的操作）；在激活函数之后理论上也是合理（对下一层的输入进行仿射变换以减轻ICS）；BN层的作用更倾向于使loss... |
| 步长 | 卷积核或池化窗口每次移动的间隔。 | 常考步长变大时输出尺寸如何变化。 | 步长越大，输出空间尺寸通常越小。 | Stride 控制滑动间隔。 |
| 权重衰减 | 梯度爆炸解决方案小结；除梯度裁剪外的解决方案；方案类别 / 具体方法 / 核心原理 / 适用场景；权重正则化 / L2正则化/权重衰减 / 限制权重大小，间接约束梯度 / 所有网络；改进初始化 / 正交初始化、LSUV、Fixup / 保持梯度范数在反向传播中稳定 / 极深网络 | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 梯度爆炸解决方案小结；除梯度裁剪外的解决方案；方案类别 / 具体方法 / 核心原理 / 适用场景；权重正则化 / L2正则化/权重衰减 / 限制权重大小，间接约束梯度 / 所有网络；改进初始化 / 正交初始化、LSUV、Fixup / 保持... |
| 优化器 | 梯度爆炸解决方案小结；除梯度裁剪外的解决方案；方案类别 / 具体方法 / 核心原理 / 适用场景；权重正则化 / L2正则化/权重衰减 / 限制权重大小，间接约束梯度 / 所有网络；改进初始化 / 正交初始化、LSUV、Fixup / 保持梯度范数在反向传播中稳定 / 极深网络 | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 梯度爆炸解决方案小结；除梯度裁剪外的解决方案；方案类别 / 具体方法 / 核心原理 / 适用场景；权重正则化 / L2正则化/权重衰减 / 限制权重大小，间接约束梯度 / 所有网络；改进初始化 / 正交初始化、LSUV、Fixup / 保持... |

### 3. 像考试答案一样组织语言

- **归一化**：把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 考试写作时要补一句：常考图像预处理为什么要缩放像素值。 易错点是：归一化不等于 BatchNorm，前者多是输入预处理。
- **学习率**：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 考试写作时要补一句：常考学习率过大/过小的训练现象，以及学习率调度的目的。 易错点是：学习率不是越大越好；也不是模型学到的参数。
- **Batch**：一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 考试写作时要补一句：常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 易错点是：Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。
- **激活函数**：给线性变换加入非线性，使网络能拟合非线性关系。 考试写作时要补一句：常考没有激活函数时多层线性网络仍等价于线性模型。 易错点是：不要把激活函数说成只改变维度，它主要提供非线性。
- **梯度爆炸**：梯度在反向传播中变得过大，使参数更新剧烈，训练损失可能震荡或变成 NaN。 考试写作时要补一句：常考梯度裁剪为什么能缓解 RNN 训练不稳定。 易错点是：Dropout 主要缓解过拟合，不是解决梯度爆炸的标准答案。
- **梯度消失**：反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。 考试写作时要补一句：常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。 易错点是：梯度消失不是过拟合；它是参数难以有效更新的问题。
- **梯度裁剪**：当梯度范数超过阈值时按比例缩小，常用于稳定 RNN 等容易梯度爆炸的训练。 考试写作时要补一句：常考它处理的是梯度爆炸，而不是梯度消失。 易错点是：裁剪不是把梯度直接清零，而是限制其大小。
- **卷积**：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 考试写作时要补一句：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 易错点是：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。

### 4. 本节自测

- 判断：归一化只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：学习率只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Batch只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：激活函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：梯度爆炸只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：梯度消失只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：梯度裁剪只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 图像常先把像素值缩放到 0-1。
- 学习率决定更新步长。
- Batch 是一批样本；Epoch 是全训练集完整训练一遍。
- 仿射变换 + 激活函数 = 神经网络层的基本模式。
- 梯度裁剪限制梯度范数，防止更新步子过大。
- 多个小于 1 的因子连乘，会让梯度趋近 0。
- Gradient Clipping：限制梯度大小，稳定训练。
- 输出尺寸：(输入尺寸 + 2P - K) / S + 1。
- 反向传播使用链式法则，梯度会逐层相乘；如果每层的梯度都小于1，多次相乘后会指数级减小；结果：靠近输入层的权重几乎不更新，模型无法学习；模型训练停滞，损失不再下降；浅层（靠近输入）的特征无法得到有效学习 与梯度消失相反，梯度爆炸是指在反向传播...
- 标准化常写作 (x-mean)/std。

## 第 3 部分：第五章 神经网络训练技巧深度解析 / 第2节 优化器改进与学习率调整

### 1. 本节先看什么

这一节先把 **学习率、Adam、优化器、RMSProp** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 学习率 | 控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 | 常考学习率过大/过小的训练现象，以及学习率调度的目的。 | 学习率不是越大越好；也不是模型学到的参数。 | 学习率决定更新步长。 |
| Adam | 结合动量和自适应学习率思想，为不同参数调整更新幅度。 | 常和 SGD、Momentum、RMSProp 对比优化器特点。 | Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。 | Adam = 一阶动量 + 二阶矩估计。 |
| 优化器 | 第2节 优化器改进与学习率调整；在解决了梯度消失/爆炸问题后，我们需要关注如何高效地收敛到最优解。学习率的选择直接影响优化效率：；学习率太小：收敛缓慢，训练时间过长；学习率太大：训练不稳定，损失可能发散；可以从两个维度优化学习率： 动量解决了"方向"问题，但所有参数使用统一的学习率，可能出现：；参数... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第2节 优化器改进与学习率调整；在解决了梯度消失/爆炸问题后，我们需要关注如何高效地收敛到最优解。学习率的选择直接影响优化效率：；学习率太小：收敛缓慢，训练时间过长；学习率太大：训练不稳定，损失可能发散；可以从两个维度优化学习率： 动量解决... |
| RMSProp | 用梯度平方的指数滑动平均调整学习率，缓解 Adagrad 学习率过快衰减。 | 常考它和 Adagrad 的区别。 | 不要写成简单固定学习率下降。 | RMSProp 使用指数滑动平均保存近期梯度尺度。 |
| Momentum | 动量（Momentum）；动量：迫使目标函数沿着"沟壑"快速下降的方法（冲量/“惯性”）；动量法的参数更新过程：；$$\boldsymbol{v}=\gamma \boldsymbol{v}-\alpha；abla_{\boldsymbol{\theta}} J\left(\boldsymbol{\... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 动量（Momentum）；动量：迫使目标函数沿着"沟壑"快速下降的方法（冲量/“惯性”）；动量法的参数更新过程：；$$\boldsymbol{v}=\gamma \boldsymbol{v}-\alpha；abla_{\boldsymbol... |
| 梯度下降 | 目标函数的形式通常是狭长且浅的沟壑；在深度网络中，目标函数的局部最优点经常以这种形式呈现，所以标准的随机梯度下降算法通常会在“沟壑”两侧反复震荡 优化器 / 核心思想 / 特点 / 适用场景；SGD / 基础梯度下降 / 简单，可能需要调整学习率 / 大规模数据、最终精调；SGD+Momentum... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 目标函数的形式通常是狭长且浅的沟壑；在深度网络中，目标函数的局部最优点经常以这种形式呈现，所以标准的随机梯度下降算法通常会在“沟壑”两侧反复震荡 优化器 / 核心思想 / 特点 / 适用场景；SGD / 基础梯度下降 / 简单，可能需要调整... |
| ResNet | 用残差/跳跃连接让网络学习 F(x)+x，缓解深层网络退化和梯度传播困难。 | 常考残差连接为什么能训练更深网络。 | 残差连接不是单纯增加层数，而是改变信息和梯度路径。 | ResNet 核心：y = F(x) + x。 |
| Epoch | 训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。 | 常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。 | Epoch 不是一次参数更新；一次更新通常对应一个 batch。 | 1 个 Epoch = 全部训练样本被用过一遍。 |
| 梯度消失 | 反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。 | 常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。 | 梯度消失不是过拟合；它是参数难以有效更新的问题。 | 多个小于 1 的因子连乘，会让梯度趋近 0。 |
| 步长 | 卷积核或池化窗口每次移动的间隔。 | 常考步长变大时输出尺寸如何变化。 | 步长越大，输出空间尺寸通常越小。 | Stride 控制滑动间隔。 |

### 3. 像考试答案一样组织语言

- **学习率**：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 考试写作时要补一句：常考学习率过大/过小的训练现象，以及学习率调度的目的。 易错点是：学习率不是越大越好；也不是模型学到的参数。
- **Adam**：结合动量和自适应学习率思想，为不同参数调整更新幅度。 考试写作时要补一句：常和 SGD、Momentum、RMSProp 对比优化器特点。 易错点是：Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。
- **优化器**：第2节 优化器改进与学习率调整；在解决了梯度消失/爆炸问题后，我们需要关注如何高效地收敛到最优解。学习率的选择直接影响优化效率：；学习率太小：收敛缓慢，训练时间过长；学习率太大：训练不稳定，损失可能发散；可以从两个维度优化学习率： 动量解决了"方向"问题，但所有参数使用统一的学习率，可能出现：；参数... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **RMSProp**：用梯度平方的指数滑动平均调整学习率，缓解 Adagrad 学习率过快衰减。 考试写作时要补一句：常考它和 Adagrad 的区别。 易错点是：不要写成简单固定学习率下降。
- **Momentum**：动量（Momentum）；动量：迫使目标函数沿着"沟壑"快速下降的方法（冲量/“惯性”）；动量法的参数更新过程：；$$\boldsymbol{v}=\gamma \boldsymbol{v}-\alpha；abla_{\boldsymbol{\theta}} J\left(\boldsymbol{\... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **梯度下降**：目标函数的形式通常是狭长且浅的沟壑；在深度网络中，目标函数的局部最优点经常以这种形式呈现，所以标准的随机梯度下降算法通常会在“沟壑”两侧反复震荡 优化器 | 核心思想 | 特点 | 适用场景；SGD | 基础梯度下降 | 简单，可能需要调整学习率 | 大规模数据、最终精调；SGD+Momentum... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **ResNet**：用残差/跳跃连接让网络学习 F(x)+x，缓解深层网络退化和梯度传播困难。 考试写作时要补一句：常考残差连接为什么能训练更深网络。 易错点是：残差连接不是单纯增加层数，而是改变信息和梯度路径。
- **Epoch**：训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。 考试写作时要补一句：常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。 易错点是：Epoch 不是一次参数更新；一次更新通常对应一个 batch。

### 4. 本节自测

- 判断：学习率只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Adam只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：优化器只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：RMSProp只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Momentum只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：梯度下降只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：ResNet只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Epoch只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 学习率决定更新步长。
- Adam = 一阶动量 + 二阶矩估计。
- 第2节 优化器改进与学习率调整；在解决了梯度消失/爆炸问题后，我们需要关注如何高效地收敛到最优解。学习率的选择直接影响优化效率：；学习率太小：收敛缓慢，训练时间过长；学习率太大：训练不稳定，损失可能发散；可以从两个维度优化学习率： 动量解决...
- RMSProp 使用指数滑动平均保存近期梯度尺度。
- 动量（Momentum）；动量：迫使目标函数沿着"沟壑"快速下降的方法（冲量/“惯性”）；动量法的参数更新过程：；$$\boldsymbol{v}=\gamma \boldsymbol{v}-\alpha；abla_{\boldsymbol...
- 目标函数的形式通常是狭长且浅的沟壑；在深度网络中，目标函数的局部最优点经常以这种形式呈现，所以标准的随机梯度下降算法通常会在“沟壑”两侧反复震荡 优化器 | 核心思想 | 特点 | 适用场景；SGD | 基础梯度下降 | 简单，可能需要调整...
- ResNet 核心：y = F(x) + x。
- 1 个 Epoch = 全部训练样本被用过一遍。
- 多个小于 1 的因子连乘，会让梯度趋近 0。
- Stride 控制滑动间隔。

## 第 4 部分：第五章 神经网络训练技巧深度解析 / 第3节 处理过拟合的综合策略

### 1. 本节先看什么

这一节先把 **Dropout、数据增强、卷积、权重衰减** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| Dropout | 训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 | 常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 | Dropout 不是提高模型容量，而是正则化。 | Dropout：训练随机失活，推理正常使用。 |
| 数据增强 | 三、数据增强（Data Augmentation） 除了从模型结构（Dropout）和训练策略（早停）防止过拟合，还可以从数据层面入手——通过数据增强扩充训练集的多样性；$\left\{\boldsymbol{x}^{(n)}, \boldsymbol{t}^{(n)}\right\}$表示原本的训... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 三、数据增强（Data Augmentation） 除了从模型结构（Dropout）和训练策略（早停）防止过拟合，还可以从数据层面入手——通过数据增强扩充训练集的多样性；$\left\{\boldsymbol{x}^{(n)}, \bold... |
| 卷积 | 用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 | 常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 | 不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。 | 输出尺寸：(输入尺寸 + 2P - K) / S + 1。 |
| 权重衰减 | 四、增加权重衰减（Weight Decay） 什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambda \sum_{i} \theta_i^2$$；$L_{lo... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 四、增加权重衰减（Weight Decay） 什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambd... |
| 损失函数 | 把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 | 常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 | 不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。 | 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。 |
| Epoch | 训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。 | 常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。 | Epoch 不是一次参数更新；一次更新通常对应一个 batch。 | 1 个 Epoch = 全部训练样本被用过一遍。 |
| 交叉熵 | 什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambda \sum_{i} \theta_i^2$$；$L_{loss}$：原始损失（如交叉熵损失）；$\lam... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambda \sum_{i} \theta_i^2$$... |

### 3. 像考试答案一样组织语言

- **Dropout**：训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 考试写作时要补一句：常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 易错点是：Dropout 不是提高模型容量，而是正则化。
- **数据增强**：三、数据增强（Data Augmentation） 除了从模型结构（Dropout）和训练策略（早停）防止过拟合，还可以从数据层面入手——通过数据增强扩充训练集的多样性；$\left\{\boldsymbol{x}^{(n)}, \boldsymbol{t}^{(n)}\right\}$表示原本的训... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **卷积**：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。 考试写作时要补一句：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。 易错点是：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- **权重衰减**：四、增加权重衰减（Weight Decay） 什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambda \sum_{i} \theta_i^2$$；$L_{lo... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **损失函数**：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。 考试写作时要补一句：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。 易错点是：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- **Epoch**：训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。 考试写作时要补一句：常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。 易错点是：Epoch 不是一次参数更新；一次更新通常对应一个 batch。
- **交叉熵**：什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambda \sum_{i} \theta_i^2$$；$L_{loss}$：原始损失（如交叉熵损失）；$\lam... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。

### 4. 本节自测

- 判断：Dropout只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：数据增强只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：卷积只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：权重衰减只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：损失函数只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Epoch只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：交叉熵只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- Dropout：训练随机失活，推理正常使用。
- 三、数据增强（Data Augmentation） 除了从模型结构（Dropout）和训练策略（早停）防止过拟合，还可以从数据层面入手——通过数据增强扩充训练集的多样性；$\left\{\boldsymbol{x}^{(n)}, \bold...
- 输出尺寸：(输入尺寸 + 2P - K) / S + 1。
- 四、增加权重衰减（Weight Decay） 什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambd...
- 损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
- 1 个 Epoch = 全部训练样本被用过一遍。
- 什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambda \sum_{i} \theta_i^2$$...

## 第 5 部分：第五章 神经网络训练技巧深度解析 / 第4节 超参数选取实践指南

### 1. 本节先看什么

这一节先把 **学习率、Dropout、权重衰减、Batch** 放到同一条知识链里理解。不要先背细节，先问自己：它在输入、模型结构、训练流程、损失函数或评估里处在哪一步？

### 2. 核心知识点表

| 知识点 | 通俗解释 | 考试怎么考 | 易错点 | 必记句子/公式 |
|---|---|---|---|---|
| 学习率 | 控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 | 常考学习率过大/过小的训练现象，以及学习率调度的目的。 | 学习率不是越大越好；也不是模型学到的参数。 | 学习率决定更新步长。 |
| Dropout | 训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 | 常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 | Dropout 不是提高模型容量，而是正则化。 | Dropout：训练随机失活，推理正常使用。 |
| 权重衰减 | 类别 / 具体参数；模型架构 / 层数、每层的神经元数目；训练参数 / 学习率、批量大小（batch size）；正则化 / 权重衰减率（weight decay）、Dropout率；优化器 / 动量系数、Adam的$\beta$参数 第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步... | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 类别 / 具体参数；模型架构 / 层数、每层的神经元数目；训练参数 / 学习率、批量大小（batch size）；正则化 / 权重衰减率（weight decay）、Dropout率；优化器 / 动量系数、Adam的$\beta$参数 第1... |
| Batch | 一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 | 常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 | Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。 | Batch 是一批样本；Epoch 是全训练集完整训练一遍。 |
| 优化器 | 类别 / 具体参数；模型架构 / 层数、每层的神经元数目；训练参数 / 学习率、批量大小（batch size）；正则化 / 权重衰减率（weight decay）、Dropout率；优化器 / 动量系数、Adam的$\beta$参数 | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 类别 / 具体参数；模型架构 / 层数、每层的神经元数目；训练参数 / 学习率、批量大小（batch size）；正则化 / 权重衰减率（weight decay）、Dropout率；优化器 / 动量系数、Adam的$\beta$参数 |
| Adam | 结合动量和自适应学习率思想，为不同参数调整更新幅度。 | 常和 SGD、Momentum、RMSProp 对比优化器特点。 | Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。 | Adam = 一阶动量 + 二阶矩估计。 |
| 归一化 | 把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 | 常考图像预处理为什么要缩放像素值。 | 归一化不等于 BatchNorm，前者多是输入预处理。 | 图像常先把像素值缩放到 0-1。 |
| Momentum | 第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率 | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率 |
| 超参数 | 💡 经验很重要！多实践、多观察损失曲线，才能找到最佳超参数。 | 会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 | 不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。 | 💡 经验很重要！多实践、多观察损失曲线，才能找到最佳超参数。 |

### 3. 像考试答案一样组织语言

- **学习率**：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。 考试写作时要补一句：常考学习率过大/过小的训练现象，以及学习率调度的目的。 易错点是：学习率不是越大越好；也不是模型学到的参数。
- **Dropout**：训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。 考试写作时要补一句：常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。 易错点是：Dropout 不是提高模型容量，而是正则化。
- **权重衰减**：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数 第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步... 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **Batch**：一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。 考试写作时要补一句：常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。 易错点是：Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。
- **优化器**：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- **Adam**：结合动量和自适应学习率思想，为不同参数调整更新幅度。 考试写作时要补一句：常和 SGD、Momentum、RMSProp 对比优化器特点。 易错点是：Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。
- **归一化**：把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。 考试写作时要补一句：常考图像预处理为什么要缩放像素值。 易错点是：归一化不等于 BatchNorm，前者多是输入预处理。
- **Momentum**：第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率 考试写作时要补一句：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。 易错点是：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。

### 4. 本节自测

- 判断：学习率只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Dropout只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：权重衰减只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Batch只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：优化器只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Adam只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：归一化只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）
- 判断：Momentum只需要背定义，不需要知道它在模型或训练流程中的位置。（答案：错。复习时必须结合“作用 + 位置 + 易错点”。）

### 5. 本节速记

- 学习率决定更新步长。
- Dropout：训练随机失活，推理正常使用。
- 类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数 第1...
- Batch 是一批样本；Epoch 是全训练集完整训练一遍。
- 类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数
- Adam = 一阶动量 + 二阶矩估计。
- 图像常先把像素值缩放到 0-1。
- 第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率
- 💡 经验很重要！多实践、多观察损失曲线，才能找到最佳超参数。

## 四、考前总复盘

考前不要平均用力。优先检查下面这些问题：

1. 每个核心概念能不能用一句话说明“它是什么”。
2. 能不能说出它在模型结构、训练流程或数据处理中的位置。
3. 能不能说出一个最常见的错误说法。
4. 遇到选择题时，能不能判断选项是在混淆概念、夸大作用，还是写反了训练/推理阶段。

## 五、打印建议

<style>@media print { @page { margin: 8mm; } body { font-size: 11pt; line-height: 1.35; } h1, h2, h3 { page-break-after: avoid; } table { font-size: 9pt; border-collapse: collapse; } th, td { padding: 3px 5px; border: 1px solid #ddd; vertical-align: top; } }</style>
