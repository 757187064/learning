<style>
@page { margin: 10mm 9mm; }
@media print {
  body { font-family: "Noto Sans CJK SC", "Microsoft YaHei", Arial, sans-serif; font-size: 10.5pt; line-height: 1.35; }
  h1, h2, h3 { page-break-after: avoid; }
  table { width: 100%; border-collapse: collapse; page-break-inside: avoid; }
  th, td { border: 1px solid #999; padding: 4px 6px; vertical-align: top; }
  .page-break { page-break-before: always; }
  .answer-space { min-height: 4.2em; border: 1px solid #ddd; margin: 6px 0 10px; }
  .short-space { min-height: 2.2em; border: 1px solid #ddd; margin: 6px 0 10px; }
}
</style>

# 课堂练习册：题目

姓名：______________　班级：______________　日期：______________

来源覆盖：`多层感知机2-课堂练习-终版2026.ipynb`、`多层感知机2-课堂练习答案-终版2026.ipynb`、`卷积神经网络1-课堂练习-终版2026.ipynb`，并参考 `database/quiz_bank.json` 中可解释性较好的 MLP、CNN、训练流程相关题目。

说明：本册只包含题目。请把计算步骤写完整，数值题可保留 3 位小数。

## 一、MLP / 分类与损失

### 1. Logit 与 Sigmoid 互相转换

已知：

$$z=\operatorname{logit}(p)=\log\frac{p}{1-p},\qquad \sigma(z)=\frac{1}{1+e^{-z}}$$

完成下表。前三行从概率算 logit，后三行从 logit 算概率。

| 序号 | 已知 | 需要计算 | 计算过程 |
|:---:|:---|:---|:---|
| 1 | $p=0.5$ | $z=$ | |
| 2 | $p=0.8$ | $z=$ | |
| 3 | $p=0.2$ | $z=$ | |
| 4 | $z=0$ | $p=\sigma(z)=$ | |
| 5 | $z=2$ | $p=\sigma(z)=$ | |
| 6 | $z=-1$ | $p=\sigma(z)=$ | |

请用一句话解释：为什么 $p=0.8$ 和 $p=0.2$ 的 logit 数值大小相同、符号相反？

<div class="short-space"></div>

### 2. Softmax 手算与置信度变化

给定三分类 logits：

$$z=[2.0,\ 1.0,\ 0.1]$$

Softmax 定义为：

$$\operatorname{softmax}(z_i)=\frac{e^{z_i}}{\sum_j e^{z_j}}$$

#### 2.1 原始 logits

| 类别 | $z_i$ | $e^{z_i}$ | Softmax 概率 |
|:---:|:---:|:---:|:---:|
| 0 | 2.0 | | |
| 1 | 1.0 | | |
| 2 | 0.1 | | |
| 合计 | | | |

预测类别是：__________，理由是：

<div class="short-space"></div>

#### 2.2 logits 放大 10 倍

现在令 $z'=[20,\ 10,\ 1]$。

| 类别 | $z'_i$ | $e^{z'_i}$ | Softmax 概率 |
|:---:|:---:|:---:|:---:|
| 0 | 20 | | |
| 1 | 10 | | |
| 2 | 1 | | |

与 2.1 相比，概率分布变得更“尖锐”还是更“平缓”？这说明什么？

<div class="answer-space"></div>

#### 2.3 logits 缩小 10 倍

现在令 $z''=[0.2,\ 0.1,\ 0.01]$。

| 类别 | $z''_i$ | $e^{z''_i}$ | Softmax 概率 |
|:---:|:---:|:---:|:---:|
| 0 | 0.2 | | |
| 1 | 0.1 | | |
| 2 | 0.01 | | |

与 2.1 相比，概率分布变得更“尖锐”还是更“平缓”？最大类别是否改变？

<div class="answer-space"></div>

### 3. 分类损失函数判断

下面是三个训练场景。请分别写出更合适的输出形式、损失函数，以及一个常见错误。

| 场景 | 模型最后一层输出 | 推荐损失函数 | 常见错误 |
|:---|:---|:---|:---|
| 十分类手写数字识别，每个样本只属于一个数字 | | | |
| 二分类猫狗识别，每张图片只有猫或狗一个标签 | | | |
| 多标签图片标注，一张图片可以同时包含“人、车、树” | | | |

### 4. MLP 为什么需要激活函数

一个网络由三层全连接层组成：

$$x \rightarrow W_1x+b_1 \rightarrow W_2h_1+b_2 \rightarrow W_3h_2+b_3$$

如果中间没有 ReLU、Sigmoid、Tanh 等非线性激活函数，请说明这个三层网络本质上等价于什么。它对分类边界有什么影响？

<div class="answer-space"></div>

<div class="page-break"></div>

## 二、CNN 感受野与参数量

### 5. 感受野逐层计算

给定网络结构：

| 层号 | 操作 | 核大小 $k$ | 步长 $s$ |
|:---:|:---:|:---:|:---:|
| 1 | 3×3 卷积 | 3 | 1 |
| 2 | 2×2 池化 | 2 | 2 |
| 3 | 3×3 卷积 | 3 | 1 |
| 4 | 2×2 池化 | 2 | 2 |
| 5 | 3×3 卷积 | 3 | 1 |

从输入开始，设初始感受野 $r_0=1$，相邻输出位置在输入上的间隔 $j_0=1$。可用递推：

$$r_l=r_{l-1}+(k_l-1)j_{l-1},\qquad j_l=j_{l-1}s_l$$

完成下表。

| 层号 | 操作 | $r$：该层一个位置看到的输入边长 | $j$：该层相邻位置在输入上的间隔 |
|:---:|:---:|:---:|:---:|
| 输入 | - | 1 | 1 |
| 1 | 3×3 卷积, s=1 | | |
| 2 | 2×2 池化, s=2 | | |
| 3 | 3×3 卷积, s=1 | | |
| 4 | 2×2 池化, s=2 | | |
| 5 | 3×3 卷积, s=1 | | |

第 5 层一个位置最终看到了原始输入的多大区域？

<div class="short-space"></div>

### 6. 多个小卷积核 vs 单个大卷积核

假设输入通道数和输出通道数都为 1，暂不考虑偏置。

方案 A：一个 $19\times19$ 卷积核。  
方案 B：三个 $3\times3$ 卷积层，中间都有非线性激活，池化层无可学习参数。

请比较两个方案的参数量，并从“参数效率”和“表达能力”两个角度说明为什么深层小卷积核常常更受欢迎。

<div class="answer-space"></div>

### 7. MNIST 简易 CNN 尺寸与参数量

输入为 $1\times28\times28$ 的灰度图，类别数为 10。所有卷积均为 Valid，无填充；池化为 $2\times2$、步长 2。

| 层号 | 操作 | 输入尺寸 | 输出尺寸 | 参数量 |
|:---:|:---:|:---:|:---:|:---:|
| 0 | 输入 | - | $1\times28\times28$ | 0 |
| 1 | Conv: $3\times3$, 16 通道 | $1\times28\times28$ | | |
| 2 | ReLU | | | |
| 3 | MaxPool | | | 0 |
| 4 | Conv: $3\times3$, 32 通道 | | | |
| 5 | ReLU | | | |
| 6 | MaxPool | | | 0 |
| 7 | Flatten | | | 0 |
| 8 | Linear: 800 → 128 | | | |
| 9 | ReLU | | | |
| 10 | Linear: 128 → 10 | | | |

卷积参数量：

$$\text{Params}_{conv}=(C_{in}\times k\times k)\times C_{out}+C_{out}$$

全连接参数量：

$$\text{Params}_{fc}=N_{in}\times N_{out}+N_{out}$$

总参数量：________________

### 8. 50,000 参数以内的 CNN 设计

请设计一个更精简的 MNIST CNN，要求：

- 至少 2 个卷积块，每个卷积块包含卷积、激活、池化；
- 最终感受野至少覆盖输入图像的 $20\times20$ 区域；
- 总参数量控制在 50,000 以内；
- 写清楚每层输出尺寸和主要参数量。

<div class="answer-space"></div>
<div class="answer-space"></div>

<div class="page-break"></div>

## 三、训练流程与常见错误

### 9. PyTorch 训练步骤排序

请把以下步骤排成一个标准训练 iteration 的顺序：

A. `loss.backward()`  
B. `optimizer.step()`  
C. `optimizer.zero_grad()`  
D. 前向传播得到 logits  
E. 计算损失  
F. 从 DataLoader 取一个 batch  

正确顺序：________________________________

为什么 `zero_grad()` 不能长期省略？

<div class="short-space"></div>

### 10. 验证阶段代码审查

某同学写验证集评估时做了如下操作：

1. 没有调用 `model.eval()`；
2. 没有使用 `torch.no_grad()`；
3. 用训练集、验证集、测试集一起计算图像标准化的均值和标准差；
4. 验证集 loss 下降后没有保存当前最优模型，只保留最后一个 epoch 的模型。

请逐条说明这些做法可能造成什么问题。

<div class="answer-space"></div>
<div class="answer-space"></div>

### 11. 训练异常诊断

请根据现象写出两个可能原因和一个可尝试的处理办法。

| 现象 | 可能原因 1 | 可能原因 2 | 可尝试处理 |
|:---|:---|:---|:---|
| loss 震荡很大，偶尔变成 NaN | | | |
| 训练 loss 很慢下降，几乎不动 | | | |
| 训练集准确率高，验证集准确率低 | | | |
| 多分类训练时准确率异常差，但代码没有报错 | | | |

### 12. 综合说明题

请用一段话串联以下概念：logits、Softmax、交叉熵、反向传播、学习率、参数更新。要求说明它们在一次训练迭代中的先后关系。

<div class="answer-space"></div>
