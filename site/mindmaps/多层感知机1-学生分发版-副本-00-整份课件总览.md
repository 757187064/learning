# 多层感知机1_学生分发版_副本 / 总览
## 第二章 神经网络训练流程 / 第一节 梯度下降法
### 梯度下降
- 通俗解释：1. 梯度下降法的定义；[ ] Gradient descent 的基本定义；是一种无约束数学优化的方法，是一种一阶迭代优化算法，其用于寻找可微函数的局部最小值；是一种迭代技术，试图从初始猜测开始，为给定模型，数据点，和损失函数找到的最佳可能的参数；[ ] 无约束数学优化 vs 有约束数学优化（SV...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：1. 梯度下降法的定义；[ ] Gradient descent 的基本定义；是一种无约束数学优化的方法，是一种一阶迭代优化算法，其用于寻找可微函数的局部最小值；是一种迭代技术，试图从初始猜测开始，为给定模型，数据点，和损失函数找到的最佳可...
### Epoch
- 通俗解释：训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。
- 考试怎么考：常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。
- 易错点：Epoch 不是一次参数更新；一次更新通常对应一个 batch。
- 必记：1 个 Epoch = 全部训练样本被用过一遍。
### Batch
- 通俗解释：一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。
- 考试怎么考：常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。
- 易错点：Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。
- 必记：Batch 是一批样本；Epoch 是全训练集完整训练一遍。
### 损失函数
- 通俗解释：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。
- 考试怎么考：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。
- 易错点：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- 必记：损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
### 反向传播
- 通俗解释：1. 梯度下降法的定义；[ ] Gradient descent 的基本定义；是一种无约束数学优化的方法，是一种一阶迭代优化算法，其用于寻找可微函数的局部最小值；是一种迭代技术，试图从初始猜测开始，为给定模型，数据点，和损失函数找到的最佳可能的参数；[ ] 无约束数学优化 vs 有约束数学优化（SV...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：1. 梯度下降法的定义；[ ] Gradient descent 的基本定义；是一种无约束数学优化的方法，是一种一阶迭代优化算法，其用于寻找可微函数的局部最小值；是一种迭代技术，试图从初始猜测开始，为给定模型，数据点，和损失函数找到的最佳可...
### 学习率
- 通俗解释：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。
- 考试怎么考：常考学习率过大/过小的训练现象，以及学习率调度的目的。
- 易错点：学习率不是越大越好；也不是模型学到的参数。
- 必记：学习率决定更新步长。
### 超参数
- 通俗解释：Step 4: 更新参数；使用梯度去更新参数，需要考虑超参数：学习率；参数更新公式：数据减去学习率乘以梯度得到新数据
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：Step 4: 更新参数；使用梯度去更新参数，需要考虑超参数：学习率；参数更新公式：数据减去学习率乘以梯度得到新数据
## 第二章 神经网络训练流程 / 第二节 模型训练流程的优化
### 自动微分
- 通俗解释：1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（Tensor）的概念 深度学习中为什么要使用GPU；张量 维...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（...
### 损失函数
- 通俗解释：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。
- 考试怎么考：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。
- 易错点：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- 必记：损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
### Batch
- 通俗解释：一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。
- 考试怎么考：常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。
- 易错点：Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。
- 必记：Batch 是一批样本；Epoch 是全训练集完整训练一遍。
### 反向传播
- 通俗解释：1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（Tensor）的概念
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（...
### 计算图
- 通俗解释：1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（Tensor）的概念
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（...
### 优化器
- 通俗解释：1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（Tensor）的概念
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：1. 使用PyTorch进行模型训练的基本步骤；[ ] 数据准备：从Numpy到PyTorch Tensor的转换；[ ] 模型配置：模型、损失函数、优化器；[ ] 训练循环：前向传播 → 计算损失 → 反向传播 → 更新参数；2. 张量（...
## 第二章 神经网络训练流程 / 第三节 自动微分和计算图
### 梯度下降
- 通俗解释：1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 神经网络的训练本质是优化问题：找到一组权重（Weights），使得损失函数（Loss）最小。；梯度指...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 神经网络的训练本质是优化问题：找到一组权...
### 损失函数
- 通俗解释：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。
- 考试怎么考：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。
- 易错点：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- 必记：损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
### 反向传播
- 通俗解释：1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 作业3:深度学习框架的自动微分实现；参考资料：/Users/pandamas/work/03深度学习...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 作业3:深度学习框架的自动微分实现；参考...
### 计算图
- 通俗解释：1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 作业3:深度学习框架的自动微分实现；参考资料：/Users/pandamas/work/03深度学习...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：1. 反向传播的核心目的；[ ] 神经网络训练本质：找到使损失函数最小的权重；[ ] 梯度指向损失函数上升最快的方向；[ ] 沿梯度反方向更新权重（梯度下降）；2. 链式法则（Chain Rule） 作业3:深度学习框架的自动微分实现；参考...
### 自动微分
- 通俗解释：作业3:深度学习框架的自动微分实现；参考资料：/Users/pandamas/work/03深度学习讲习班/参考资料/Build Framework/part01.ipynb；本作业旨在通过阅读简化版自动微分框架的实现代码，深入理解深度学习框架中自动微分的核心原理。；请仔细阅读参考资料 notebo...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：作业3:深度学习框架的自动微分实现；参考资料：/Users/pandamas/work/03深度学习讲习班/参考资料/Build Framework/part01.ipynb；本作业旨在通过阅读简化版自动微分框架的实现代码，深入理解深度学习...
## 第二章 神经网络训练流程 / 第四节 构建神经网络训练代码
### 优化器
- 通俗解释：1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer.zero_grad()；2. 常见优化算法 至此，我们已经...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer...
### 损失函数
- 通俗解释：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。
- 考试怎么考：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。
- 易错点：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- 必记：损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
### Dropout
- 通俗解释：训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。
- 考试怎么考：常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。
- 易错点：Dropout 不是提高模型容量，而是正则化。
- 必记：Dropout：训练随机失活，推理正常使用。
### 梯度下降
- 通俗解释：1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer.zero_grad()；2. 常见优化算法
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer...
### 学习率
- 通俗解释：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。
- 考试怎么考：常考学习率过大/过小的训练现象，以及学习率调度的目的。
- 易错点：学习率不是越大越好；也不是模型学到的参数。
- 必记：学习率决定更新步长。
### CrossEntropyLoss
- 通俗解释：1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer.zero_grad()；2. 常见优化算法
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer...
### Xavier
- 通俗解释：1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer.zero_grad()；2. 常见优化算法
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer...
### BatchNorm
- 通俗解释：在小批量上标准化中间激活，再用可学习参数恢复表达能力，帮助训练更稳定。
- 考试怎么考：常考训练/推理阶段统计量不同，以及放在卷积/全连接和激活附近。
- 易错点：推理阶段一般使用训练中累计的均值方差，不依赖当前 batch。
- 必记：BatchNorm：标准化激活，稳定训练，加快收敛。
### 权重衰减
- 通俗解释：1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer.zero_grad()；2. 常见优化算法
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：1. 优化器（Optimizer）；[ ] 优化器 的定义：用于更新和调整神经网络权重的算法；[ ] 优化器的作用：计算梯度 → 引导参数向损失减小方向更新；[ ] 优化器的使用模式：optimizer.step() + optimizer...
### RMSProp
- 通俗解释：用梯度平方的指数滑动平均调整学习率，缓解 Adagrad 学习率过快衰减。
- 考试怎么考：常考它和 Adagrad 的区别。
- 易错点：不要写成简单固定学习率下降。
- 必记：RMSProp 使用指数滑动平均保存近期梯度尺度。
