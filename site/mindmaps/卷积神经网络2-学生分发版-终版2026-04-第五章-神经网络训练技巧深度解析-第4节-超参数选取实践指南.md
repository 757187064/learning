# 卷积神经网络2-学生分发版-终版2026 / 第五章 神经网络训练技巧深度解析 / 第4节 超参数选取实践指南
## 第五章 神经网络训练技巧深度解析 / 第4节 超参数选取实践指南
### 学习率
- 通俗解释：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数 第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数 第1...
### Dropout
- 通俗解释：训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。
- 考试怎么考：常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。
- 易错点：Dropout 不是提高模型容量，而是正则化。
- 必记：Dropout：训练随机失活，推理正常使用。
### 权重衰减
- 通俗解释：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数 第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数 第1...
### Batch
- 通俗解释：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数
### 优化器
- 通俗解释：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数
### Adam
- 通俗解释：结合动量和自适应学习率思想，为不同参数调整更新幅度。
- 考试怎么考：常和 SGD、Momentum、RMSProp 对比优化器特点。
- 易错点：Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。
- 必记：Adam = 一阶动量 + 二阶矩估计。
### 归一化
- 通俗解释：把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。
- 考试怎么考：常考图像预处理为什么要缩放像素值。
- 易错点：归一化不等于 BatchNorm，前者多是输入预处理。
- 必记：图像常先把像素值缩放到 0-1。
### Xavier
- 通俗解释：第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率
### 早停
- 通俗解释：第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率
### Momentum
- 通俗解释：第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率
