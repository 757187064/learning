# 循环神经网络-学生分发版-终版2026 / 总览
## 第五章 循环神经网络
### 卷积
- 通俗解释：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。
- 考试怎么考：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。
- 易错点：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- 必记：输出尺寸：(输入尺寸 + 2P - K) / S + 1。
### GRU
- 通俗解释：用更新门和重置门控制历史信息保留与遗忘，是简化版门控循环网络。
- 考试怎么考：常和 LSTM 对比门结构和参数量。
- 易错点：GRU 没有单独的细胞状态 c_t。
- 必记：GRU：更新门 + 重置门。
### LSTM
- 通俗解释：通过输入门、遗忘门、输出门和细胞状态保存长期信息。
- 考试怎么考：常考门控作用，以及为什么能缓解长序列梯度问题。
- 易错点：不要漏掉细胞状态是 LSTM 的关键通道。
- 必记：LSTM：输入门、遗忘门、输出门、细胞状态。
### Padding
- 通俗解释：1. 序列数据与循环神经网络；2. 门控循环单元（GRU）；3. 长短时记忆网络（LSTM）；4. 变长序列处理：Padding与Packing；5. 1D卷积在序列数据上的应用
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：1. 序列数据与循环神经网络；2. 门控循环单元（GRU）；3. 长短时记忆网络（LSTM）；4. 变长序列处理：Padding与Packing；5. 1D卷积在序列数据上的应用
### Packing
- 通俗解释：1. 序列数据与循环神经网络；2. 门控循环单元（GRU）；3. 长短时记忆网络（LSTM）；4. 变长序列处理：Padding与Packing；5. 1D卷积在序列数据上的应用
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：1. 序列数据与循环神经网络；2. 门控循环单元（GRU）；3. 长短时记忆网络（LSTM）；4. 变长序列处理：Padding与Packing；5. 1D卷积在序列数据上的应用
### 1D卷积
- 通俗解释：1. 序列数据与循环神经网络；2. 门控循环单元（GRU）；3. 长短时记忆网络（LSTM）；4. 变长序列处理：Padding与Packing；5. 1D卷积在序列数据上的应用
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：1. 序列数据与循环神经网络；2. 门控循环单元（GRU）；3. 长短时记忆网络（LSTM）；4. 变长序列处理：Padding与Packing；5. 1D卷积在序列数据上的应用
## 第五章 循环神经网络 / 第一节 序列数据与循环神经网络
### RNN
- 通俗解释：按时间步处理序列，用隐藏状态把前面信息传到后面。
- 考试怎么考：常考隐藏状态、序列建模、梯度消失/爆炸。
- 易错点：RNN 不是一次性把所有时间步完全独立处理。
- 必记：当前输出依赖当前输入和上一时刻隐藏状态。
### 隐藏状态
- 通俗解释：RNN的循环连接；RNN通过引入隐藏状态（Hidden State）来传递序列信息；参数共享：同一组参数处理所有时间步；记忆能力：隐藏状态保存历史信息；变长处理：可处理任意长度的序列 循环神经网络的核心目标：就是产生一个能够最好地表示一个序列的隐藏状态；1. "隐藏状态"= 记忆的压缩表示；2. "...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：RNN的循环连接；RNN通过引入隐藏状态（Hidden State）来传递序列信息；参数共享：同一组参数处理所有时间步；记忆能力：隐藏状态保存历史信息；变长处理：可处理任意长度的序列 循环神经网络的核心目标：就是产生一个能够最好地表示一个序...
### 双向RNN
- 通俗解释：RNN的输入输出Shape详解；⚠️ 形状是最容易混淆的地方！；关键对比：DataLoader输出 vs RNN输入；来源 | 默认Shape | 说明 | 冲突点；DataLoader | (N, L, F) | batch-first，符合直觉 | RNN默认不匹配！ 对比项 | batch_f...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：RNN的输入输出Shape详解；⚠️ 形状是最容易混淆的地方！；关键对比：DataLoader输出 vs RNN输入；来源 | 默认Shape | 说明 | 冲突点；DataLoader | (N, L, F) | batch-first...
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
### Batch
- 通俗解释：RNN Layer vs RNN Cell：两种使用方式；PyTorch 提供了两种 RNN 接口，适用于不同场景：；特性 | nn.RNNCell | nn.RNN；处理粒度 | 单个时间步 | 整个序列；使用方式 | 手动循环 | 自动处理 RNN的输入输出Shape详解；⚠️ 形状是最容易混淆...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：RNN Layer vs RNN Cell：两种使用方式；PyTorch 提供了两种 RNN 接口，适用于不同场景：；特性 | nn.RNNCell | nn.RNN；处理粒度 | 单个时间步 | 整个序列；使用方式 | 手动循环 | 自动...
### 梯度消失
- 通俗解释：堆叠RNN的原理；堆叠RNN：将多个RNN层堆叠在一起，前一层的输出作为后一层的输入。；两层堆叠RNN已经计算代价很高（时间上的顺序依赖 + 层间的顺序依赖）；输出是最后一层的隐藏状态序列；每层的最终隐藏状态会被保存 步骤5：引入范数上界——量化风险；为了判断连乘是否会消失或爆炸，我们引入范数上界来...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：堆叠RNN的原理；堆叠RNN：将多个RNN层堆叠在一起，前一层的输出作为后一层的输入。；两层堆叠RNN已经计算代价很高（时间上的顺序依赖 + 层间的顺序依赖）；输出是最后一层的隐藏状态序列；每层的最终隐藏状态会被保存 步骤5：引入范数上界—...
### 堆叠RNN
- 通俗解释：堆叠RNN的原理；堆叠RNN：将多个RNN层堆叠在一起，前一层的输出作为后一层的输入。；两层堆叠RNN已经计算代价很高（时间上的顺序依赖 + 层间的顺序依赖）；输出是最后一层的隐藏状态序列；每层的最终隐藏状态会被保存 深度双向循环神经网络；将堆叠RNN与双向RNN结合，形成深度双向RNN；普通LST...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：堆叠RNN的原理；堆叠RNN：将多个RNN层堆叠在一起，前一层的输出作为后一层的输入。；两层堆叠RNN已经计算代价很高（时间上的顺序依赖 + 层间的顺序依赖）；输出是最后一层的隐藏状态序列；每层的最终隐藏状态会被保存 深度双向循环神经网络...
### 损失函数
- 通俗解释：步骤1：建立核心直觉；梯度 = 过去的信息对现在输出的影响；步骤2：提出问题；RNN在每个时间步都使用同一组权重 $W_{hh}$ 和 $W_{xh}$。现在我们要算：损失函数对 $W_{hh}$ 的导数是多少？ 步骤3：链式法则展开；根据链式法则，损失函数对 $W_{hh}$ 的梯度可以写成：；$...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：步骤1：建立核心直觉；梯度 = 过去的信息对现在输出的影响；步骤2：提出问题；RNN在每个时间步都使用同一组权重 $W_{hh}$ 和 $W_{xh}$。现在我们要算：损失函数对 $W_{hh}$ 的导数是多少？ 步骤3：链式法则展开；根据...
### MLP
- 通俗解释：为什么 MLP/CNN 不擅长序列建模？；核心矛盾：结构设计与序列特性的不匹配；1. 固定输入维度的限制；MLP：输入层神经元数量固定 → 无法直接处理变长序列；CNN：需要固定大小的张量 → 变长序列需 padding/truncate
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：为什么 MLP/CNN 不擅长序列建模？；核心矛盾：结构设计与序列特性的不匹配；1. 固定输入维度的限制；MLP：输入层神经元数量固定 → 无法直接处理变长序列；CNN：需要固定大小的张量 → 变长序列需 padding/truncate
### 卷积
- 通俗解释：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。
- 考试怎么考：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。
- 易错点：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- 必记：输出尺寸：(输入尺寸 + 2P - K) / S + 1。
## 第五章 循环神经网络 / 第二节 门控循环单元（GRU）
### GRU
- 通俗解释：用更新门和重置门控制历史信息保留与遗忘，是简化版门控循环网络。
- 考试怎么考：常和 LSTM 对比门结构和参数量。
- 易错点：GRU 没有单独的细胞状态 c_t。
- 必记：GRU：更新门 + 重置门。
### Sigmoid
- 通俗解释：一、GRU的设计目标；GRU（Gated Recurrent Unit）由Cho等人于2014年提出；核心思想：引入门控机制来控制信息的流动；组件 | 功能 | 激活函数；重置门（Reset Gate） | 控制过去信息的遗忘程度 | Sigmoid
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：一、GRU的设计目标；GRU（Gated Recurrent Unit）由Cho等人于2014年提出；核心思想：引入门控机制来控制信息的流动；组件 | 功能 | 激活函数；重置门（Reset Gate） | 控制过去信息的遗忘程度 | Si...
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
### GAT
- 通俗解释：图注意力网络，给不同邻居分配不同注意力权重再聚合。
- 考试怎么考：常和 GCN 对比：GCN 多按固定归一化聚合，GAT 学习邻居重要性。
- 易错点：GAT 的重点是邻居权重可学习，不是所有邻居同等重要。
- 必记：GAT：用注意力学习邻居权重。
### 隐藏状态
- 通俗解释：二、GRU的数学表示；1. 重置门（控制保留多少过去的信息），更新门（控制新信息对当前状态的贡献）；3. 候选隐藏状态：计算新的候选状态；$$h_{\mathrm{new}}=n=\tanh \left(r * t_{h}+t_{x}\right)$$；4. 最终隐藏状态：新旧状态的加权平均
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：二、GRU的数学表示；1. 重置门（控制保留多少过去的信息），更新门（控制新信息对当前状态的贡献）；3. 候选隐藏状态：计算新的候选状态；$$h_{\mathrm{new}}=n=\tanh \left(r * t_{h}+t_{x}\ri...
## 第五章 循环神经网络 / 第三节 长短时记忆网络（LSTM）
### LSTM
- 通俗解释：通过输入门、遗忘门、输出门和细胞状态保存长期信息。
- 考试怎么考：常考门控作用，以及为什么能缓解长序列梯度问题。
- 易错点：不要漏掉细胞状态是 LSTM 的关键通道。
- 必记：LSTM：输入门、遗忘门、输出门、细胞状态。
### RNN
- 通俗解释：按时间步处理序列，用隐藏状态把前面信息传到后面。
- 考试怎么考：常考隐藏状态、序列建模、梯度消失/爆炸。
- 易错点：RNN 不是一次性把所有时间步完全独立处理。
- 必记：当前输出依赖当前输入和上一时刻隐藏状态。
### Transformer
- 通俗解释：以注意力为核心的序列模型，用多头注意力、前馈网络、残差和归一化堆叠编码器/解码器。
- 考试怎么考：常考编码器/解码器结构、多头注意力和位置编码。
- 易错点：Transformer 不依赖 RNN 的逐步递归来建模序列。
- 必记：Transformer 核心：Attention + FFN + 残差 + Norm + 位置编码。
### Sigmoid
- 通俗解释：LSTM的核心组件；LSTM使用两个状态和三个门控：；Hidden State ($h_t$)：短期记忆，与RNN相同；Cell State ($C_t$)：长期记忆，贯穿整个链条；门控 | 功能 | 激活函数
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：LSTM的核心组件；LSTM使用两个状态和三个门控：；Hidden State ($h_t$)：短期记忆，与RNN相同；Cell State ($C_t$)：长期记忆，贯穿整个链条；门控 | 功能 | 激活函数
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
### GAT
- 通俗解释：图注意力网络，给不同邻居分配不同注意力权重再聚合。
- 考试怎么考：常和 GCN 对比：GCN 多按固定归一化聚合，GAT 学习邻居重要性。
- 易错点：GAT 的重点是邻居权重可学习，不是所有邻居同等重要。
- 必记：GAT：用注意力学习邻居权重。
### 隐藏状态
- 通俗解释：三、LSTM的数学表示；1. 遗忘门："要忘记多少过去？"；$$f_t = \sigma(W_f \cdot [h_{t-1}, x_t] + b_f)$$；2. 输入门："要添加多少新信息？"；$$i_t = \sigma(W_i \cdot [h_{t-1}, x_t] + b_i)$$
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：三、LSTM的数学表示；1. 遗忘门："要忘记多少过去？"；$$f_t = \sigma(W_f \cdot [h_{t-1}, x_t] + b_f)$$；2. 输入门："要添加多少新信息？"；$$i_t = \sigma(W_i \cd...
### 梯度消失
- 通俗解释：特性 | RNN | GRU | LSTM；状态数量 | 1 | 1 | 2 (h + C)；门控数量 | 0 | 2 | 3；参数量 | 少 | 中等 | 多；长期记忆 | 弱 | 中等 | 强
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：特性 | RNN | GRU | LSTM；状态数量 | 1 | 1 | 2 (h + C)；门控数量 | 0 | 2 | 3；参数量 | 少 | 中等 | 多；长期记忆 | 弱 | 中等 | 强
### GRU
- 通俗解释：用更新门和重置门控制历史信息保留与遗忘，是简化版门控循环网络。
- 考试怎么考：常和 LSTM 对比门结构和参数量。
- 易错点：GRU 没有单独的细胞状态 c_t。
- 必记：GRU：更新门 + 重置门。
## 第五章 循环神经网络 / 第四节 变长序列处理：Padding与Packing
### Padding
- 通俗解释：第四节 变长序列处理：Padding与Packing 在实际应用中，序列数据的长度往往不一致。；问题：PyTorch无法直接创建不同长度的张量；1. Padding：用零填充到相同长度；2. Packing：打包序列，避免填充带来的问题 Padding原理；Padding意味着用零填充序列，使所有序...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：第四节 变长序列处理：Padding与Packing 在实际应用中，序列数据的长度往往不一致。；问题：PyTorch无法直接创建不同长度的张量；1. Padding：用零填充到相同长度；2. Packing：打包序列，避免填充带来的问题 P...
### Packing
- 通俗解释：第四节 变长序列处理：Padding与Packing 在实际应用中，序列数据的长度往往不一致。；问题：PyTorch无法直接创建不同长度的张量；1. Padding：用零填充到相同长度；2. Packing：打包序列，避免填充带来的问题 Packing原理；Packing将序列拼接在一起，记录每个序...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：第四节 变长序列处理：Padding与Packing 在实际应用中，序列数据的长度往往不一致。；问题：PyTorch无法直接创建不同长度的张量；1. Padding：用零填充到相同长度；2. Packing：打包序列，避免填充带来的问题 P...
### 填充
- 通俗解释：在输入边界补值，让卷积能处理边缘或保持输出尺寸。
- 考试怎么考：常考 Same 卷积为何需要 padding。
- 易错点：填充不是增加有效信息，而是控制边界和尺寸。
- 必记：Padding 用于控制输出尺寸和边界信息。
### RNN
- 通俗解释：按时间步处理序列，用隐藏状态把前面信息传到后面。
- 考试怎么考：常考隐藏状态、序列建模、梯度消失/爆炸。
- 易错点：RNN 不是一次性把所有时间步完全独立处理。
- 必记：当前输出依赖当前输入和上一时刻隐藏状态。
### 隐藏状态
- 通俗解释：Padding原理；Padding意味着用零填充序列，使所有序列达到相同长度。；Padding的问题：；填充的零值会影响RNN的隐藏状态！即使输入是零，RNN的变换和偏置仍会产生非零输出。
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：Padding原理；Padding意味着用零填充序列，使所有序列达到相同长度。；Padding的问题：；填充的零值会影响RNN的隐藏状态！即使输入是零，RNN的变换和偏置仍会产生非零输出。
### Batch
- 通俗解释：PackedSequence 的 Shape 详解；PackedSequence 的"时间步"：参差不齐；PackedSequence 不是规则张量，它的 data 把所有有效 token 按时间步拼接成一维（或二维）张量。；time维度是什么意思？；在 PackedSequence 中，"time...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：PackedSequence 的 Shape 详解；PackedSequence 的"时间步"：参差不齐；PackedSequence 不是规则张量，它的 data 把所有有效 token 按时间步拼接成一维（或二维）张量。；time维度是...
### PackedSequence
- 通俗解释：PackedSequence 的 Shape 详解；PackedSequence 的"时间步"：参差不齐；PackedSequence 不是规则张量，它的 data 把所有有效 token 按时间步拼接成一维（或二维）张量。；time维度是什么意思？；在 PackedSequence 中，"time...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：PackedSequence 的 Shape 详解；PackedSequence 的"时间步"：参差不齐；PackedSequence 不是规则张量，它的 data 把所有有效 token 按时间步拼接成一维（或二维）张量。；time维度是...
## 第五章 循环神经网络 / 第五节 1D卷积在序列数据上的应用
### 卷积
- 通俗解释：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。
- 考试怎么考：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。
- 易错点：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- 必记：输出尺寸：(输入尺寸 + 2P - K) / S + 1。
### 感受野
- 通俗解释：某一层一个神经元能看到原始输入图像的区域大小。
- 考试怎么考：常考层数、卷积核大小、步长如何影响感受野。
- 易错点：步长变大通常会让前一层对应感受野扩大，不是缩小。
- 必记：层越深，感受野通常越大；多层小卷积能逐步扩大感受野。
### 1D卷积
- 通俗解释：一、1D卷积基础；什么是1D卷积？；1D卷积与2D卷积类似，但滤波器只在一个维度（时间/序列维度）上滑动。；1D卷积 vs 2D卷积对比；特性 | 1D卷积 | 2D卷积 二、1D卷积 vs RNN：如何选择？；维度 | 1D卷积 | RNN/LSTM/GRU；并行性 | ✅ 完全并行 | ❌ 时序...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：一、1D卷积基础；什么是1D卷积？；1D卷积与2D卷积类似，但滤波器只在一个维度（时间/序列维度）上滑动。；1D卷积 vs 2D卷积对比；特性 | 1D卷积 | 2D卷积 二、1D卷积 vs RNN：如何选择？；维度 | 1D卷积 | RN...
### RNN
- 通俗解释：按时间步处理序列，用隐藏状态把前面信息传到后面。
- 考试怎么考：常考隐藏状态、序列建模、梯度消失/爆炸。
- 易错点：RNN 不是一次性把所有时间步完全独立处理。
- 必记：当前输出依赖当前输入和上一时刻隐藏状态。
### GRU
- 通俗解释：用更新门和重置门控制历史信息保留与遗忘，是简化版门控循环网络。
- 考试怎么考：常和 LSTM 对比门结构和参数量。
- 易错点：GRU 没有单独的细胞状态 c_t。
- 必记：GRU：更新门 + 重置门。
### Padding
- 通俗解释：二、1D卷积 vs RNN：如何选择？；维度 | 1D卷积 | RNN/LSTM/GRU；并行性 | ✅ 完全并行 | ❌ 时序依赖，无法并行；长程依赖 | ❌ 感受野有限（需深层） | ⚠️ LSTM/GRU 有限缓解；位置敏感性 | ✅ 局部模式强 | ✅ 顺序敏感 三、TCN：时序卷积网络；为...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：二、1D卷积 vs RNN：如何选择？；维度 | 1D卷积 | RNN/LSTM/GRU；并行性 | ✅ 完全并行 | ❌ 时序依赖，无法并行；长程依赖 | ❌ 感受野有限（需深层） | ⚠️ LSTM/GRU 有限缓解；位置敏感性 | ✅...
### TCN
- 通俗解释：三、TCN：时序卷积网络；为什么需要TCN？；传统1D卷积感受野有限，难以捕捉长程依赖。TCN（Temporal Convolutional Network）通过三种技术解决这个问题：；技术 | 解决的问题 | 原理；因果卷积 | 信息泄漏（偷看未来） | 只依赖历史信息，左边padding 四、总...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：三、TCN：时序卷积网络；为什么需要TCN？；传统1D卷积感受野有限，难以捕捉长程依赖。TCN（Temporal Convolutional Network）通过三种技术解决这个问题：；技术 | 解决的问题 | 原理；因果卷积 | 信息泄漏...
### 卷积核
- 通俗解释：卷积层中可学习的小矩阵/滤波器，用来扫描局部区域并提取特征。
- 考试怎么考：常考卷积核大小、通道数、参数共享和输出尺寸计算。
- 易错点：卷积核参数会训练更新，不是手工固定模板。
- 必记：卷积核大小 K、步长 S、填充 P 共同决定输出尺寸。
### LSTM
- 通俗解释：通过输入门、遗忘门、输出门和细胞状态保存长期信息。
- 考试怎么考：常考门控作用，以及为什么能缓解长序列梯度问题。
- 易错点：不要漏掉细胞状态是 LSTM 的关键通道。
- 必记：LSTM：输入门、遗忘门、输出门、细胞状态。
### Packing
- 通俗解释：二、1D卷积 vs RNN：如何选择？；维度 | 1D卷积 | RNN/LSTM/GRU；并行性 | ✅ 完全并行 | ❌ 时序依赖，无法并行；长程依赖 | ❌ 感受野有限（需深层） | ⚠️ LSTM/GRU 有限缓解；位置敏感性 | ✅ 局部模式强 | ✅ 顺序敏感
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：二、1D卷积 vs RNN：如何选择？；维度 | 1D卷积 | RNN/LSTM/GRU；并行性 | ✅ 完全并行 | ❌ 时序依赖，无法并行；长程依赖 | ❌ 感受野有限（需深层） | ⚠️ LSTM/GRU 有限缓解；位置敏感性 | ✅...
## 第五章 循环神经网络 / 第六节 PyTorch实现综合案例
### Dropout
- 通俗解释：训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。
- 考试怎么考：常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。
- 易错点：Dropout 不是提高模型容量，而是正则化。
- 必记：Dropout：训练随机失活，推理正常使用。
### LSTM
- 通俗解释：通过输入门、遗忘门、输出门和细胞状态保存长期信息。
- 考试怎么考：常考门控作用，以及为什么能缓解长序列梯度问题。
- 易错点：不要漏掉细胞状态是 LSTM 的关键通道。
- 必记：LSTM：输入门、遗忘门、输出门、细胞状态。
### 学习率
- 通俗解释：技巧 | 实现代码 | 作用 | 原理；梯度裁剪 | clip_grad_norm_(..., max_norm=5) | 防止梯度爆炸 | 若梯度范数 > 5，则按比例缩小，保持方向不变；L2正则化 | Adam(..., weight_decay=1e-5) | 防止过拟合 | 在损失函数中增加...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：技巧 | 实现代码 | 作用 | 原理；梯度裁剪 | clip_grad_norm_(..., max_norm=5) | 防止梯度爆炸 | 若梯度范数 > 5，则按比例缩小，保持方向不变；L2正则化 | Adam(..., weight_...
### 梯度爆炸
- 通俗解释：技巧 | 实现代码 | 作用 | 原理；梯度裁剪 | clip_grad_norm_(..., max_norm=5) | 防止梯度爆炸 | 若梯度范数 > 5，则按比例缩小，保持方向不变；L2正则化 | Adam(..., weight_decay=1e-5) | 防止过拟合 | 在损失函数中增加...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：技巧 | 实现代码 | 作用 | 原理；梯度裁剪 | clip_grad_norm_(..., max_norm=5) | 防止梯度爆炸 | 若梯度范数 > 5，则按比例缩小，保持方向不变；L2正则化 | Adam(..., weight_...
### 梯度裁剪
- 通俗解释：技巧 | 实现代码 | 作用 | 原理；梯度裁剪 | clip_grad_norm_(..., max_norm=5) | 防止梯度爆炸 | 若梯度范数 > 5，则按比例缩小，保持方向不变；L2正则化 | Adam(..., weight_decay=1e-5) | 防止过拟合 | 在损失函数中增加...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：技巧 | 实现代码 | 作用 | 原理；梯度裁剪 | clip_grad_norm_(..., max_norm=5) | 防止梯度爆炸 | 若梯度范数 > 5，则按比例缩小，保持方向不变；L2正则化 | Adam(..., weight_...
### 早停
- 通俗解释：技巧 | 实现代码 | 作用 | 原理；梯度裁剪 | clip_grad_norm_(..., max_norm=5) | 防止梯度爆炸 | 若梯度范数 > 5，则按比例缩小，保持方向不变；L2正则化 | Adam(..., weight_decay=1e-5) | 防止过拟合 | 在损失函数中增加...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：技巧 | 实现代码 | 作用 | 原理；梯度裁剪 | clip_grad_norm_(..., max_norm=5) | 防止梯度爆炸 | 若梯度范数 > 5，则按比例缩小，保持方向不变；L2正则化 | Adam(..., weight_...
### Adam
- 通俗解释：结合动量和自适应学习率思想，为不同参数调整更新幅度。
- 考试怎么考：常和 SGD、Momentum、RMSProp 对比优化器特点。
- 易错点：Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。
- 必记：Adam = 一阶动量 + 二阶矩估计。
### ReduceLROnPlateau
- 通俗解释：技巧 | 实现代码 | 作用 | 原理；梯度裁剪 | clip_grad_norm_(..., max_norm=5) | 防止梯度爆炸 | 若梯度范数 > 5，则按比例缩小，保持方向不变；L2正则化 | Adam(..., weight_decay=1e-5) | 防止过拟合 | 在损失函数中增加...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：技巧 | 实现代码 | 作用 | 原理；梯度裁剪 | clip_grad_norm_(..., max_norm=5) | 防止梯度爆炸 | 若梯度范数 > 5，则按比例缩小，保持方向不变；L2正则化 | Adam(..., weight_...
### GAT
- 通俗解释：图注意力网络，给不同邻居分配不同注意力权重再聚合。
- 考试怎么考：常和 GCN 对比：GCN 多按固定归一化聚合，GAT 学习邻居重要性。
- 易错点：GAT 的重点是邻居权重可学习，不是所有邻居同等重要。
- 必记：GAT：用注意力学习邻居权重。
### Batch
- 通俗解释：二、模型定义：SentimentLSTM；输入序列 (PackedSequence, 变长)；Embedding层 (vocab_size → embed_dim)；单层单向LSTM (embed_dim → hidden_dim)；取最后时刻隐藏状态 h_T
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：二、模型定义：SentimentLSTM；输入序列 (PackedSequence, 变长)；Embedding层 (vocab_size → embed_dim)；单层单向LSTM (embed_dim → hidden_dim)；取最后...
