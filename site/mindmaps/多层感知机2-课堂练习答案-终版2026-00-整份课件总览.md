# 多层感知机2-课堂练习答案-终版2026 / 总览
## 多层感知机 - 课堂练习答案
### Softmax
- 通俗解释：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。
- 考试怎么考：常考多分类输出、注意力权重为什么能加权求和。
- 易错点：Softmax 是对一组数整体归一化，不是逐个独立压缩。
- 必记：Softmax 输出非负且总和为 1。
### Sigmoid
- 通俗解释：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。
- 考试怎么考：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。
- 易错点：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- 必记：Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
### 多层感知机
- 通俗解释：多层感知机 - 课堂练习答案；本文件包含课堂练习的参考答案和详细解析。
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：多层感知机 - 课堂练习答案；本文件包含课堂练习的参考答案和详细解析。
### 梯度下降
- 通俗解释：问题1：Softmax为什么叫Softmax？与Hardmax有什么区别？；Hardmax：直接将最大值位置设为1，其余为0。例如输入[2, 1, 0] → 输出[1, 0, 0]。；缺点：不可微分，无法使用梯度下降优化；Softmax：通过指数函数将输入转换为概率分布。例如输入[2, 1, 0]...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：问题1：Softmax为什么叫Softmax？与Hardmax有什么区别？；Hardmax：直接将最大值位置设为1，其余为0。例如输入[2, 1, 0] → 输出[1, 0, 0]。；缺点：不可微分，无法使用梯度下降优化；Softmax：通...
