# 卷积神经网络2-学生分发版-终版2026(1) / 总览
## 第五章 神经网络训练技巧深度解析
### 学习率
- 通俗解释：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。
- 考试怎么考：常考学习率过大/过小的训练现象，以及学习率调度的目的。
- 易错点：学习率不是越大越好；也不是模型学到的参数。
- 必记：学习率决定更新步长。
### 梯度消失
- 通俗解释：反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。
- 考试怎么考：常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。
- 易错点：梯度消失不是过拟合；它是参数难以有效更新的问题。
- 必记：多个小于 1 的因子连乘，会让梯度趋近 0。
### 梯度爆炸
- 通俗解释：梯度在反向传播中变得过大，使参数更新剧烈，训练损失可能震荡或变成 NaN。
- 考试怎么考：常考梯度裁剪为什么能缓解 RNN 训练不稳定。
- 易错点：Dropout 主要缓解过拟合，不是解决梯度爆炸的标准答案。
- 必记：梯度裁剪限制梯度范数，防止更新步子过大。
### 优化器
- 通俗解释：第五章 神经网络训练技巧深度解析；1. 梯度消失、梯度爆炸及其解决方案；2. 优化器改进与学习率调整；3. 过拟合处理；4. 超参数选取实践指南 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：第五章 神经网络训练技巧深度解析；1. 梯度消失、梯度爆炸及其解决方案；2. 优化器改进与学习率调整；3. 过拟合处理；4. 超参数选取实践指南 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致...
### 超参数
- 通俗解释：第五章 神经网络训练技巧深度解析；1. 梯度消失、梯度爆炸及其解决方案；2. 优化器改进与学习率调整；3. 过拟合处理；4. 超参数选取实践指南 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：第五章 神经网络训练技巧深度解析；1. 梯度消失、梯度爆炸及其解决方案；2. 优化器改进与学习率调整；3. 过拟合处理；4. 超参数选取实践指南 第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致...
### 损失函数
- 通俗解释：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。
- 考试怎么考：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。
- 易错点：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- 必记：损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
### Batch
- 通俗解释：一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。
- 考试怎么考：常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。
- 易错点：Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。
- 必记：Batch 是一批样本；Epoch 是全训练集完整训练一遍。
### Sigmoid
- 通俗解释：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。
- 考试怎么考：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。
- 易错点：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- 必记：Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
### 卷积
- 通俗解释：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。
- 考试怎么考：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。
- 易错点：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- 必记：输出尺寸：(输入尺寸 + 2P - K) / S + 1。
## 第五章 神经网络训练技巧深度解析 / 第1节 梯度消失、梯度爆炸及其解决方案
### 归一化
- 通俗解释：把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。
- 考试怎么考：常考图像预处理为什么要缩放像素值。
- 易错点：归一化不等于 BatchNorm，前者多是输入预处理。
- 必记：图像常先把像素值缩放到 0-1。
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
### 学习率
- 通俗解释：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。
- 考试怎么考：常考学习率过大/过小的训练现象，以及学习率调度的目的。
- 易错点：学习率不是越大越好；也不是模型学到的参数。
- 必记：学习率决定更新步长。
### Batch
- 通俗解释：一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。
- 考试怎么考：常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。
- 易错点：Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。
- 必记：Batch 是一批样本；Epoch 是全训练集完整训练一遍。
### 梯度消失
- 通俗解释：反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。
- 考试怎么考：常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。
- 易错点：梯度消失不是过拟合；它是参数难以有效更新的问题。
- 必记：多个小于 1 的因子连乘，会让梯度趋近 0。
### 梯度爆炸
- 通俗解释：梯度在反向传播中变得过大，使参数更新剧烈，训练损失可能震荡或变成 NaN。
- 考试怎么考：常考梯度裁剪为什么能缓解 RNN 训练不稳定。
- 易错点：Dropout 主要缓解过拟合，不是解决梯度爆炸的标准答案。
- 必记：梯度裁剪限制梯度范数，防止更新步子过大。
### 梯度裁剪
- 通俗解释：当梯度范数超过阈值时按比例缩小，常用于稳定 RNN 等容易梯度爆炸的训练。
- 考试怎么考：常考它处理的是梯度爆炸，而不是梯度消失。
- 易错点：裁剪不是把梯度直接清零，而是限制其大小。
- 必记：Gradient Clipping：限制梯度大小，稳定训练。
### 卷积
- 通俗解释：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。
- 考试怎么考：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。
- 易错点：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- 必记：输出尺寸：(输入尺寸 + 2P - K) / S + 1。
### 反向传播
- 通俗解释：在深层神经网络的反向传播过程中，梯度会逐层传递。当网络层数增加时，靠近输入层的梯度会变得越来越小，这种现象被称为梯度消失。；反向传播使用链式法则，梯度会逐层相乘；如果每层的梯度都小于1，多次相乘后会指数级减小；结果：靠近输入层的权重几乎不更新，模型无法学习；模型训练停滞，损失不再下降 与梯度消失相反...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：在深层神经网络的反向传播过程中，梯度会逐层传递。当网络层数增加时，靠近输入层的梯度会变得越来越小，这种现象被称为梯度消失。；反向传播使用链式法则，梯度会逐层相乘；如果每层的梯度都小于1，多次相乘后会指数级减小；结果：靠近输入层的权重几乎不更...
### 标准化
- 通俗解释：把数据按均值和方差调整到更稳定的尺度，帮助模型训练。
- 考试怎么考：常考归一化和标准化的区别、为什么图像输入要预处理。
- 易错点：不要把标准化简单等同于除以 255；标准化通常涉及均值和标准差。
- 必记：标准化常写作 (x-mean)/std。
## 第五章 神经网络训练技巧深度解析 / 第2节 优化器改进与学习率调整
### 学习率
- 通俗解释：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。
- 考试怎么考：常考学习率过大/过小的训练现象，以及学习率调度的目的。
- 易错点：学习率不是越大越好；也不是模型学到的参数。
- 必记：学习率决定更新步长。
### Adam
- 通俗解释：结合动量和自适应学习率思想，为不同参数调整更新幅度。
- 考试怎么考：常和 SGD、Momentum、RMSProp 对比优化器特点。
- 易错点：Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。
- 必记：Adam = 一阶动量 + 二阶矩估计。
### 优化器
- 通俗解释：第2节 优化器改进与学习率调整；在解决了梯度消失/爆炸问题后，我们需要关注如何高效地收敛到最优解。学习率的选择直接影响优化效率：；学习率太小：收敛缓慢，训练时间过长；学习率太大：训练不稳定，损失可能发散；可以从两个维度优化学习率： 动量解决了"方向"问题，但所有参数使用统一的学习率，可能出现：；参数...
- 考试怎么考：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。
- 易错点：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- 必记：第2节 优化器改进与学习率调整；在解决了梯度消失/爆炸问题后，我们需要关注如何高效地收敛到最优解。学习率的选择直接影响优化效率：；学习率太小：收敛缓慢，训练时间过长；学习率太大：训练不稳定，损失可能发散；可以从两个维度优化学习率： 动量解决...
### RMSProp
- 通俗解释：用梯度平方的指数滑动平均调整学习率，缓解 Adagrad 学习率过快衰减。
- 考试怎么考：常考它和 Adagrad 的区别。
- 易错点：不要写成简单固定学习率下降。
- 必记：RMSProp 使用指数滑动平均保存近期梯度尺度。
### Momentum
- 通俗解释：动量（Momentum）；动量：迫使目标函数沿着"沟壑"快速下降的方法（冲量/“惯性”）；动量法的参数更新过程：；$$\boldsymbol{v}=\gamma \boldsymbol{v}-\alpha；abla_{\boldsymbol{\theta}} J\left(\boldsymbol{\...
- 考试怎么考：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。
- 易错点：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- 必记：动量（Momentum）；动量：迫使目标函数沿着"沟壑"快速下降的方法（冲量/“惯性”）；动量法的参数更新过程：；$$\boldsymbol{v}=\gamma \boldsymbol{v}-\alpha；abla_{\boldsymbol...
### 梯度下降
- 通俗解释：目标函数的形式通常是狭长且浅的沟壑；在深度网络中，目标函数的局部最优点经常以这种形式呈现，所以标准的随机梯度下降算法通常会在“沟壑”两侧反复震荡 优化器 | 核心思想 | 特点 | 适用场景；SGD | 基础梯度下降 | 简单，可能需要调整学习率 | 大规模数据、最终精调；SGD+Momentum...
- 考试怎么考：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。
- 易错点：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- 必记：目标函数的形式通常是狭长且浅的沟壑；在深度网络中，目标函数的局部最优点经常以这种形式呈现，所以标准的随机梯度下降算法通常会在“沟壑”两侧反复震荡 优化器 | 核心思想 | 特点 | 适用场景；SGD | 基础梯度下降 | 简单，可能需要调整...
### ResNet
- 通俗解释：用残差/跳跃连接让网络学习 F(x)+x，缓解深层网络退化和梯度传播困难。
- 考试怎么考：常考残差连接为什么能训练更深网络。
- 易错点：残差连接不是单纯增加层数，而是改变信息和梯度路径。
- 必记：ResNet 核心：y = F(x) + x。
### Epoch
- 通俗解释：训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。
- 考试怎么考：常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。
- 易错点：Epoch 不是一次参数更新；一次更新通常对应一个 batch。
- 必记：1 个 Epoch = 全部训练样本被用过一遍。
### 梯度消失
- 通俗解释：反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。
- 考试怎么考：常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。
- 易错点：梯度消失不是过拟合；它是参数难以有效更新的问题。
- 必记：多个小于 1 的因子连乘，会让梯度趋近 0。
### 步长
- 通俗解释：卷积核或池化窗口每次移动的间隔。
- 考试怎么考：常考步长变大时输出尺寸如何变化。
- 易错点：步长越大，输出空间尺寸通常越小。
- 必记：Stride 控制滑动间隔。
## 第五章 神经网络训练技巧深度解析 / 第3节 处理过拟合的综合策略
### Dropout
- 通俗解释：训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。
- 考试怎么考：常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。
- 易错点：Dropout 不是提高模型容量，而是正则化。
- 必记：Dropout：训练随机失活，推理正常使用。
### 数据增强
- 通俗解释：三、数据增强（Data Augmentation） 除了从模型结构（Dropout）和训练策略（早停）防止过拟合，还可以从数据层面入手——通过数据增强扩充训练集的多样性；$\left\{\boldsymbol{x}^{(n)}, \boldsymbol{t}^{(n)}\right\}$表示原本的训...
- 考试怎么考：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。
- 易错点：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- 必记：三、数据增强（Data Augmentation） 除了从模型结构（Dropout）和训练策略（早停）防止过拟合，还可以从数据层面入手——通过数据增强扩充训练集的多样性；$\left\{\boldsymbol{x}^{(n)}, \bold...
### 卷积
- 通俗解释：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。
- 考试怎么考：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。
- 易错点：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- 必记：输出尺寸：(输入尺寸 + 2P - K) / S + 1。
### 权重衰减
- 通俗解释：四、增加权重衰减（Weight Decay） 什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambda \sum_{i} \theta_i^2$$；$L_{lo...
- 考试怎么考：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。
- 易错点：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- 必记：四、增加权重衰减（Weight Decay） 什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambd...
### 损失函数
- 通俗解释：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。
- 考试怎么考：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。
- 易错点：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- 必记：损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
### Epoch
- 通俗解释：训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。
- 考试怎么考：常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。
- 易错点：Epoch 不是一次参数更新；一次更新通常对应一个 batch。
- 必记：1 个 Epoch = 全部训练样本被用过一遍。
### 交叉熵
- 通俗解释：什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambda \sum_{i} \theta_i^2$$；$L_{loss}$：原始损失（如交叉熵损失）；$\lam...
- 考试怎么考：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。
- 易错点：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- 必记：什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambda \sum_{i} \theta_i^2$$...
## 第五章 神经网络训练技巧深度解析 / 第4节 超参数选取实践指南
### Batch
- 通俗解释：一次送入模型并一起计算损失的一组样本。它决定每次参数更新看到多少样本，也影响显存占用和训练稳定性。
- 考试怎么考：常考 batch size、mini-batch、epoch、iteration 的区别，也会结合张量形状中的 N 维度判断。
- 易错点：Batch 不是序列长度；在 RNN 中常见形状里 N 是样本数，L 才是时间步长度。
- 必记：Batch 是一批样本；Epoch 是全训练集完整训练一遍。
### 优化器
- 通俗解释：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数 第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步...
- 考试怎么考：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。
- 易错点：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- 必记：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数 第1...
### Adam
- 通俗解释：结合动量和自适应学习率思想，为不同参数调整更新幅度。
- 考试怎么考：常和 SGD、Momentum、RMSProp 对比优化器特点。
- 易错点：Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。
- 必记：Adam = 一阶动量 + 二阶矩估计。
### Epoch
- 通俗解释：训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。
- 考试怎么考：常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。
- 易错点：Epoch 不是一次参数更新；一次更新通常对应一个 batch。
- 必记：1 个 Epoch = 全部训练样本被用过一遍。
### 学习率
- 通俗解释：控制每次参数沿梯度方向更新的步子大小。学习率太大容易震荡甚至发散，太小则收敛很慢。
- 考试怎么考：常考学习率过大/过小的训练现象，以及学习率调度的目的。
- 易错点：学习率不是越大越好；也不是模型学到的参数。
- 必记：学习率决定更新步长。
### Dropout
- 通俗解释：训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。
- 考试怎么考：常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。
- 易错点：Dropout 不是提高模型容量，而是正则化。
- 必记：Dropout：训练随机失活，推理正常使用。
### 权重衰减
- 通俗解释：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数 第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步...
- 考试怎么考：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。
- 易错点：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- 必记：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数 第1...
### Momentum
- 通俗解释：第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到分类100%训练准确率，回归loss第3步：找到使损失下降的学习率 第一步：定义"相似性"；寻找参考配置时，需从以下几个维度判断相似度：；维度 | 优先级 | 例子；网络架构 | 最高 |...
- 考试怎么考：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。
- 易错点：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- 必记：第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到分类100%训练准确率，回归loss第3步：找到使损失下降的学习率 第一步：定义"相似性"；寻找参考配置时，需从以下几个维度判断相...
### ResNet
- 通俗解释：用残差/跳跃连接让网络学习 F(x)+x，缓解深层网络退化和梯度传播困难。
- 考试怎么考：常考残差连接为什么能训练更深网络。
- 易错点：残差连接不是单纯增加层数，而是改变信息和梯度路径。
- 必记：ResNet 核心：y = F(x) + x。
### 超参数
- 通俗解释：💡 经验很重要！多实践、多观察损失曲线，才能找到最佳超参数。 消融实验（Ablation Study）；1. 什么是消融实验？；消融实验（Ablation Study） 源自神经科学中的 "ablation"（切除）概念。在深度学习中，它指的是通过系统地移除、替换或修改模型中的某个组件/超参数，来量...
- 考试怎么考：会放在 CNN 结构、输出尺寸、参数共享或训练流程里考，重点判断它在卷积网络中起什么作用。
- 易错点：不要只背名词；要能说清它和卷积层、池化、损失函数或训练阶段的关系。
- 必记：💡 经验很重要！多实践、多观察损失曲线，才能找到最佳超参数。 消融实验（Ablation Study）；1. 什么是消融实验？；消融实验（Ablation Study） 源自神经科学中的 "ablation"（切除）概念。在深度学习中，它指...
