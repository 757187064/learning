# 多层感知机2-学生分发版-终版2026 / 总览
## 第三章 分类问题与多层感知机
### Sigmoid
- 通俗解释：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。
- 考试怎么考：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。
- 易错点：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- 必记：Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
### Softmax
- 通俗解释：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。
- 考试怎么考：常考多分类输出、注意力权重为什么能加权求和。
- 易错点：Softmax 是对一组数整体归一化，不是逐个独立压缩。
- 必记：Softmax 输出非负且总和为 1。
### 交叉熵
- 通俗解释：衡量预测概率分布与真实类别之间差距的分类损失。
- 考试怎么考：常考多分类用 CrossEntropyLoss，二分类/多标签用 BCE 类损失。
- 易错点：不要把交叉熵和准确率混成同一个东西。
- 必记：交叉熵越小，预测分布越接近真实标签。
### 多层感知机
- 通俗解释：多层感知机是由输入层、隐藏层和输出层组成的前馈神经网络。
- 考试怎么考：常考隐藏层作用、激活函数必要性、训练流程。
- 易错点：多层感知机不是循环网络，也不保留时间状态。
- 必记：多层感知机靠非线性激活增强表达能力。
### Logistic
- 通俗解释：Logistic 回归常用于二分类，本质是线性输出接 Sigmoid 得到概率。
- 考试怎么考：常考二分类输出、Sigmoid、BCE 类损失的搭配。
- 易错点：Logistic 回归名字里有回归，但常用于分类。
- 必记：Logistic：线性打分 + Sigmoid。
### MLP
- 通俗解释：多层感知机由多层线性变换和非线性激活组成，是最基础的前馈神经网络。
- 考试怎么考：常考为什么需要激活函数、前向传播流程、分类输出和损失函数匹配。
- 易错点：不要把多层线性层误认为天然具有非线性；没有激活函数仍等价于线性变换。
- 必记：MLP = 线性层 + 激活函数 + 输出层。
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
### 损失函数
- 通俗解释：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。
- 考试怎么考：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。
- 易错点：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- 必记：损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
### BCEWithLogitsLoss
- 通俗解释：二分类或多标签任务常用损失，内部把 Sigmoid 和 BCE 合在一起，数值更稳定。
- 考试怎么考：常考它输入的是 logits，不需要手动先做 Sigmoid。
- 易错点：不要在 BCEWithLogitsLoss 前再手动 Sigmoid。
- 必记：BCEWithLogitsLoss = Sigmoid + BCE。
### CrossEntropyLoss
- 通俗解释：多分类常用损失，内部包含 LogSoftmax 和 NLLLoss。
- 考试怎么考：常考它输入 logits，标签通常是类别索引。
- 易错点：不要在 CrossEntropyLoss 前手动 Softmax。
- 必记：CrossEntropyLoss 用于单标签多分类。
## 第三章 分类问题与多层感知机 / 第一节 分类问题概述
### 损失函数
- 通俗解释：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。
- 考试怎么考：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。
- 易错点：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- 必记：损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
### Sigmoid
- 通俗解释：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。
- 考试怎么考：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。
- 易错点：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- 必记：Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
### Softmax
- 通俗解释：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。
- 考试怎么考：常考多分类输出、注意力权重为什么能加权求和。
- 易错点：Softmax 是对一组数整体归一化，不是逐个独立压缩。
- 必记：Softmax 输出非负且总和为 1。
### 交叉熵
- 通俗解释：衡量预测概率分布与真实类别之间差距的分类损失。
- 考试怎么考：常考多分类用 CrossEntropyLoss，二分类/多标签用 BCE 类损失。
- 易错点：不要把交叉熵和准确率混成同一个东西。
- 必记：交叉熵越小，预测分布越接近真实标签。
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
## 第三章 分类问题与多层感知机 / 第二节 Logistic回归（二分类）
### Sigmoid
- 通俗解释：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。
- 考试怎么考：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。
- 易错点：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- 必记：Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
### Logistic
- 通俗解释：Logistic 回归常用于二分类，本质是线性输出接 Sigmoid 得到概率。
- 考试怎么考：常考二分类输出、Sigmoid、BCE 类损失的搭配。
- 易错点：Logistic 回归名字里有回归，但常用于分类。
- 必记：Logistic：线性打分 + Sigmoid。
### 交叉熵
- 通俗解释：衡量预测概率分布与真实类别之间差距的分类损失。
- 考试怎么考：常考多分类用 CrossEntropyLoss，二分类/多标签用 BCE 类损失。
- 易错点：不要把交叉熵和准确率混成同一个东西。
- 必记：交叉熵越小，预测分布越接近真实标签。
### 损失函数
- 通俗解释：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。
- 考试怎么考：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。
- 易错点：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- 必记：损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
### BCEWithLogitsLoss
- 通俗解释：二分类或多标签任务常用损失，内部把 Sigmoid 和 BCE 合在一起，数值更稳定。
- 考试怎么考：常考它输入的是 logits，不需要手动先做 Sigmoid。
- 易错点：不要在 BCEWithLogitsLoss 前再手动 Sigmoid。
- 必记：BCEWithLogitsLoss = Sigmoid + BCE。
## 第三章 分类问题与多层感知机 / 第三节 Softmax回归（多分类）
### Softmax
- 通俗解释：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。
- 考试怎么考：常考多分类输出、注意力权重为什么能加权求和。
- 易错点：Softmax 是对一组数整体归一化，不是逐个独立压缩。
- 必记：Softmax 输出非负且总和为 1。
### 梯度下降
- 通俗解释：沿着损失函数下降最快的方向调整参数，让模型预测逐步变好。
- 考试怎么考：常考参数更新方向、学习率作用、训练循环位置。
- 易错点：梯度下降不是直接调准确率，而是通过损失函数和梯度改参数。
- 必记：参数更新方向通常与梯度相反。
### Sigmoid
- 通俗解释：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。
- 考试怎么考：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。
- 易错点：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- 必记：Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
### 交叉熵
- 通俗解释：衡量预测概率分布与真实类别之间差距的分类损失。
- 考试怎么考：常考多分类用 CrossEntropyLoss，二分类/多标签用 BCE 类损失。
- 易错点：不要把交叉熵和准确率混成同一个东西。
- 必记：交叉熵越小，预测分布越接近真实标签。
## 第三章 分类问题与多层感知机 / 第四节 Logistic vs Softmax：统一视角
### 损失函数
- 通俗解释：把模型预测和真实标签之间的差距变成一个可优化的数值，反向传播就是从损失开始计算梯度。
- 考试怎么考：常考输出层与损失函数的匹配，例如多分类常配 CrossEntropyLoss，二分类可配 BCEWithLogitsLoss。
- 易错点：不要把准确率当作训练损失；也不要把 BCE 和 CE 的适用场景混用。
- 必记：损失函数告诉模型“错在哪里”，优化器负责“怎么改参数”。
### Logistic
- 通俗解释：Logistic 回归常用于二分类，本质是线性输出接 Sigmoid 得到概率。
- 考试怎么考：常考二分类输出、Sigmoid、BCE 类损失的搭配。
- 易错点：Logistic 回归名字里有回归，但常用于分类。
- 必记：Logistic：线性打分 + Sigmoid。
### Sigmoid
- 通俗解释：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。
- 考试怎么考：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。
- 易错点：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- 必记：Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
### Softmax
- 通俗解释：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。
- 考试怎么考：常考多分类输出、注意力权重为什么能加权求和。
- 易错点：Softmax 是对一组数整体归一化，不是逐个独立压缩。
- 必记：Softmax 输出非负且总和为 1。
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
## 第三章 分类问题与多层感知机 / 第五节 多层感知机（MLP），也叫做全连接神经网络
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
### MLP
- 通俗解释：多层感知机由多层线性变换和非线性激活组成，是最基础的前馈神经网络。
- 考试怎么考：常考为什么需要激活函数、前向传播流程、分类输出和损失函数匹配。
- 易错点：不要把多层线性层误认为天然具有非线性；没有激活函数仍等价于线性变换。
- 必记：MLP = 线性层 + 激活函数 + 输出层。
### Logistic
- 通俗解释：Logistic 回归常用于二分类，本质是线性输出接 Sigmoid 得到概率。
- 考试怎么考：常考二分类输出、Sigmoid、BCE 类损失的搭配。
- 易错点：Logistic 回归名字里有回归，但常用于分类。
- 必记：Logistic：线性打分 + Sigmoid。
### 多层感知机
- 通俗解释：多层感知机是由输入层、隐藏层和输出层组成的前馈神经网络。
- 考试怎么考：常考隐藏层作用、激活函数必要性、训练流程。
- 易错点：多层感知机不是循环网络，也不保留时间状态。
- 必记：多层感知机靠非线性激活增强表达能力。
### Sigmoid
- 通俗解释：把任意实数压到 0 到 1 之间，可解释为概率或门控强度。GRU/LSTM 的门通常用它决定保留多少信息。
- 考试怎么考：常考输出范围、二分类概率、以及门控结构中为什么适合控制开关。
- 易错点：Sigmoid 不只属于分类输出，在 RNN 门控里也很重要。
- 必记：Sigmoid 输出在 0 到 1；越接近 1 越保留，越接近 0 越抑制。
### 梯度消失
- 通俗解释：反向传播经过很多层或很多时间步时，梯度可能不断变小，导致前面层或早期时间步很难学到东西。
- 考试怎么考：常考深层网络/RNN 为什么训练困难，以及 LSTM、GRU、残差连接如何缓解。
- 易错点：梯度消失不是过拟合；它是参数难以有效更新的问题。
- 必记：多个小于 1 的因子连乘，会让梯度趋近 0。
### 仿射变换
- 通俗解释：线性加权求和再加偏置，常写作 Wx+b。
- 考试怎么考：常考它和激活函数的配合：仿射变换本身仍是线性结构。
- 易错点：不要把仿射变换本身说成非线性映射。
- 必记：仿射变换：Wx+b。
