# 多层感知机2-学生分发版-终版2026 / 总览
## 第三章 分类问题与多层感知机
### Sigmoid
- 通俗解释：第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds...
### Softmax
- 通俗解释：第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds...
### 交叉熵
- 通俗解释：第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds...
### MLP
- 通俗解释：第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds...
### 多层感知机
- 通俗解释：第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第三章 分类问题与多层感知机；📌 学习目标：掌握二分类/多分类原理、理解Sigmoid和Softmax、学会使用交叉熵损失、了解MLP结构 1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds...
### Logistic
- 通俗解释：1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失)，PyTorch实现；3. Softmax回归：多分类原理，Softmax vs Hardmax，交叉熵损失；4. Logistic vs...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：1. 分类问题概述：回归 vs 分类，二分类 vs 多分类；2. Logistic回归：从Odds Ratio到Sigmoid，BCE Loss(二元交叉熵损失)，PyTorch实现；3. Softmax回归：多分类原理，Softmax v...
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
### 损失函数
- 通俗解释：一、分类问题基础；回归 vs 分类：连续值 vs 离散类别；二分类 vs 多分类：Sigmoid/BCE vs Softmax/CE；二、Logistic回归；Sigmoid：$\sigma(z) = \frac{1}{1+e^{-z}}$，将$(-\infty, +\infty)$映射到$(0,1...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：一、分类问题基础；回归 vs 分类：连续值 vs 离散类别；二分类 vs 多分类：Sigmoid/BCE vs Softmax/CE；二、Logistic回归；Sigmoid：$\sigma(z) = \frac{1}{1+e^{-z}}$...
### BCEWithLogitsLoss
- 通俗解释：一、分类问题基础；回归 vs 分类：连续值 vs 离散类别；二分类 vs 多分类：Sigmoid/BCE vs Softmax/CE；二、Logistic回归；Sigmoid：$\sigma(z) = \frac{1}{1+e^{-z}}$，将$(-\infty, +\infty)$映射到$(0,1...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：一、分类问题基础；回归 vs 分类：连续值 vs 离散类别；二分类 vs 多分类：Sigmoid/BCE vs Softmax/CE；二、Logistic回归；Sigmoid：$\sigma(z) = \frac{1}{1+e^{-z}}$...
### CrossEntropyLoss
- 通俗解释：一、分类问题基础；回归 vs 分类：连续值 vs 离散类别；二分类 vs 多分类：Sigmoid/BCE vs Softmax/CE；二、Logistic回归；Sigmoid：$\sigma(z) = \frac{1}{1+e^{-z}}$，将$(-\infty, +\infty)$映射到$(0,1...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：一、分类问题基础；回归 vs 分类：连续值 vs 离散类别；二分类 vs 多分类：Sigmoid/BCE vs Softmax/CE；二、Logistic回归；Sigmoid：$\sigma(z) = \frac{1}{1+e^{-z}}$...
## 第三章 分类问题与多层感知机 / 第一节 分类问题概述
### 损失函数
- 通俗解释：第一节 分类问题概述；回归 vs 分类；给定数据点集合 ${(x^{(i)}, t^{(i)})}$，目标是找到映射 $f: \mathbb{R}^m \rightarrow \Omega$。；任务类型 | 标签空间 $\Omega$ | 典型问题；回归 | 连续值 | 房价预测、温度预测
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第一节 分类问题概述；回归 vs 分类；给定数据点集合 ${(x^{(i)}, t^{(i)})}$，目标是找到映射 $f: \mathbb{R}^m \rightarrow \Omega$。；任务类型 | 标签空间 $\Omega$ |...
### Sigmoid
- 通俗解释：第一节 分类问题概述；回归 vs 分类；给定数据点集合 ${(x^{(i)}, t^{(i)})}$，目标是找到映射 $f: \mathbb{R}^m \rightarrow \Omega$。；任务类型 | 标签空间 $\Omega$ | 典型问题；回归 | 连续值 | 房价预测、温度预测
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第一节 分类问题概述；回归 vs 分类；给定数据点集合 ${(x^{(i)}, t^{(i)})}$，目标是找到映射 $f: \mathbb{R}^m \rightarrow \Omega$。；任务类型 | 标签空间 $\Omega$ |...
### Softmax
- 通俗解释：第一节 分类问题概述；回归 vs 分类；给定数据点集合 ${(x^{(i)}, t^{(i)})}$，目标是找到映射 $f: \mathbb{R}^m \rightarrow \Omega$。；任务类型 | 标签空间 $\Omega$ | 典型问题；回归 | 连续值 | 房价预测、温度预测
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第一节 分类问题概述；回归 vs 分类；给定数据点集合 ${(x^{(i)}, t^{(i)})}$，目标是找到映射 $f: \mathbb{R}^m \rightarrow \Omega$。；任务类型 | 标签空间 $\Omega$ |...
### 交叉熵
- 通俗解释：第一节 分类问题概述；回归 vs 分类；给定数据点集合 ${(x^{(i)}, t^{(i)})}$，目标是找到映射 $f: \mathbb{R}^m \rightarrow \Omega$。；任务类型 | 标签空间 $\Omega$ | 典型问题；回归 | 连续值 | 房价预测、温度预测
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第一节 分类问题概述；回归 vs 分类；给定数据点集合 ${(x^{(i)}, t^{(i)})}$，目标是找到映射 $f: \mathbb{R}^m \rightarrow \Omega$。；任务类型 | 标签空间 $\Omega$ |...
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
## 第三章 分类问题与多层感知机 / 第二节 Logistic回归（二分类）
### Sigmoid
- 通俗解释：第二节 Logistic回归（二分类）；从线性回归到Logistic回归；线性回归的局限：对于二分类（标签0或1），线性回归预测值可能是任意实数，无法解释为概率。；解决方案：引入Logits和Sigmoid；Step 1: Odds Ratio（几率比） PyTorch最佳实践：BCEWithLog...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第二节 Logistic回归（二分类）；从线性回归到Logistic回归；线性回归的局限：对于二分类（标签0或1），线性回归预测值可能是任意实数，无法解释为概率。；解决方案：引入Logits和Sigmoid；Step 1: Odds Rat...
### Logistic
- 通俗解释：第二节 Logistic回归（二分类）；从线性回归到Logistic回归；线性回归的局限：对于二分类（标签0或1），线性回归预测值可能是任意实数，无法解释为概率。；解决方案：引入Logits和Sigmoid；Step 1: Odds Ratio（几率比）
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第二节 Logistic回归（二分类）；从线性回归到Logistic回归；线性回归的局限：对于二分类（标签0或1），线性回归预测值可能是任意实数，无法解释为概率。；解决方案：引入Logits和Sigmoid；Step 1: Odds Rat...
### 交叉熵
- 通俗解释：二元交叉熵损失（BCE Loss）；$$L = -\frac{1}{N} \sum_{i=1}^{N} \left[ y_i \log \hat{y}_i + (1-y_i)\log(1-\hat{y}_i) \right]$$；当 $y_i = 1$：损失为 $-\log \hat{y}_i$，希...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：二元交叉熵损失（BCE Loss）；$$L = -\frac{1}{N} \sum_{i=1}^{N} \left[ y_i \log \hat{y}_i + (1-y_i)\log(1-\hat{y}_i) \right]$$；当 $y_...
### 损失函数
- 通俗解释：PyTorch最佳实践：BCEWithLogitsLoss；数值稳定性问题：直接 Sigmoid + BCELoss 当logits很大时会溢出。；解决方案：BCEWithLogitsLoss 内部合并 sigmoid和log运算。；组合 | 模型最后一层 | 损失函数 | 数值稳定性 | 计算效率...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：PyTorch最佳实践：BCEWithLogitsLoss；数值稳定性问题：直接 Sigmoid + BCELoss 当logits很大时会溢出。；解决方案：BCEWithLogitsLoss 内部合并 sigmoid和log运算。；组合...
### BCEWithLogitsLoss
- 通俗解释：PyTorch最佳实践：BCEWithLogitsLoss；数值稳定性问题：直接 Sigmoid + BCELoss 当logits很大时会溢出。；解决方案：BCEWithLogitsLoss 内部合并 sigmoid和log运算。；组合 | 模型最后一层 | 损失函数 | 数值稳定性 | 计算效率...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：PyTorch最佳实践：BCEWithLogitsLoss；数值稳定性问题：直接 Sigmoid + BCELoss 当logits很大时会溢出。；解决方案：BCEWithLogitsLoss 内部合并 sigmoid和log运算。；组合...
## 第三章 分类问题与多层感知机 / 第三节 Softmax回归（多分类）
### Softmax
- 通俗解释：第三节 Softmax回归（多分类）；Softmax函数；对于 $K$ 类分类，；有独热分布来把每个分类类别进行无大小区别的区分；将K个logits转换为概率分布： 使用Softmax的原因；为什么叫softmax（软分类）；hardmax最大的问题（取最大值的问题）是不能求导，无法用梯度下降训练神...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第三节 Softmax回归（多分类）；Softmax函数；对于 $K$ 类分类，；有独热分布来把每个分类类别进行无大小区别的区分；将K个logits转换为概率分布： 使用Softmax的原因；为什么叫softmax（软分类）；hardmax...
### 梯度下降
- 通俗解释：使用Softmax的原因；为什么叫softmax（软分类）；hardmax最大的问题（取最大值的问题）是不能求导，无法用梯度下降训练神经网络
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：使用Softmax的原因；为什么叫softmax（软分类）；hardmax最大的问题（取最大值的问题）是不能求导，无法用梯度下降训练神经网络
### Sigmoid
- 通俗解释：交叉熵损失（Cross-Entropy Loss）；$$L = -\frac{1}{N} \sum_{n=1}^{N} \log h_{k^*}(x^{(n)})$$；其中 $k^*$ 是样本的真实类别。；由于独热编码的简化：；原始形式：$L = -\frac{1}{N} \sum_{n} \sum...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：交叉熵损失（Cross-Entropy Loss）；$$L = -\frac{1}{N} \sum_{n=1}^{N} \log h_{k^*}(x^{(n)})$$；其中 $k^*$ 是样本的真实类别。；由于独热编码的简化：；原始形式：$...
### 交叉熵
- 通俗解释：交叉熵损失（Cross-Entropy Loss）；$$L = -\frac{1}{N} \sum_{n=1}^{N} \log h_{k^*}(x^{(n)})$$；其中 $k^*$ 是样本的真实类别。；由于独热编码的简化：；原始形式：$L = -\frac{1}{N} \sum_{n} \sum...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：交叉熵损失（Cross-Entropy Loss）；$$L = -\frac{1}{N} \sum_{n=1}^{N} \log h_{k^*}(x^{(n)})$$；其中 $k^*$ 是样本的真实类别。；由于独热编码的简化：；原始形式：$...
## 第三章 分类问题与多层感知机 / 第四节 Logistic vs Softmax：统一视角
### 损失函数
- 通俗解释：第四节 Logistic vs Softmax：统一视角；Logistic是Softmax的特例；当 $K=2$ 时，Softmax退化为Sigmoid：；$$h_1(x) = \frac{e^{z_1}}{e^{z_1} + e^{z_2}} = \frac{1}{1 + e^{-(z_1-z_2...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第四节 Logistic vs Softmax：统一视角；Logistic是Softmax的特例；当 $K=2$ 时，Softmax退化为Sigmoid：；$$h_1(x) = \frac{e^{z_1}}{e^{z_1} + e^{z_2...
### Logistic
- 通俗解释：第四节 Logistic vs Softmax：统一视角；Logistic是Softmax的特例；当 $K=2$ 时，Softmax退化为Sigmoid：；$$h_1(x) = \frac{e^{z_1}}{e^{z_1} + e^{z_2}} = \frac{1}{1 + e^{-(z_1-z_2...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第四节 Logistic vs Softmax：统一视角；Logistic是Softmax的特例；当 $K=2$ 时，Softmax退化为Sigmoid：；$$h_1(x) = \frac{e^{z_1}}{e^{z_1} + e^{z_2...
### Sigmoid
- 通俗解释：第四节 Logistic vs Softmax：统一视角；Logistic是Softmax的特例；当 $K=2$ 时，Softmax退化为Sigmoid：；$$h_1(x) = \frac{e^{z_1}}{e^{z_1} + e^{z_2}} = \frac{1}{1 + e^{-(z_1-z_2...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第四节 Logistic vs Softmax：统一视角；Logistic是Softmax的特例；当 $K=2$ 时，Softmax退化为Sigmoid：；$$h_1(x) = \frac{e^{z_1}}{e^{z_1} + e^{z_2...
### Softmax
- 通俗解释：第四节 Logistic vs Softmax：统一视角；Logistic是Softmax的特例；当 $K=2$ 时，Softmax退化为Sigmoid：；$$h_1(x) = \frac{e^{z_1}}{e^{z_1} + e^{z_2}} = \frac{1}{1 + e^{-(z_1-z_2...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第四节 Logistic vs Softmax：统一视角；Logistic是Softmax的特例；当 $K=2$ 时，Softmax退化为Sigmoid：；$$h_1(x) = \frac{e^{z_1}}{e^{z_1} + e^{z_2...
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
## 第三章 分类问题与多层感知机 / 第五节 多层感知机（MLP），也叫做全连接神经网络
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
### MLP
- 通俗解释：第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间的神经元不连接 常见激活函数对比；激活函数 | 公式 | 输...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间...
### Logistic
- 通俗解释：第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间的神经元不连接
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间...
### 多层感知机
- 通俗解释：第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间的神经元不连接
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：第五节 多层感知机（MLP），也叫做全连接神经网络；为什么需要MLP？；Logistic回归决策边界是线性的；对于非线性可分数据（如make_moons），需要非线性变换；网格结构特点：共L层，相邻层之间的神经元两两链接（全连接），一层之间...
### Sigmoid
- 通俗解释：常见激活函数对比；激活函数 | 公式 | 输出范围 | 特点；Sigmoid | $\frac{1}{1+e^{-x}}$ | (0, 1) | 非零中心化，梯度消失；Tanh | $\frac{e^x-e^{-x}}{e^x+e^{-x}}$ | (-1, 1) | 零中心化，收敛更快 双曲正切...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：常见激活函数对比；激活函数 | 公式 | 输出范围 | 特点；Sigmoid | $\frac{1}{1+e^{-x}}$ | (0, 1) | 非零中心化，梯度消失；Tanh | $\frac{e^x-e^{-x}}{e^x+e^{-x}...
### 梯度消失
- 通俗解释：常见激活函数对比；激活函数 | 公式 | 输出范围 | 特点；Sigmoid | $\frac{1}{1+e^{-x}}$ | (0, 1) | 非零中心化，梯度消失；Tanh | $\frac{e^x-e^{-x}}{e^x+e^{-x}}$ | (-1, 1) | 零中心化，收敛更快 双曲正切...
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：常见激活函数对比；激活函数 | 公式 | 输出范围 | 特点；Sigmoid | $\frac{1}{1+e^{-x}}$ | (0, 1) | 非零中心化，梯度消失；Tanh | $\frac{e^x-e^{-x}}{e^x+e^{-x}...
### Transformer
- 通俗解释：以注意力为核心的序列模型，用多头注意力、前馈网络、残差和归一化堆叠编码器/解码器。
- 考试怎么考：常考编码器/解码器结构、多头注意力和位置编码。
- 易错点：Transformer 不依赖 RNN 的逐步递归来建模序列。
- 必记：Transformer 核心：Attention + FFN + 残差 + Norm + 位置编码。
### 仿射变换
- 通俗解释：激活函数的本质作用；仿射变换（线性变换+平移）：旋转、缩放、平移特征空间；$$u_j^{(l)} = \sum_{i} w_{ji}^{(l)} y_i^{(l-1)} + b_j^{(l)}$$；神经元的操作仅限于仿射变换（线性组合+平移），不包含激活函数。；激活函数：对特征空间进行非线性扭曲
- 考试怎么考：会结合前向传播、损失函数、梯度下降、激活函数或分类输出层一起考。
- 易错点：不要把线性变换、非线性激活、损失函数和优化器的职责混在一起。
- 必记：激活函数的本质作用；仿射变换（线性变换+平移）：旋转、缩放、平移特征空间；$$u_j^{(l)} = \sum_{i} w_{ji}^{(l)} y_i^{(l-1)} + b_j^{(l)}$$；神经元的操作仅限于仿射变换（线性组合+平移...
