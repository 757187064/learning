# 卷积神经网络2-学生分发版-终版2026 / 总览
## 第五章 神经网络训练技巧深度解析 / 第1节 梯度消失、梯度爆炸及其解决方案
### Batch
- 通俗解释：梯度爆炸解决方案小结；除梯度裁剪外的解决方案；方案类别 | 具体方法 | 核心原理 | 适用场景；权重正则化 | L2正则化/权重衰减 | 限制权重大小，间接约束梯度 | 所有网络；改进初始化 | 正交初始化、LSUV、Fixup | 保持梯度范数在反向传播中稳定 | 极深网络 2.批归一化（Bat...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：梯度爆炸解决方案小结；除梯度裁剪外的解决方案；方案类别 | 具体方法 | 核心原理 | 适用场景；权重正则化 | L2正则化/权重衰减 | 限制权重大小，间接约束梯度 | 所有网络；改进初始化 | 正交初始化、LSUV、Fixup | 保持...
### 归一化
- 通俗解释：把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。
- 考试怎么考：常考图像预处理为什么要缩放像素值。
- 易错点：归一化不等于 BatchNorm，前者多是输入预处理。
- 必记：图像常先把像素值缩放到 0-1。
### 学习率
- 通俗解释：与梯度消失相反，梯度爆炸是指在反向传播过程中，梯度变得越来越大，最终导致数值溢出。；训练过程中损失突然变成 NaN（Not a Number）；如果每层的梯度都大于1，多次相乘后会指数级增长；目标变量值范围过大（如回归问题中目标值从-400到400） 梯度爆炸解决方案小结；除梯度裁剪外的解决方案；方...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：与梯度消失相反，梯度爆炸是指在反向传播过程中，梯度变得越来越大，最终导致数值溢出。；训练过程中损失突然变成 NaN（Not a Number）；如果每层的梯度都大于1，多次相乘后会指数级增长；目标变量值范围过大（如回归问题中目标值从-400...
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
### 梯度爆炸
- 通俗解释：梯度爆炸（Exploding Gradients） 与梯度消失相反，梯度爆炸是指在反向传播过程中，梯度变得越来越大，最终导致数值溢出。；训练过程中损失突然变成 NaN（Not a Number）；如果每层的梯度都大于1，多次相乘后会指数级增长；目标变量值范围过大（如回归问题中目标值从-400到400...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：梯度爆炸（Exploding Gradients） 与梯度消失相反，梯度爆炸是指在反向传播过程中，梯度变得越来越大，最终导致数值溢出。；训练过程中损失突然变成 NaN（Not a Number）；如果每层的梯度都大于1，多次相乘后会指数级增...
### 梯度消失
- 通俗解释：梯度消失（Vanishing Gradients） 与梯度消失相反，梯度爆炸是指在反向传播过程中，梯度变得越来越大，最终导致数值溢出。；训练过程中损失突然变成 NaN（Not a Number）；如果每层的梯度都大于1，多次相乘后会指数级增长；目标变量值范围过大（如回归问题中目标值从-400到400...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：梯度消失（Vanishing Gradients） 与梯度消失相反，梯度爆炸是指在反向传播过程中，梯度变得越来越大，最终导致数值溢出。；训练过程中损失突然变成 NaN（Not a Number）；如果每层的梯度都大于1，多次相乘后会指数级增...
### 梯度裁剪
- 通俗解释：二、梯度爆炸解决方案：梯度裁剪（Gradient Clipping） 什么是梯度裁剪？；梯度裁剪是一种解决梯度爆炸的技术，通过限制梯度的大小来防止其无限增长。；基本思想：设置一个阈值，当梯度的绝对值超过阈值时，将其裁剪到阈值范围内。；1. Value Clipping（元素级裁剪）：逐个元素限制梯度...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：二、梯度爆炸解决方案：梯度裁剪（Gradient Clipping） 什么是梯度裁剪？；梯度裁剪是一种解决梯度爆炸的技术，通过限制梯度的大小来防止其无限增长。；基本思想：设置一个阈值，当梯度的绝对值超过阈值时，将其裁剪到阈值范围内。；1....
### 卷积
- 通俗解释：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。
- 考试怎么考：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。
- 易错点：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- 必记：输出尺寸：(输入尺寸 + 2P - K) / S + 1。
### 反向传播
- 通俗解释：反向传播使用链式法则，梯度会逐层相乘；如果每层的梯度都小于1，多次相乘后会指数级减小；结果：靠近输入层的权重几乎不更新，模型无法学习；模型训练停滞，损失不再下降；浅层（靠近输入）的特征无法得到有效学习 与梯度消失相反，梯度爆炸是指在反向传播过程中，梯度变得越来越大，最终导致数值溢出。；训练过程中损失...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：反向传播使用链式法则，梯度会逐层相乘；如果每层的梯度都小于1，多次相乘后会指数级减小；结果：靠近输入层的权重几乎不更新，模型无法学习；模型训练停滞，损失不再下降；浅层（靠近输入）的特征无法得到有效学习 与梯度消失相反，梯度爆炸是指在反向传播...
### RNN
- 通俗解释：按时间步处理序列，用隐藏状态把前面信息传到后面。
- 考试怎么考：常考隐藏状态、序列建模、梯度消失/爆炸。
- 易错点：RNN 不是一次性把所有时间步完全独立处理。
- 必记：当前输出依赖当前输入和上一时刻隐藏状态。
## 第五章 神经网络训练技巧深度解析 / 第2节 优化器改进与学习率调整
### 学习率
- 通俗解释：第2节 优化器改进与学习率调整；在解决了梯度消失/爆炸问题后，我们需要关注如何高效地收敛到最优解。学习率的选择直接影响优化效率：；学习率太小：收敛缓慢，训练时间过长；学习率太大：训练不稳定，损失可能发散；可以从两个维度优化学习率： Nesterov加速梯度 (NAG，Nesterov Acceler...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：第2节 优化器改进与学习率调整；在解决了梯度消失/爆炸问题后，我们需要关注如何高效地收敛到最优解。学习率的选择直接影响优化效率：；学习率太小：收敛缓慢，训练时间过长；学习率太大：训练不稳定，损失可能发散；可以从两个维度优化学习率： Nest...
### Adam
- 通俗解释：结合动量和自适应学习率思想，为不同参数调整更新幅度。
- 考试怎么考：常和 SGD、Momentum、RMSProp 对比优化器特点。
- 易错点：Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。
- 必记：Adam = 一阶动量 + 二阶矩估计。
### 优化器
- 通俗解释：第2节 优化器改进与学习率调整；在解决了梯度消失/爆炸问题后，我们需要关注如何高效地收敛到最优解。学习率的选择直接影响优化效率：；学习率太小：收敛缓慢，训练时间过长；学习率太大：训练不稳定，损失可能发散；可以从两个维度优化学习率： 动量解决了"方向"问题，但所有参数使用统一的学习率，可能出现：；参数...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：第2节 优化器改进与学习率调整；在解决了梯度消失/爆炸问题后，我们需要关注如何高效地收敛到最优解。学习率的选择直接影响优化效率：；学习率太小：收敛缓慢，训练时间过长；学习率太大：训练不稳定，损失可能发散；可以从两个维度优化学习率： 动量解决...
### Adagrad
- 通俗解释：动量解决了"方向"问题，但所有参数使用统一的学习率，可能出现：；参数A（梯度大）：统一学习率 → 更新过大 → 可能震荡；参数B（梯度小）：统一学习率 → 更新过小 → 更新缓慢；解决方案：自适应学习率；让每个参数拥有自己的学习率，根据历史梯度自动调整： Adagrad；一种自适应学习率算法（Duc...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：动量解决了"方向"问题，但所有参数使用统一的学习率，可能出现：；参数A（梯度大）：统一学习率 → 更新过大 → 可能震荡；参数B（梯度小）：统一学习率 → 更新过小 → 更新缓慢；解决方案：自适应学习率；让每个参数拥有自己的学习率，根据历史...
### RMSProp
- 通俗解释：用梯度平方的指数滑动平均调整学习率，缓解 Adagrad 学习率过快衰减。
- 考试怎么考：常考它和 Adagrad 的区别。
- 易错点：不要写成简单固定学习率下降。
- 必记：RMSProp 使用指数滑动平均保存近期梯度尺度。
### Momentum
- 通俗解释：动量（Momentum）；动量：迫使目标函数沿着"沟壑"快速下降的方法（冲量/“惯性”）；动量法的参数更新过程：；$$\boldsymbol{v}=\gamma \boldsymbol{v}-\alpha；abla_{\boldsymbol{\theta}} J\left(\boldsymbol{\...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：动量（Momentum）；动量：迫使目标函数沿着"沟壑"快速下降的方法（冲量/“惯性”）；动量法的参数更新过程：；$$\boldsymbol{v}=\gamma \boldsymbol{v}-\alpha；abla_{\boldsymbol...
### 学习率调度器
- 通俗解释：第2节 优化器改进与学习率调整；在解决了梯度消失/爆炸问题后，我们需要关注如何高效地收敛到最优解。学习率的选择直接影响优化效率：；学习率太小：收敛缓慢，训练时间过长；学习率太大：训练不稳定，损失可能发散；可以从两个维度优化学习率： 学习率调度的重要性；至此，我们已经介绍了多种自适应优化器（SGD+M...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：第2节 优化器改进与学习率调整；在解决了梯度消失/爆炸问题后，我们需要关注如何高效地收敛到最优解。学习率的选择直接影响优化效率：；学习率太小：收敛缓慢，训练时间过长；学习率太大：训练不稳定，损失可能发散；可以从两个维度优化学习率： 学习率调...
### 梯度下降
- 通俗解释：目标函数的形式通常是狭长且浅的沟壑；在深度网络中，目标函数的局部最优点经常以这种形式呈现，所以标准的随机梯度下降算法通常会在“沟壑”两侧反复震荡 优化器 | 核心思想 | 特点 | 适用场景；SGD | 基础梯度下降 | 简单，可能需要调整学习率 | 大规模数据、最终精调；SGD+Momentum...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：目标函数的形式通常是狭长且浅的沟壑；在深度网络中，目标函数的局部最优点经常以这种形式呈现，所以标准的随机梯度下降算法通常会在“沟壑”两侧反复震荡 优化器 | 核心思想 | 特点 | 适用场景；SGD | 基础梯度下降 | 简单，可能需要调整...
### Nesterov
- 通俗解释：Nesterov加速梯度 (NAG，Nesterov Accelerated Gradient)；Nesterov动量是对标准动量的改进，由俄罗斯数学家 Yurii Nesterov 在1983年提出的优化算法：；标准动量：先计算当前梯度，再与历史动量融合更新；Nesterov动量：先"冲"到预估位...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：Nesterov加速梯度 (NAG，Nesterov Accelerated Gradient)；Nesterov动量是对标准动量的改进，由俄罗斯数学家 Yurii Nesterov 在1983年提出的优化算法：；标准动量：先计算当前梯度...
### ResNet
- 通俗解释：用残差/跳跃连接让网络学习 F(x)+x，缓解深层网络退化和梯度传播困难。
- 考试怎么考：常考残差连接为什么能训练更深网络。
- 易错点：残差连接不是单纯增加层数，而是改变信息和梯度路径。
- 必记：ResNet 核心：y = F(x) + x。
## 第五章 神经网络训练技巧深度解析 / 第3节 处理过拟合的综合策略
### Dropout
- 通俗解释：训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。
- 考试怎么考：常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。
- 易错点：Dropout 不是提高模型容量，而是正则化。
- 必记：Dropout：训练随机失活，推理正常使用。
### 数据增强
- 通俗解释：三、数据增强（Data Augmentation） 除了从模型结构（Dropout）和训练策略（早停）防止过拟合，还可以从数据层面入手——通过数据增强扩充训练集的多样性；$\left\{\boldsymbol{x}^{(n)}, \boldsymbol{t}^{(n)}\right\}$表示原本的训...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：三、数据增强（Data Augmentation） 除了从模型结构（Dropout）和训练策略（早停）防止过拟合，还可以从数据层面入手——通过数据增强扩充训练集的多样性；$\left\{\boldsymbol{x}^{(n)}, \bold...
### 早停
- 通俗解释：当训练损失逐渐下降，但验证集损失逐渐上升时，主动停止优化。与Adagrad优化引起的“早停”是两回事（被动的）；监控验证集损失（或准确率）；如果连续patience个epoch没有改善，停止训练；恢复验证集表现最好的模型权重 除了从模型结构（Dropout）和训练策略（早停）防止过拟合，还可以从数据...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：当训练损失逐渐下降，但验证集损失逐渐上升时，主动停止优化。与Adagrad优化引起的“早停”是两回事（被动的）；监控验证集损失（或准确率）；如果连续patience个epoch没有改善，停止训练；恢复验证集表现最好的模型权重 除了从模型结构...
### 卷积
- 通俗解释：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。
- 考试怎么考：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。
- 易错点：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- 必记：输出尺寸：(输入尺寸 + 2P - K) / S + 1。
### 权重衰减
- 通俗解释：四、增加权重衰减（Weight Decay） 什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambda \sum_{i} \theta_i^2$$；$L_{lo...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：四、增加权重衰减（Weight Decay） 什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambd...
### 损失函数
- 通俗解释：什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambda \sum_{i} \theta_i^2$$；$L_{loss}$：原始损失（如交叉熵损失）；$\lam...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：什么是权重衰减？；权重衰减（Weight Decay），也称为L2正则化，是一种通过在损失函数中添加参数惩罚项来防止过拟合的技术。；$$L_{total} = L_{loss} + \lambda \sum_{i} \theta_i^2$$...
### Epoch
- 通俗解释：当训练损失逐渐下降，但验证集损失逐渐上升时，主动停止优化。与Adagrad优化引起的“早停”是两回事（被动的）；监控验证集损失（或准确率）；如果连续patience个epoch没有改善，停止训练；恢复验证集表现最好的模型权重
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：当训练损失逐渐下降，但验证集损失逐渐上升时，主动停止优化。与Adagrad优化引起的“早停”是两回事（被动的）；监控验证集损失（或准确率）；如果连续patience个epoch没有改善，停止训练；恢复验证集表现最好的模型权重
### Adagrad
- 通俗解释：当训练损失逐渐下降，但验证集损失逐渐上升时，主动停止优化。与Adagrad优化引起的“早停”是两回事（被动的）；监控验证集损失（或准确率）；如果连续patience个epoch没有改善，停止训练；恢复验证集表现最好的模型权重
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：当训练损失逐渐下降，但验证集损失逐渐上升时，主动停止优化。与Adagrad优化引起的“早停”是两回事（被动的）；监控验证集损失（或准确率）；如果连续patience个epoch没有改善，停止训练；恢复验证集表现最好的模型权重
### RNN
- 通俗解释：按时间步处理序列，用隐藏状态把前面信息传到后面。
- 考试怎么考：常考隐藏状态、序列建模、梯度消失/爆炸。
- 易错点：RNN 不是一次性把所有时间步完全独立处理。
- 必记：当前输出依赖当前输入和上一时刻隐藏状态。
### LSTM
- 通俗解释：通过输入门、遗忘门、输出门和细胞状态保存长期信息。
- 考试怎么考：常考门控作用，以及为什么能缓解长序列梯度问题。
- 易错点：不要漏掉细胞状态是 LSTM 的关键通道。
- 必记：LSTM：输入门、遗忘门、输出门、细胞状态。
## 第五章 神经网络训练技巧深度解析 / 第4节 超参数选取实践指南
### 学习率
- 通俗解释：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数 第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数 第1...
### Dropout
- 通俗解释：训练时随机丢弃一部分神经元，迫使网络不要过度依赖某些特征。
- 考试怎么考：常考训练阶段启用、推理阶段关闭，以及它缓解过拟合。
- 易错点：Dropout 不是提高模型容量，而是正则化。
- 必记：Dropout：训练随机失活，推理正常使用。
### 权重衰减
- 通俗解释：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数 第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步...
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数 第1...
### Batch
- 通俗解释：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数
### 优化器
- 通俗解释：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：类别 | 具体参数；模型架构 | 层数、每层的神经元数目；训练参数 | 学习率、批量大小（batch size）；正则化 | 权重衰减率（weight decay）、Dropout率；优化器 | 动量系数、Adam的$\beta$参数
### Adam
- 通俗解释：结合动量和自适应学习率思想，为不同参数调整更新幅度。
- 考试怎么考：常和 SGD、Momentum、RMSProp 对比优化器特点。
- 易错点：Adam 方便但不等于一定泛化最好，仍需调学习率等超参数。
- 必记：Adam = 一阶动量 + 二阶矩估计。
### 归一化
- 通俗解释：把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。
- 考试怎么考：常考图像预处理为什么要缩放像素值。
- 易错点：归一化不等于 BatchNorm，前者多是输入预处理。
- 必记：图像常先把像素值缩放到 0-1。
### Xavier
- 通俗解释：第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率
### 早停
- 通俗解释：第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率
### Momentum
- 通俗解释：第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率
- 考试怎么考：会考定义、作用、使用位置、和相近概念的区别。
- 易错点：不要脱离课件上下文孤立背诵。
- 必记：第1步：观察初始损失；确保损失计算正确；将权重衰减设为零；第2步：在小样本上过拟合；在少量样本上训练，尝试达到100%训练准确率
## 第五章 神经网络训练技巧深度解析
### 损失函数
- 通俗解释：第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Clipping（范数裁剪）、Backward Hooks（RNN...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Cli...
### 学习率
- 通俗解释：第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Clipping（范数裁剪）、Backward Hooks（RNN...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Cli...
### Batch
- 通俗解释：第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Clipping（范数裁剪）、Backward Hooks（RNN...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Cli...
### Sigmoid
- 通俗解释：第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Clipping（范数裁剪）、Backward Hooks（RNN...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Cli...
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
### 卷积
- 通俗解释：用小窗口在图像局部滑动，对局部像素和卷积核权重相乘求和，提取边缘、纹理等局部特征。
- 考试怎么考：常考输出尺寸、卷积核/步长/填充的作用，以及 CNN 实际常用互相关。
- 易错点：不要把 CNN 说成全连接；卷积核心是局部连接和参数共享。
- 必记：输出尺寸：(输入尺寸 + 2P - K) / S + 1。
### 归一化
- 通俗解释：把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。
- 考试怎么考：常考图像预处理为什么要缩放像素值。
- 易错点：归一化不等于 BatchNorm，前者多是输入预处理。
- 必记：图像常先把像素值缩放到 0-1。
### 梯度消失
- 通俗解释：第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Clipping（范数裁剪）、Backward Hooks（RNN...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Cli...
### 梯度爆炸
- 通俗解释：第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Clipping（范数裁剪）、Backward Hooks（RNN...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Cli...
### 梯度裁剪
- 通俗解释：第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Clipping（范数裁剪）、Backward Hooks（RNN...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：第一部分：梯度消失与梯度爆炸及解决方案；梯度消失：深层网络中靠近输入层的梯度逐层减小→0，导致浅层无法学习；梯度爆炸：梯度指数级增长，损失变为NaN，常见于深层网络和RNN；梯度裁剪：Value Clipping（元素级）、Norm Cli...
