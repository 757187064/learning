# 注意力机制及Transformer3-学生分发版-终版2026(3) / 总览
## 第六章（三）词嵌入与预训练模型
### Transformer
- 通俗解释：以注意力为核心的序列模型，用多头注意力、前馈网络、残差和归一化堆叠编码器/解码器。
- 考试怎么考：常考编码器/解码器结构、多头注意力和位置编码。
- 易错点：Transformer 不依赖 RNN 的逐步递归来建模序列。
- 必记：Transformer 核心：Attention + FFN + 残差 + Norm + 位置编码。
## 第六章（三）词嵌入与预训练模型 / 第1节 词嵌入的演进历程
### Transformer
- 通俗解释：以注意力为核心的序列模型，用多头注意力、前馈网络、残差和归一化堆叠编码器/解码器。
- 考试怎么考：常考编码器/解码器结构、多头注意力和位置编码。
- 易错点：Transformer 不依赖 RNN 的逐步递归来建模序列。
- 必记：Transformer 核心：Attention + FFN + 残差 + Norm + 位置编码。
## 第六章（三）词嵌入与预训练模型 / 第2节 预训练模型时代：BERT vs GPT
### Transformer
- 通俗解释：以注意力为核心的序列模型，用多头注意力、前馈网络、残差和归一化堆叠编码器/解码器。
- 考试怎么考：常考编码器/解码器结构、多头注意力和位置编码。
- 易错点：Transformer 不依赖 RNN 的逐步递归来建模序列。
- 必记：Transformer 核心：Attention + FFN + 残差 + Norm + 位置编码。
### Attention
- 通俗解释：第2节 预训练模型时代：BERT vs GPT；一、从词嵌入到预训练模型：质的飞跃；前面我们讨论了词嵌入技术的演进：One-Hot → Word2Vec → GloVe → ELMo。；这些技术的核心目标都是把词表示为向量。；但从 2018 年开始，NLP 的研究范式发生了根本转变： 词嵌入演进时间...
- 考试怎么考：会结合序列输入、隐藏状态、长距离依赖或门控结构考。
- 易错点：不要把序列模型当成普通独立样本分类；时间步之间有信息传递。
- 必记：第2节 预训练模型时代：BERT vs GPT；一、从词嵌入到预训练模型：质的飞跃；前面我们讨论了词嵌入技术的演进：One-Hot → Word2Vec → GloVe → ELMo。；这些技术的核心目标都是把词表示为向量。；但从 2018...
### Softmax
- 通俗解释：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。
- 考试怎么考：常考多分类输出、注意力权重为什么能加权求和。
- 易错点：Softmax 是对一组数整体归一化，不是逐个独立压缩。
- 必记：Softmax 输出非负且总和为 1。
### 标准化
- 通俗解释：把数据按均值和方差调整到更稳定的尺度，帮助模型训练。
- 考试怎么考：常考归一化和标准化的区别、为什么图像输入要预处理。
- 易错点：不要把标准化简单等同于除以 255；标准化通常涉及均值和标准差。
- 必记：标准化常写作 (x-mean)/std。
### Epoch
- 通俗解释：训练集被模型完整看过一遍，叫一个 epoch。一个 epoch 内通常包含很多个 batch 更新。
- 考试怎么考：常考 Epoch、Batch、Iteration 的区别，或判断训练轮数增加对欠拟合/过拟合的影响。
- 易错点：Epoch 不是一次参数更新；一次更新通常对应一个 batch。
- 必记：1 个 Epoch = 全部训练样本被用过一遍。
### Self-Attention
- 通俗解释：Q、K、V 来自同一个序列，让序列内部不同位置相互建立联系。
- 考试怎么考：常考它和 Cross-Attention 的区别。
- 易错点：Self 不是只看自己一个位置，而是同一序列内部互相看。
- 必记：自注意力：同一序列内部做注意力。
## 第六章（三）词嵌入与预训练模型 / 第4节 附录：Transformer 架构的现代演进（2022-2025）
### Transformer
- 通俗解释：以注意力为核心的序列模型，用多头注意力、前馈网络、残差和归一化堆叠编码器/解码器。
- 考试怎么考：常考编码器/解码器结构、多头注意力和位置编码。
- 易错点：Transformer 不依赖 RNN 的逐步递归来建模序列。
- 必记：Transformer 核心：Attention + FFN + 残差 + Norm + 位置编码。
### Attention
- 通俗解释：第4节 附录：Transformer 架构的现代演进（2022-2025）；从原始 Transformer 到现代大模型；前面我们学习的 Transformer 架构（2017）是后续所有大模型的基石。但从 2022 年开始，LLaMA、GPT-4、Qwen、Gemma、DeepSeek 等主流模型...
- 考试怎么考：会结合 Q/K/V、注意力权重、位置编码、mask 或编码器解码器结构考。
- 易错点：不要只背公式；要能说明每个张量来自哪里、为什么要 mask 或缩放。
- 必记：第4节 附录：Transformer 架构的现代演进（2022-2025）；从原始 Transformer 到现代大模型；前面我们学习的 Transformer 架构（2017）是后续所有大模型的基石。但从 2022 年开始，LLaMA、G...
### RoPE
- 通俗解释：一种旋转位置编码，把位置信息注入向量表示，常用于现代 Transformer。
- 考试怎么考：如果课件讲到，主要考它属于位置编码思想，不会要求复杂推导。
- 易错点：不要把 RoPE 写成普通绝对位置编号相加。
- 必记：RoPE 用旋转方式表达相对位置信息。
### 位置编码
- 通俗解释：一、当前 Decoder-Only Transformer 的事实标准架构；下面展示了 2024-2025 年主流大模型（LLaMA、Qwen、Gemma、DeepSeek 等）普遍采用的架构配方：；输入 → Token Embedding；[重复 N 层]；├── RMSNorm (pre-nor...
- 考试怎么考：会结合 Q/K/V、注意力权重、位置编码、mask 或编码器解码器结构考。
- 易错点：不要只背公式；要能说明每个张量来自哪里、为什么要 mask 或缩放。
- 必记：一、当前 Decoder-Only Transformer 的事实标准架构；下面展示了 2024-2025 年主流大模型（LLaMA、Qwen、Gemma、DeepSeek 等）普遍采用的架构配方：；输入 → Token Embedding...
### Softmax
- 通俗解释：把一组分数转成和为 1 的概率分布，常用于多分类或注意力权重。
- 考试怎么考：常考多分类输出、注意力权重为什么能加权求和。
- 易错点：Softmax 是对一组数整体归一化，不是逐个独立压缩。
- 必记：Softmax 输出非负且总和为 1。
### Multi-Head Attention
- 通俗解释：一、当前 Decoder-Only Transformer 的事实标准架构；下面展示了 2024-2025 年主流大模型（LLaMA、Qwen、Gemma、DeepSeek 等）普遍采用的架构配方：；输入 → Token Embedding；[重复 N 层]；├── RMSNorm (pre-nor...
- 考试怎么考：会结合 Q/K/V、注意力权重、位置编码、mask 或编码器解码器结构考。
- 易错点：不要只背公式；要能说明每个张量来自哪里、为什么要 mask 或缩放。
- 必记：一、当前 Decoder-Only Transformer 的事实标准架构；下面展示了 2024-2025 年主流大模型（LLaMA、Qwen、Gemma、DeepSeek 等）普遍采用的架构配方：；输入 → Token Embedding...
### 激活函数
- 通俗解释：给线性变换加入非线性，使网络能拟合非线性关系。
- 考试怎么考：常考没有激活函数时多层线性网络仍等价于线性模型。
- 易错点：不要把激活函数说成只改变维度，它主要提供非线性。
- 必记：仿射变换 + 激活函数 = 神经网络层的基本模式。
### 归一化
- 通俗解释：把数值缩放到较统一范围，例如图像像素从 0-255 缩放到 0-1。
- 考试怎么考：常考图像预处理为什么要缩放像素值。
- 易错点：归一化不等于 BatchNorm，前者多是输入预处理。
- 必记：图像常先把像素值缩放到 0-1。
### Query
- 通俗解释：五、注意力机制优化：更高效的 Attention；5.1 GQA (Grouped-Query Attention) —— 压缩 KV Cache；标准 Multi-Head Attention 中，每个头有自己的 K/V，导致 KV Cache 随头数线性增长。；GQA 让多个 Q 头共享同一组...
- 考试怎么考：会结合 Q/K/V、注意力权重、位置编码、mask 或编码器解码器结构考。
- 易错点：不要只背公式；要能说明每个张量来自哪里、为什么要 mask 或缩放。
- 必记：五、注意力机制优化：更高效的 Attention；5.1 GQA (Grouped-Query Attention) —— 压缩 KV Cache；标准 Multi-Head Attention 中，每个头有自己的 K/V，导致 KV Ca...
### Self-Attention
- 通俗解释：Q、K、V 来自同一个序列，让序列内部不同位置相互建立联系。
- 考试怎么考：常考它和 Cross-Attention 的区别。
- 易错点：Self 不是只看自己一个位置，而是同一序列内部互相看。
- 必记：自注意力：同一序列内部做注意力。
